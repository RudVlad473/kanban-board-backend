---
quick_id: 260820-g3u
type: quick
files_modified:
  - docs/CODE_STYLE.md
  - src/test/java/com/vrudenko/kanban_board/activitylog/HistoricalActivityEventReconstructorTest.java
  - src/test/java/com/vrudenko/kanban_board/controller/BoardControllerTest.java
  - src/test/java/com/vrudenko/kanban_board/controller/ColumnControllerTest.java
  - src/test/java/com/vrudenko/kanban_board/controller/SubtaskControllerTest.java
  - src/test/java/com/vrudenko/kanban_board/controller/TaskControllerTest.java
  - src/test/java/com/vrudenko/kanban_board/e2e/reset/ResetServiceE2ETest.java
  - gradle/verification-metadata.xml
  - .planning/todos/completed/2026-08-18-add-gradle-dependency-verification-metadata.md
  - .planning/todos/pending/2026-08-20-no-ci-check-for-stale-gradle-verification-metadata.md
---

<objective>
Pick off 4 small, well-scoped pending todos surfaced during the earlier `.planning/todos/`
triage, per user request to "iterate on all of these":

1. `docs/CODE_STYLE.md` rule 4's `.with(user(userId))` refusal-mechanism claims were measurably
   wrong (verified by an earlier quick task's own probe evidence) — correct the two false
   sentences.
2. `HistoricalActivityEventReconstructorTest` extends `AbstractKafkaContainerTest` but was the
   only such class in the repo not carrying `@Tag("kafka")`, so it ran (and started a real
   Redpanda container) inside the pre-commit `fastTest` gate on every commit.
3. Four `*ControllerTest` classes statically import `MockMvcResultHandlers.print` and call it
   bare (`.andDo(print())`), reading ambiguously against 38 call sites — qualify instead.
4. `ResetServiceE2ETest`'s `should_emptyBothStores_when_resetAllCalledAfterRealTraffic` is flaky
   in CI — root-caused live during this session (see SUMMARY for the full trace) and fixed.

Along the way, fixing #4 required a real local `compileTestJava` (the first since Phase 10's
`gradle/verification-metadata.xml` was generated), which surfaced a genuine gap in that file: a
missing `.pom` checksum for `guava-33.5.0-jre`. Fixed in the same pass; a new pending todo filed
for the still-missing CI staleness check that let it go undetected.
</objective>

<task id="1">
  <name>Correct docs/CODE_STYLE.md rule 4</name>
  <files>docs/CODE_STYLE.md</files>
  <action>Replace the two falsified claims (that the `sessionAuthenticationStrategy` bean enforces
  this MockMvc-shortcut ceiling, and that the refusal carries the RFC 7807 envelope) with the
  measured mechanism: `SessionManagementFilter`'s own DSL-composed
  `CompositeSessionAuthenticationStrategy` (a different instance from the bean), and a bare
  servlet `sendError`, not the `ProblemDetail` envelope.</action>
  <verify>Docs-only; no build verification needed.</verify>
  <done>Both sentences corrected in place.</done>
</task>

<task id="2">
  <name>Tag HistoricalActivityEventReconstructorTest as kafka</name>
  <files>src/test/java/com/vrudenko/kanban_board/activitylog/HistoricalActivityEventReconstructorTest.java</files>
  <action>Add `@Tag("kafka")`, matching every other `AbstractKafkaContainerTest` subclass in the
  repo (option 1 of the todo's three framed options — consistent with existing tier-placement
  convention).</action>
  <verify>`build/test-results/fastTest/` no longer contains this class after a `fastTest` run;
  `build/test-results/test/` still does, and its tests still pass.</verify>
  <done>Verified both ways live.</done>
</task>

<task id="3">
  <name>Qualify the bare print() static import</name>
  <files>src/test/java/com/vrudenko/kanban_board/controller/{Board,Column,Subtask,Task}ControllerTest.java</files>
  <action>Replace `import static ...MockMvcResultHandlers.print;` with a plain class import, and
  qualify all 41 call sites as `MockMvcResultHandlers.print()` (option 1 of the todo's three
  framed options — zero new tooling, matching the codebase's own judgment that a dedicated
  Checkstyle rule for one outlier method name is disproportionate).</action>
  <verify>`spotlessCheck` passes (import ordering); `compileTestJava` passes.</verify>
  <done>Verified.</done>
</task>

<task id="4">
  <name>Fix ResetServiceE2ETest's flaky race</name>
  <files>src/test/java/com/vrudenko/kanban_board/e2e/reset/ResetServiceE2ETest.java</files>
  <action>Root cause: `KafkaEventPublisher.onActivityEvent` is `@Async` on `AFTER_COMMIT`, so
  `createDomainFixture()`'s 4 domain-service calls return with no guarantee their activity events
  have reached the broker. `resetService.resetAll()` called immediately after can race a
  late-arriving publish past its own topic-trim step, which then gets consumed by the
  listener `resetAll()` restarts in its `finally` block and lands a stray row in `activity_log`
  after the Postgres truncate. Fix: await a RELATIVE gain of 4 rows (not a hardcoded absolute
  total, since sibling tests in the same class have the identical unawaited-publish gap and can
  contaminate a fixed-total wait) before calling `resetAll()`.</action>
  <verify>3 consecutive isolated `--tests '*ResetServiceE2ETest'` reruns, all clean; a full
  `./gradlew test` (474 tests) clean.</verify>
  <done>Verified live, multiple times.</done>
</task>

<task id="5">
  <name>Fix the gradle/verification-metadata.xml gap surfaced by Task 4</name>
  <files>gradle/verification-metadata.xml, .planning/todos/completed/2026-08-18-add-gradle-dependency-verification-metadata.md, .planning/todos/pending/2026-08-20-no-ci-check-for-stale-gradle-verification-metadata.md</files>
  <action>Regenerate via `./gradlew --write-verification-metadata sha256 compileTestJava`
  (targeting the actually-failing task, since a bare `help` invocation did not reproduce or fix
  it) — added the missing `guava-33.5.0-jre.pom` checksum. Corrected the earlier "already
  resolved" todo's resolution note with an addendum (not rewritten, per this repo's own
  convention) and filed a new pending todo for the still-missing CI staleness check that let this
  gap go undetected through every Phase 10 CI run.</action>
  <verify>`compileTestJava` passes (previously failed with `Dependency verification failed ...
  guava-33.5.0-jre.pom`).</verify>
  <done>Verified.</done>
</task>
