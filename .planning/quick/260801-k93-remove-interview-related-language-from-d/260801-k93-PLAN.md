---
phase: quick/260801-k93
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/CLAUDE.md
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/research/SUMMARY.md
  - .planning/research/STACK.md
  - .planning/research/PITFALLS.md
  - .planning/research/FEATURES.md
  - .planning/research/ARCHITECTURE.md
  - docs/plans/backend-modernization/README.md
  - docs/plans/backend-modernization/01-kafka-activity-feed.md
  - docs/plans/backend-modernization/02-n-plus-one-optimistic-locking.md
  - docs/plans/backend-modernization/03-flyway-openapi.md
  - docs/plans/backend-modernization/04-redis.md
  - docs/plans/backend-modernization/05-testcontainers.md
  - docs/plans/backend-modernization/06-observability.md
  - docs/plans/backend-modernization/07-kubernetes-stretch.md
autonomous: true
requirements: [QUICK-260801-k93]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No git-tracked file in the repository contains the string 'interview' (case-insensitive), except on lines that also carry this quick task's own `260801-k93` slug."
    - "Every rewritten sentence still parses as English: no dangling 'in an', 'if asked in', 'for ... purposes' fragments, no orphaned parentheticals, no stranded conjunctions left where a clause was removed."
    - "Every rewritten sentence preserves the original claim's meaning — the reason a technique is recommended, deferred, or rejected is unchanged; only the hiring-context framing is gone."
    - "Two section headings that were named for hiring context are renamed and still sit above their original, unmodified body text."
    - "No file under `src/` is modified — this change touches documentation and planning artifacts only."
    - "Markdown structure survives intact: table rows keep their pipe/column count, hard-wrapped paragraphs stay hard-wrapped at the file's existing width, and no heading level changes."
  artifacts:
    - ".claude/CLAUDE.md, .planning/PROJECT.md, .planning/STATE.md, .planning/REQUIREMENTS.md (project-level framing)"
    - ".planning/research/{SUMMARY,STACK,PITFALLS,FEATURES,ARCHITECTURE}.md (research narrative)"
    - "docs/plans/backend-modernization/{README,01..07}.md (modernization epic docs)"
  key_links:
    - "PROJECT.md core-value sentence -> STATE.md core-value sentence -> REQUIREMENTS.md core-value line -> .claude/CLAUDE.md core-value sentence (four copies of one phrase that must be reworded identically)"
    - "docs/plans/backend-modernization/README.md epic table -> the seven epic files it links (no anchor link targets either renamed heading, verified during planning)"
    - "the verification gate's `260801-k93` line filter -> this PLAN.md, the future SUMMARY.md, and the STATE.md quick-task row (the three places the literal term is legitimately allowed to survive)"
---

<!-- planner-discipline-allow: interview -->
<!-- Rationale: the acceptance gate negative-greps for the literal term, and this plan's actions must name
     that term to be executable. This is not self-invalidating: the gate filters out every output line whose
     path or text carries the `260801-k93` slug, and this file lives at
     .planning/quick/260801-k93-remove-interview-related-language-from-d/260801-k93-PLAN.md. Verified during
     planning: `git grep -In -i "interview" -- . | grep -v '260801-k93'` returns exactly 30 lines today and
     does not include this file. -->

<!-- planner-discipline-allow: technically defensible -->
<!-- Rationale: this literal is the chosen *replacement* wording, and every gate referencing it is a
     positive assertion — Task 1 requires `git grep -c "technically defensible"` to match in all 4 files,
     Task 2 requires it present in FEATURES.md. No acceptance criterion greps it to zero; the `-eq 0`
     comparisons sharing those verify lines belong to the unrelated `git status --porcelain -- src` and
     wrong-directory checks. -->

<objective>
Strip hiring-context framing ("interview-defensible", "interview-ready explanation", "what interviewers ask for", "interview-relevance") out of every git-tracked document in this repository, rewording each sentence so it still reads naturally and still makes the same technical claim.

Purpose: these documents are the durable technical record of the project — a research corpus, a seven-epic modernization plan, and the GSD planning artifacts. The hiring framing dates them to one moment and reads as noise to anyone else reading the repo. The engineering arguments underneath are sound on their own and should stand on their own.
Output: 17 documentation and planning files, 30 reworded lines, and a repo-wide gate proving zero remaining occurrences.

Documentation only — nothing under `src/` is touched, no build runs, no test runs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<interface_context>
Grounding facts verified against the live working tree during planning. Do not re-derive these; do not run a discovery grep before Task 1.

**The complete occurrence inventory — 30 lines across 17 git-tracked files.** Verified with `git grep -In -i "interview" -- . | grep -v '260801-k93' | wc -l` → `30`. Per-file line numbers:

| File | Lines |
|---|---|
| `.claude/CLAUDE.md` | 9 |
| `.planning/PROJECT.md` | 9, 37 |
| `.planning/STATE.md` | 25 |
| `.planning/REQUIREMENTS.md` | 4 |
| `.planning/research/SUMMARY.md` | 27 |
| `.planning/research/STACK.md` | 26, 45, 91, 152, 199 |
| `.planning/research/PITFALLS.md` | 21 |
| `.planning/research/FEATURES.md` | 27, 31, 48 |
| `.planning/research/ARCHITECTURE.md` | 207 |
| `docs/plans/backend-modernization/README.md` | 36, 42, 52 |
| `docs/plans/backend-modernization/01-kafka-activity-feed.md` | 12, 41, 49 |
| `docs/plans/backend-modernization/02-n-plus-one-optimistic-locking.md` | 63, 96, 99 |
| `docs/plans/backend-modernization/03-flyway-openapi.md` | 16 |
| `docs/plans/backend-modernization/04-redis.md` | 9 |
| `docs/plans/backend-modernization/05-testcontainers.md` | 21 |
| `docs/plans/backend-modernization/06-observability.md` | 8 |
| `docs/plans/backend-modernization/07-kubernetes-stretch.md` | 15 |

Line numbers were accurate at plan time, but rewording changes line lengths within a file. Re-locate each site by its surrounding text, not by offset, and work top-to-bottom within a file so earlier edits do not invalidate later line numbers.

**Zero source-code occurrences.** A repo-wide scan across all file types found the term in no `.java`, `.properties`, `.gradle`, `.yml` or `Dockerfile` content. Nothing under `src/` needs to change, and nothing under `src/` may change.

**Two headings are renamed, and nothing links to either.** `01-kafka-activity-feed.md:49` is `## Interview-ready explanation to have afterward`; `02-n-plus-one-optimistic-locking.md:96` is `## Interview-ready explanation`. A repo-wide search for `#interview` anchor references returned zero hits, so renaming both headings breaks no cross-reference. `README.md` links to each epic file by filename only, never to a heading anchor.

**One deliberate, documented exclusion — do not "fix" it.** `.claude/settings.local.json:4` contains a stale Claude Code permission entry referencing `docs/plans/interview-gap-plan`, the directory that was renamed to `backend-modernization` before this project began. That file is **gitignored** (`.gitignore:40`), is local machine config rather than documentation, and the entry is already dead (the directory it names no longer exists). It is out of scope: the verification gate is defined over git-tracked files precisely so this file is excluded. Do not edit it, and do not widen the gate to catch it.

**Wrapping conventions, which the rewrites must respect.** The seven epic docs and `README.md` under `docs/plans/backend-modernization/` are hard-wrapped at roughly 100 columns. The `.planning/` files are not hard-wrapped — those paragraphs are single long lines. Several occurrences sit inside markdown table cells (`STACK.md:26`, `STACK.md:91`, `STACK.md:199`, `FEATURES.md:31`, `FEATURES.md:48`, `ARCHITECTURE.md:207`, `README.md:42`); those must stay on one line with their pipe count unchanged.

**The verification gate, verified working during planning:**

```
git grep -In -i "interview" -- . | grep -v '260801-k93'
```

`git grep` searches the working tree but only git-tracked files, which is what excludes the gitignored `.claude/settings.local.json`, plus `build/`, `.gradle/` and `.git/`. The `grep -v '260801-k93'` filter drops every line whose printed path or text carries this quick task's slug — that covers this `PLAN.md`, the `SUMMARY.md` written at the end, and the `STATE.md` quick-task table row, whose Directory-link cell embeds the slug. Expected line counts as the plan progresses: **30 now → 25 after Task 1 → 14 after Task 2 → 0 after Task 3.**
</interface_context>

<tasks>

<task type="tracer">
  <name>Task 1: Reword the four project-level docs and prove the verification gate end-to-end</name>
  <files>.claude/CLAUDE.md, .planning/PROJECT.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
.claude/CLAUDE.md:1-12
.planning/PROJECT.md:1-12 and :33-40
.planning/STATE.md:20-28
.planning/REQUIREMENTS.md:1-8
Do not grep for occurrences first — the complete inventory is already stated in `&lt;interface_context&gt;`.
  </read_first>
  <action>
This is the thin end-to-end slice: it fixes the smallest group of files (one shared phrase, four copies) and then runs the full repo-wide gate for real, so the gate command and its self-reference filter are proven correct before the bulk rewording in Tasks 2 and 3. If the gate misbehaves here — if it reports this PLAN.md, or reports the gitignored settings file — stop and resolve that before touching any further file.

Use scoped `Edit` calls on all four files. Never use `Write` on any of them: `.planning/STATE.md` is live workflow state, `.planning/PROJECT.md` and `.planning/REQUIREMENTS.md` are project artifacts, and `.claude/CLAUDE.md` is the project instruction file. Change only the words named below; every other character in these four files must survive byte-identical.

Four files carry the same core-value phrase, `clean, independently reviewable, interview-defensible work`. Reword all four identically to `clean, independently reviewable, technically defensible work` — keeping the surrounding sentence untouched in each case. The sites:

- `.claude/CLAUDE.md:9` — the `**Core Value:**` line in the `## Project` section. Note this line still describes *two* Epic 2 deliverables including the "get full board" endpoint; that scope statement is stale but is NOT in scope for this task. Change the three words, leave the rest.
- `.planning/PROJECT.md:9` — the core-value paragraph, which continues with a parenthetical about the `/full` endpoint being narrowed to v2. Leave the parenthetical alone.
- `.planning/STATE.md:25` — the `**Core value:**` line under `## Project Reference`.
- `.planning/REQUIREMENTS.md:4` — the `**Core Value:**` line.

One further site, `.planning/PROJECT.md:37`, reads `the modernization plan's stated purpose is closing real technology gaps for interview-relevance (see [README.md](...))`. Reword the trailing fragment to `closing real technology gaps in the backend stack`, so the sentence becomes `...the modernization plan's stated purpose is closing real technology gaps in the backend stack (see [README.md](...)), but this specific GSD project is scoped purely to the remaining Epic 2 technical work.` Keep the markdown link and its relative path exactly as they are.

`technically defensible` is the chosen replacement for `interview-defensible` throughout this plan — do not invent per-file variants, since the value of these four lines is that they agree with each other.
  </action>
  <verify>
    <automated>! git grep -Ili "interview" -- .claude/CLAUDE.md .planning/PROJECT.md .planning/STATE.md .planning/REQUIREMENTS.md &amp;&amp; [ "$(git grep -c "technically defensible" -- .claude/CLAUDE.md .planning/PROJECT.md .planning/STATE.md .planning/REQUIREMENTS.md | wc -l)" -eq 4 ] &amp;&amp; git grep -q "closing real technology gaps in the backend stack" -- .planning/PROJECT.md &amp;&amp; git grep -q "independently reviewable" -- .planning/STATE.md &amp;&amp; [ "$(git grep -In -i "interview" -- . | grep -v '260801-k93' | wc -l)" -eq 25 ] &amp;&amp; [ "$(git grep -In -i "interview" -- . | grep -v '260801-k93' | grep -cv '^\.planning/research/\|^docs/plans/backend-modernization/')" -eq 0 ] &amp;&amp; [ "$(git status --porcelain -- src | wc -l)" -eq 0 ] &amp;&amp; echo GATE_PROVEN</automated>
  </verify>
  <done>All four project-level files are free of the term, and each contains the phrase `technically defensible`. `.planning/PROJECT.md` contains `closing real technology gaps in the backend stack`. The repo-wide gate `git grep -In -i "interview" -- . | grep -v '260801-k93'` now returns exactly 25 lines, every one of which lives under `.planning/research/` or `docs/plans/backend-modernization/` — proving the gate neither reports this plan's own files nor the gitignored `.claude/settings.local.json`. Nothing under `src/` is modified. The verify command prints `GATE_PROVEN`.</done>
</task>

<task type="auto">
  <name>Task 2: Reword the five research documents</name>
  <files>.planning/research/SUMMARY.md, .planning/research/STACK.md, .planning/research/PITFALLS.md, .planning/research/FEATURES.md, .planning/research/ARCHITECTURE.md</files>
  <read_first>
Read only the neighbourhood of each site listed below (a few lines either side is enough to see the sentence and whether it is a table row):
.planning/research/SUMMARY.md:25-29
.planning/research/STACK.md:24-28, :43-47, :89-93, :150-154, :197-201
.planning/research/PITFALLS.md:19-23
.planning/research/FEATURES.md:25-33, :46-50
.planning/research/ARCHITECTURE.md:205-209
These files are long; do not read them in full.
  </read_first>
  <action>
Eleven sites across five research documents. Use scoped `Edit` per site — never `Write` on these files. Six of the eleven sit inside markdown table cells; for those, the pipe count and single-line shape must survive exactly, so keep the replacement roughly the same length and never introduce a newline. None of these files are hard-wrapped, so prose paragraphs stay as single long lines.

The rule for every rewrite: keep the technical claim, drop the hiring frame. If a clause exists *only* to say "this is what a hiring conversation wants", delete the clause rather than paraphrasing it — but only when the surrounding sentence still stands on its own afterward.

The eleven sites and their replacements:

- `SUMMARY.md:27` — ends `...out of scope for this epic; mention only as interview color.` Replace the trailing fragment with `note only as background context.` so it reads `...out of scope for this epic; note only as background context.`
- `STACK.md:26` (table cell) — contains `explicitness aids the interview-narrative goal stated in the epic spec`. Replace `the interview-narrative goal` with `the explain-the-SQL goal`, giving `...explicitness aids the explain-the-SQL goal stated in the epic spec; use global config only if...`.
- `STACK.md:45` — ends `...which is precisely the failure mode interviewers probe for.` Replace with `...which is precisely the failure mode this epic exists to eliminate.`
- `STACK.md:91` (table cell) — contains `Worth mentioning in an interview as "the newer alternative" but not worth adopting here.` Replace with `Worth knowing as "the newer alternative" but not worth adopting here.` Preserve the inner double quotes.
- `STACK.md:152` — contains `for documentation/interview clarity`. Replace with `for documentation clarity`, giving `**Option B (more precise, if you want the epic's exact exception type explicit in the signature for documentation clarity):**`. Keep the bold markers and the parenthesis.
- `STACK.md:199` (table cell) — ends `mention MULTISET as the "next generation" alternative in the interview narrative only |`. Replace with `note MULTISET as the "next generation" alternative in the written rationale only |`. Keep the trailing pipe.
- `PITFALLS.md:21` — contains `two-query-stitch is more explicit/interview-legible if you want to show you understand exactly what SQL runs`. Replace with `two-query-stitch is more explicit and easier to reason about if you want certainty about exactly what SQL runs`. Keep the closing parenthesis that follows.
- `FEATURES.md:27` — ends `...not required to make Epic 2's deliverables correct or interview-defensible at this project's size.` Replace `interview-defensible` with `technically defensible`, matching Task 1's wording.
- `FEATURES.md:31` (table cell) — contains `Mention in interview as "I know Trello/Jira paginate at scale; didn't add it here because board sizes in this app don't warrant it — here's ...`. Replace the lead-in `Mention in interview as` with `Record the rationale as`, and drop the now-odd first-person `I know` so the quoted rationale reads `"Trello/Jira paginate at scale; didn't add it here because board sizes in this app don't warrant it — ..."`. Leave the remainder of the quoted string, including whatever follows the em dash, exactly as it is.
- `FEATURES.md:48` (table cell) — contains `that contrast is itself the interview-ready explanation the epic spec calls for`. Replace with `that contrast is itself the explanation the epic spec calls for`.
- `ARCHITECTURE.md:207` (table cell) — ends `...easiest to reason about, count in a test, and explain in an interview |`. Replace with `...easiest to reason about, count in a test, and explain to a reviewer |`. Keep the trailing pipe.
  </action>
  <verify>
    <automated>! git grep -Ili "interview" -- .planning/research &amp;&amp; git grep -q "note only as background context" -- .planning/research/SUMMARY.md &amp;&amp; git grep -q "explain-the-SQL goal" -- .planning/research/STACK.md &amp;&amp; git grep -q "this epic exists to eliminate" -- .planning/research/STACK.md &amp;&amp; git grep -q "for documentation clarity" -- .planning/research/STACK.md &amp;&amp; git grep -q "easier to reason about" -- .planning/research/PITFALLS.md &amp;&amp; git grep -q "technically defensible" -- .planning/research/FEATURES.md &amp;&amp; git grep -q "explain to a reviewer" -- .planning/research/ARCHITECTURE.md &amp;&amp; [ "$(git grep -In -i "interview" -- . | grep -v '260801-k93' | wc -l)" -eq 14 ] &amp;&amp; [ "$(git grep -In -i "interview" -- . | grep -v '260801-k93' | grep -cv '^docs/plans/backend-modernization/')" -eq 0 ] &amp;&amp; [ "$(git status --porcelain -- src | wc -l)" -eq 0 ] &amp;&amp; awk -F'|' '/^\|/ {print FILENAME": "NF}' .planning/research/STACK.md .planning/research/FEATURES.md .planning/research/ARCHITECTURE.md | sort -u | head -20 &amp;&amp; echo RESEARCH_OK</automated>
  </verify>
  <done>No file under `.planning/research/` contains the term. Each of the seven anchor phrases above is present in its file. The repo-wide gate returns exactly 14 lines, all of them under `docs/plans/backend-modernization/`. Every markdown table row in the three edited table-bearing files still has a consistent field count for its table (the `awk` output shows no row with an anomalous count). Nothing under `src/` is modified. The verify command prints `RESEARCH_OK`.</done>
</task>

<task type="auto">
  <name>Task 3: Reword the eight modernization epic docs and close the repo-wide sweep</name>
  <files>docs/plans/backend-modernization/README.md, docs/plans/backend-modernization/01-kafka-activity-feed.md, docs/plans/backend-modernization/02-n-plus-one-optimistic-locking.md, docs/plans/backend-modernization/03-flyway-openapi.md, docs/plans/backend-modernization/04-redis.md, docs/plans/backend-modernization/05-testcontainers.md, docs/plans/backend-modernization/06-observability.md, docs/plans/backend-modernization/07-kubernetes-stretch.md</files>
  <read_first>
Read only the neighbourhood of each site (these files are hard-wrapped at ~100 columns, so read enough to see the whole wrapped paragraph):
docs/plans/backend-modernization/README.md:32-38, :40-50, :48-54
docs/plans/backend-modernization/01-kafka-activity-feed.md:8-16, :38-44, :47-53
docs/plans/backend-modernization/02-n-plus-one-optimistic-locking.md:58-66, :94-102
docs/plans/backend-modernization/03-flyway-openapi.md:12-18
docs/plans/backend-modernization/04-redis.md:5-11
docs/plans/backend-modernization/05-testcontainers.md:18-23
docs/plans/backend-modernization/06-observability.md:4-11
docs/plans/backend-modernization/07-kubernetes-stretch.md:12-18
  </read_first>
  <action>
Fourteen sites across eight files, then the closing repo-wide sweep. Use scoped `Edit` per site — never `Write`. These files are hard-wrapped at roughly 100 columns; after each prose edit, re-wrap the affected paragraph to match so the diff stays to the lines that actually changed and no line runs conspicuously long or short. `README.md:42` is a table row and must stay on one line with its pipe count unchanged.

The fourteen sites:

**`01-kafka-activity-feed.md`**
- `:12` — `which is exactly the objection interviewers raise about resume-driven tech ("why does a kanban board need Kafka?")`. Delete the actor: `which is exactly the objection raised about resume-driven tech ("why does a kanban board need Kafka?")`. Keep the parenthetical question and the following `— have this answer ready either way` clause.
- `:41` — `This is the concrete "at-least-once + idempotency" story interviewers ask for.` Replace with `This is the concrete "at-least-once + idempotency" story worth being able to tell.`
- `:49` — the heading `## Interview-ready explanation to have afterward` becomes `## Explanation to have afterward`. The heading level and the body beneath it are unchanged.

**`02-n-plus-one-optimistic-locking.md`**
- `:63` — `this before/after pair is the single best interview artifact from this whole plan.` Replace with `this before/after pair is the single best demonstration artifact from this whole plan.`
- `:96` — the heading `## Interview-ready explanation` becomes `## Explanation to have afterward`, matching the heading now used in `01-kafka-activity-feed.md`. Heading level and body unchanged.
- `:99` — `(Finding 2) — that distinction itself is a good interview answer.` Replace with `(Finding 2) — that distinction itself is the key takeaway.` The sentence continues `Also be able to draw the before/after query count...` — leave that alone.

**`03-flyway-openapi.md`**
- `:16` — `is worth being able to state explicitly in an interview, since "why not let Hibernate auto-generate the schema in prod" is a common follow-up question.` Delete the two words: `is worth being able to state explicitly, since "why not let Hibernate auto-generate the schema in prod" is a common follow-up question.`

**`04-redis.md`**
- `:9` — `which is more defensible in an interview than one caching demo.` Replace with `which is more defensible than one caching demo.`

**`05-testcontainers.md`**
- `:21` — `which is a stronger claim than "H2 in-memory" if asked in an interview.` Replace with `which is a stronger claim than "H2 in-memory".` Do not leave the dangling `if asked`.

**`06-observability.md`**
- `:8` — `That's exactly the kind of "how would you know this is unhealthy in prod" question that comes up in system-design-flavored portions of EU-remote product-company interviews, and it's cheap to close.` Rewrite the whole clause as `That's exactly the kind of "how would you know this is unhealthy in prod" gap that any system-design review surfaces, and it's cheap to close.` This one spans wrapped lines 6-8; re-wrap the paragraph afterward.

**`07-kubernetes-stretch.md`**
- `:15` — `This is a local demonstration artifact for interview purposes, not a production migration.` Replace with `This is a local demonstration artifact, not a production migration.`

**`README.md`**
- `:36` — `Each is a separate, defensible unit of work you can describe individually in an interview ("tell me about a project where you...").` Both the clause and its parenthetical are hiring framing; the parenthetical must go with it rather than being left orphaned. Replace the sentence with `Each is a separate, defensible unit of work you can describe individually.` This is item 6 of a numbered list — preserve the list numbering and the two-space continuation indent of the wrapped line.
- `:42` (table row) — the Priority cell `Highest — biggest interview-relevance gain` becomes `Highest — biggest capability gain`. One line, pipe count unchanged.
- `:50-52` — the closing paragraph currently reads `Total realistic timeline: roughly **3–5 weeks** of evenings/weekends work, run in parallel with applying. Treat Epics 1–3 as the ones worth having *done* before backend-only/fintech-style interviews specifically, and the rest as ongoing.` Rewrite both sentences as `Total realistic timeline: roughly **3–5 weeks** of evenings/weekends work. Treat Epics 1–3 as the ones worth having *done* first, and the rest as ongoing.` — dropping `run in parallel with applying` too, since it is the same job-hunt framing and would read as a fragment once the following clause loses its object. Keep the `**` and `*` emphasis markers and the en dashes, and re-wrap to ~100 columns.

**Then close the sweep.** Run the gate over the whole repository, not just the files this plan named, so any occurrence in a file missed at plan time is caught:

`git grep -In -i "interview" -- . | grep -v '260801-k93'`

It must return nothing. If it returns a line in a file this plan did not name, reword that site by the same rule (keep the claim, drop the hiring frame) and note it in the summary as an addition to the planned set — unless it is under `src/`, in which case do NOT edit it: stop and report it, because this task is documentation-only.

**One last trap to avoid.** When writing this task's `SUMMARY.md`, and in any `STATE.md` prose describing this quick task, phrase the description so it does not itself reintroduce the literal term on a line that lacks the `260801-k93` slug — for example "Remove hiring-context framing from documentation" rather than a description containing the word being removed. The `STATE.md` quick-task table row is safe because its Directory-link cell embeds the slug; a `last_activity_desc` frontmatter line is not.
  </action>
  <verify>
    <automated>[ "$(git grep -In -i "interview" -- . | grep -v '260801-k93' | wc -l)" -eq 0 ] &amp;&amp; git grep -q "^## Explanation to have afterward$" -- docs/plans/backend-modernization/01-kafka-activity-feed.md &amp;&amp; git grep -q "^## Explanation to have afterward$" -- docs/plans/backend-modernization/02-n-plus-one-optimistic-locking.md &amp;&amp; git grep -q "single best demonstration artifact" -- docs/plans/backend-modernization/02-n-plus-one-optimistic-locking.md &amp;&amp; git grep -q "the key takeaway" -- docs/plans/backend-modernization/02-n-plus-one-optimistic-locking.md &amp;&amp; git grep -q "biggest capability gain" -- docs/plans/backend-modernization/README.md &amp;&amp; git grep -q "any system-design review surfaces" -- docs/plans/backend-modernization/06-observability.md &amp;&amp; ! git grep -Iln -i "if asked\|for interview purposes\|in parallel with applying" -- docs/plans/backend-modernization &amp;&amp; [ "$(git status --porcelain -- src | wc -l)" -eq 0 ] &amp;&amp; [ "$(git grep -c '^|' -- docs/plans/backend-modernization/README.md | cut -d: -f2)" -eq 10 ] &amp;&amp; echo SWEEP_CLEAN</automated>
  </verify>
  <done>The repo-wide gate `git grep -In -i "interview" -- . | grep -v '260801-k93'` returns zero lines. Both renamed headings read exactly `## Explanation to have afterward`. The anchor phrases `single best demonstration artifact`, `the key takeaway`, `biggest capability gain` and `any system-design review surfaces` are present. No dangling fragment (`if asked`, `for interview purposes`, `in parallel with applying`) survives anywhere under `docs/plans/backend-modernization/`. `README.md` still has its original 10 table lines, so the epic table survived intact. Nothing under `src/` is modified. The verify command prints `SWEEP_CLEAN`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Prose edits to markdown documentation and planning artifacts. No production or test source is touched, no dependency is added or changed, no configuration is altered, no runtime behaviour exists to affect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-01 | Tampering | `.planning/STATE.md` | medium | mitigate | `STATE.md` is live workflow state; a whole-file rewrite in pursuit of one phrase would destroy the position, metrics, decisions and quick-task ledger the workflow depends on. Task 1 forbids `Write` on it, scopes the change to three words on line 25, and its gate additionally requires the untouched marker phrase `independently reviewable` to still be present. |
| T-quick-02 | Tampering | `docs/plans/backend-modernization/*.md` | low | mitigate | An over-eager rewrite could silently drop a technical claim (why a technique is rejected, what the follow-up question is) while removing its framing. Every one of the 14 sites is specified as an exact before/after in Task 3 rather than left to paraphrase, and the gate pins four content anchors plus `README.md`'s table line count so structural loss fails verification. |
| T-quick-03 | Tampering | `.claude/settings.local.json` | low | accept | A stale permission entry naming a since-renamed directory matches the search term. It is gitignored local machine config, not documentation, and the referenced directory no longer exists so the entry is already inert. The gate is scoped to git-tracked files, which excludes it by construction; `<interface_context>` records this as a deliberate exclusion so it does not read as an undetected gap. |
| T-quick-04 | Repudiation | verification gate | low | mitigate | The gate greps for a literal that this plan and its summary must themselves contain, so a naive gate would either always fail or be quietly weakened. The `260801-k93` line filter confines the allowance to this task's own artifacts, and Task 3 explicitly requires the summary and any state prose to avoid reintroducing the literal on an unslugged line. |
| T-quick-SC | Tampering | package installs | low | accept | No package-manager install occurs in this plan — no npm/pip/cargo/Gradle dependency is added or changed, and no build or test is run. The supply-chain surface is unchanged, so the legitimacy gate does not apply. |
</threat_model>

<verification>
1. Task 1 prints `GATE_PROVEN`, Task 2 prints `RESEARCH_OK`, Task 3 prints `SWEEP_CLEAN`.
2. The gate count falls monotonically through the expected sequence 30 → 25 → 14 → 0, confirming each task closed exactly its own share and no earlier work regressed.
3. `git status --porcelain -- src` is empty at every task boundary — no application or test source was touched.
4. `git status --porcelain` lists at most the 17 files in `files_modified`, plus this quick task's own directory. No file outside that set changed.
5. `git diff --stat` shows a small, roughly balanced insertion/deletion count per file — a file showing wholesale replacement means a paragraph was rewritten or re-wrapped beyond the sentence in question, and should be re-inspected.
6. Read the final `git diff` for the eight `docs/plans/backend-modernization/` files end to end and confirm every rewritten sentence parses: no `if asked` with nothing after it, no `in an` left stranded, no orphaned `("tell me about a project where you...")`, no numbered-list item that lost its number or indent.
7. Both renamed headings still sit directly above their original body paragraphs, and `README.md` still links to all seven epic files by filename.
</verification>

<success_criteria>
- Zero git-tracked files in the repository contain the term, except on lines carrying this task's own `260801-k93` slug.
- All 30 known occurrences are reworded, not deleted wholesale: each host sentence still makes the technical point it made before, and the reason a technique is recommended, deferred, or rejected is unchanged everywhere.
- The four copies of the core-value phrase agree with each other on the same replacement wording.
- The two hiring-framed headings are renamed to the same neutral heading and their bodies are untouched; no cross-reference is broken.
- Markdown structure is intact: table rows keep their column counts, hard-wrapped files stay wrapped at their existing width, list numbering and indentation survive, and no heading level changes.
- Nothing under `src/` is modified, no dependency changes, and the gitignored `.claude/settings.local.json` is left alone as documented.
- The summary describing this work does not itself reintroduce the term outside a slug-carrying line.
</success_criteria>

<output>
Create `.planning/quick/260801-k93-remove-interview-related-language-from-d/260801-k93-SUMMARY.md` when done.
</output>
