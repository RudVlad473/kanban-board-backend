---
phase: quick-260804-oy8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/SESSION_LESSONS.md
  - .claude/CLAUDE.md
autonomous: true
requirements: [QUICK-260804-oy8]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - A developer or agent with only this repo (no access to Claude's external memory) can read both git-hygiene lessons from a git-tracked file.
    - Each lesson states what happened, the mechanism that caused it, and a concrete forward-going rule — in that order, under those three labels.
    - The doc names its source session (v1.2 Phase 4, 2026-08-04) so every claim is traceable back to STATE.md's Phase 04 record rather than being an unattributable assertion.
    - An agent reading .claude/CLAUDE.md before starting a phase-execution session is pointed at the doc.
  artifacts:
    - docs/SESSION_LESSONS.md
    - .claude/CLAUDE.md (one appended pointer bullet)
  key_links:
    - .claude/CLAUDE.md `## GSD Execution Directives` -> `../docs/SESSION_LESSONS.md` (relative link must resolve from the .claude/ directory, matching the existing `../docs/CODE_STYLE.md` reference)
    - docs/SESSION_LESSONS.md -> docs/CODE_STYLE.md (stated as sibling/complement so the scope split — how work is run vs. how Java is written — is explicit and neither doc grows into the other)
---

<objective>
Make two git-hygiene lessons from the 2026-08-04 v1.2 Phase 4 (Schema Registry) execution session durable in this repository's git history, rather than surviving only in an AI agent's external memory file (`feedback_gsd_git_hygiene.md`) that does not travel with the repo.

Purpose: both lessons cost real wall-clock time and created real risk during a live session. External agent memory is per-machine, per-agent, and invisible to human contributors and to code review — so the same mistakes are re-payable by any future session on any other machine. Committing them to `docs/` puts them under review, under version control, and in front of anyone who opens the repo.
Output: a new `docs/SESSION_LESSONS.md` (living, additive, CODE_STYLE.md-shaped) plus a one-line discoverability pointer from `.claude/CLAUDE.md`.
</objective>

<approach_analysis>

## Alternate Approaches Considered

**Approach A (picked): New standalone `docs/SESSION_LESSONS.md`, numbered-rule structure mirroring `docs/CODE_STYLE.md`, linked from `.claude/CLAUDE.md`.**

**Approach B: Append the two lessons as rules 10 and 11 inside the existing `docs/CODE_STYLE.md`.**

**Approach C: Record them only in `.planning/` — a GSD retrospective entry, a STATE.md decision line, or a todo file.**

## Trade-off Matrix

| Approach | Pros / Cons | Why Picked / Rejected |
|----------|-------------|----------------------|
| **A — new `docs/SESSION_LESSONS.md` + CLAUDE.md pointer** | **Pros:** clean scope boundary (process vs. Java style); grows additively without bloating an already 331-line style guide; sits beside `CODE_STYLE.md` and `LOCAL_DEV.md` where a contributor already looks; git-tracked and reviewable. **Cons:** a third `docs/` file to keep discoverable, which is why the CLAUDE.md pointer is part of this plan and not optional. | **PICKED.** The lessons are about *how work is run*, and the repo has no home for that yet. Creating one is the honest fix. |
| **B — append into `docs/CODE_STYLE.md`** | **Pros:** zero new files; the numbered-rule format already exists there. **Cons:** that file self-declares its scope in its opening line as code-style preferences for *writing Java*, judgement-level choices Spotless cannot check; its own "Adding a rule" contract mandates a bad-vs-good **code example**, which a git-workflow lesson cannot supply without faking one. | **REJECTED.** Would violate the target file's own stated scope and its own stated rule-shape contract on the first insertion. |
| **C — keep it in `.planning/` only** | **Pros:** no change to repo-facing docs; matches where GSD already records decisions. **Cons:** `.planning/` is GSD-internal machinery, invisible to a human contributor and to PR review; per-milestone artifacts there are archived at milestone close (`/gsd-cleanup` precedent), so a v1.2-scoped note stops being surfaced once v1.2 archives — the exact "durable and visible" property the task exists to buy. | **REJECTED.** Fails the stated goal. Note the STATE.md `### Decisions` entries stay as the *audit trail*; this doc is the *operating rule*. |

## Non-obvious trade-offs

- **Performance / build surface: none, deliberately.** `spotless` targets `src/**/*.java`, so a `docs/` markdown file is entirely outside `./gradlew spotlessCheck`'s scope; no Gradle run is required to verify this plan's content. Verification is grep-based and runs in under a second.
- **The real risk here is staleness, not correctness.** Lesson 1's *mechanism* is coupled to two things this repo does not own: Claude Code's current `isolation="worktree"` fork-from-`origin/HEAD` behavior, and GSD's `worktree-branch-check` exit-42 contract. If either changes upstream, the mechanism paragraph rots while the rule stays correct. Mitigation is structural, not aspirational: each lesson states the mechanism as a *dated observation* with the concrete exit code and commit count, so a future reader can re-verify the claim instead of trusting it — and can invalidate the mechanism without having to also relitigate the rule.
- **Security — information disclosure.** The source narrative is a live infrastructure session (Oracle Cloud, Neon, Redpanda, a local Postgres port conflict). The doc must carry process mechanisms only: no hostnames, connection strings, credentials, tokens, or absolute machine-local paths. Repo-relative paths and tool/exit-code names are fine.
- **Security — agent-instruction surface.** Appending to `.claude/CLAUDE.md` edits text that is auto-loaded into every future agent's context. The addition is therefore a single descriptive pointer with no imperative granting new capability or relaxing an existing guardrail.
- **Placement is load-bearing, not cosmetic.** The pointer goes in `## GSD Execution Directives` (after the `<!-- GSD:workflow-end -->` marker), because content inside the GSD-managed block is regenerated on update and would be silently lost — the same reasoning that placed the `.dev/gsd-run.sh` bullet there in quick task 260804-oq0.

## Data-flow mechanism (3 sentences)

Task 1 writes one new markdown file into `docs/`; nothing reads it at build or run time, so there is no code path to trace. Task 2 appends one bullet to `.claude/CLAUDE.md`, whose only consumer is an AI agent loading project instructions at session start — that bullet is the sole link making the new file discoverable. The single moving part with real behavior is the commit itself, which fires this repo's `.githooks/pre-commit` (`spotlessApply` then `./gradlew test --exclude-tests '*E2ETest'`) — several minutes of work on a change that alters zero Java.

</approach_analysis>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@docs/CODE_STYLE.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Write docs/SESSION_LESSONS.md with both git-hygiene lessons</name>
  <files>docs/SESSION_LESSONS.md</files>
  <read_first>
    - `docs/CODE_STYLE.md` — copy its document shape, not its content: a scope-setting intro paragraph, a `## Rules` container, numbered `###` sections, a bolded **Why** line inside each, and a closing "Adding a rule" contract section. Match its register: direct, second-person-free, explains the mechanism rather than asserting the conclusion.
    - `.planning/STATE.md` — the `### Decisions` list and the Phase 04 per-plan metrics table are the factual record this doc must stay consistent with (Phase 04 ran 4 plans; Plan 04 is the 130-minute entry).
  </read_first>
  <action>
Create `docs/SESSION_LESSONS.md` as a living, additive document recording operational lessons from running GSD workflows in this repo. Structure it as: title, scope paragraph, `## Lessons`, two numbered `###` lessons, then an `## Adding a lesson` contract section.

The scope paragraph must establish four things: this file records how work is *run* in this repo (the sibling to `docs/CODE_STYLE.md`, which records how Java is *written*); it is additive — new lessons are appended, never rewritten wholesale; these first two lessons were captured during the v1.2 Phase 4 (Schema Registry) execution session on 2026-08-04; and they previously existed only in an AI agent's machine-local external memory, which does not travel with the repository and is invisible to review — which is why they are here.

Each of the two lessons carries exactly three bolded labels in this order: **What happened**, **Why**, **The rule**. State this three-part shape once, in the `## Adding a lesson` section, and explicitly note it differs from `CODE_STYLE.md`'s rule shape (which requires a bad-vs-good Java example) because these lessons describe process, not code — so a reader appending a lesson does not go copy the wrong contract from the sibling file.

Lesson 1 — heading: pushing to origin periodically during an active phase-execution session.
- **What happened:** Wave 1 of Phase 4 dispatched its first worktree-isolated executor; that dispatch failed its own base check (`worktree-branch-check`, exit 42). The phase degraded to sequential execution for every remaining wave — including a wave of 2 plans with zero `files_modified` overlap, confirmed independently by the plan-checker, that was genuinely eligible for parallel execution. The cost was real wall-clock across the rest of the phase, not merely the one failed dispatch.
- **Why:** worktree isolation forks the new worktree from `origin/HEAD`, not from live local `HEAD`. The working tree carried 139 commits that had never been pushed, so the forked worktree's base was 139 commits behind the state the plans were written against; the base check refused it, correctly.
- **The rule:** push to origin before starting phase execution and again at wave boundaries during a long session — not only at milestone boundaries. Treat unpushed local commits as a hard precondition failure for worktree parallelism rather than as cosmetic backlog.

Lesson 2 — heading: not running ad-hoc git commands on the main tree while a sequential executor is mid-task.
- **What happened:** during a sequential (non-worktree) executor's ~67-minute task, an ad-hoc `git add` of `.planning/todos/...` plus `.planning/STATE.md` was run on that same tree, followed by `git commit`. The commit's pre-commit hook was killed by a tool timeout mid-flight, and by then the `git add` had already landed those paths in the same index that held the executor's own in-progress staged files.
- **Why:** a sequential executor works in the main working tree, so there is exactly one `.git/index` shared between it and any ad-hoc command — staging is global and there is no per-agent staging area. The executor's per-task commit is meant to be atomic (stage its own files, commit exactly those), so anything staged by anyone else inside that window either rides along into its commit or gets caught by a recovery aimed at something else.
- Record the recovery that worked, because it is the reusable part: a `git reset` scoped explicitly to the ad-hoc paths only, never a bare `git reset`, and never touching the executor's staged paths. It recovered cleanly — but the exposure to the executor's atomic commit was real, and the clean outcome should not be read as evidence the sequence was safe.
- **The rule:** writing files into the working tree is always safe during an executor run. `git add`, `git commit`, `git reset`, and `git stash` are not — hold them until the executor's completion notification confirms the tree is quiet. If something must be captured mid-run, write the file then and stage it after.
- Add the timeout corollary, since it is what converted a safe commit into a recovery: a `git commit` in this repo triggers `.githooks/pre-commit` (`spotlessApply`, then `./gradlew test --exclude-tests '*E2ETest'`), which takes minutes — give it a generous timeout instead of letting a default kill it partway through.

Constraints on content: process mechanisms only. No hostnames, connection strings, credentials, tokens, or absolute machine-local filesystem paths anywhere in the file — repo-relative paths, tool names, and exit codes only. Keep every concrete figure (139 commits, exit 42, 2 plans, ~67 minutes) intact and attributed to the dated session, since those specifics are what let a future reader re-verify a mechanism that depends on external tool behavior this repo does not control.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; test -f docs/SESSION_LESSONS.md &amp;&amp; test "$(grep -c '^### ' docs/SESSION_LESSONS.md)" = "2" &amp;&amp; test "$(grep -c '^\*\*What happened:\*\*' docs/SESSION_LESSONS.md)" = "2" &amp;&amp; test "$(grep -c '^\*\*Why:\*\*' docs/SESSION_LESSONS.md)" = "2" &amp;&amp; test "$(grep -c '^\*\*The rule:\*\*' docs/SESSION_LESSONS.md)" = "2" &amp;&amp; grep -q 'origin/HEAD' docs/SESSION_LESSONS.md &amp;&amp; grep -q 'exit 42' docs/SESSION_LESSONS.md &amp;&amp; grep -q '139' docs/SESSION_LESSONS.md &amp;&amp; grep -q '2026-08-04' docs/SESSION_LESSONS.md &amp;&amp; grep -q 'CODE_STYLE.md' docs/SESSION_LESSONS.md &amp;&amp; grep -q 'Adding a lesson' docs/SESSION_LESSONS.md &amp;&amp; echo TASK1_OK</automated>
    <human-check>Read the finished file end to end against `docs/CODE_STYLE.md` open beside it: does it read as the same author's sibling document, or as a pasted session log? Each lesson should explain the mechanism well enough that a reader who was not in the session could predict the failure recurring.</human-check>
  </verify>
  <done>`docs/SESSION_LESSONS.md` exists with exactly two numbered lessons, each carrying **What happened** / **Why** / **The rule**; it names the 2026-08-04 v1.2 Phase 4 session as its source, cites `origin/HEAD`, `exit 42`, 139 unpushed commits and the ~67-minute sequential task, cross-references `docs/CODE_STYLE.md` as the sibling scope, closes with an "Adding a lesson" contract, and contains no credentials, hostnames, or absolute machine-local paths.</done>
</task>

<task type="auto">
  <name>Task 2: Point .claude/CLAUDE.md at the new doc so it is actually found</name>
  <files>.claude/CLAUDE.md</files>
  <read_first>
    - `.claude/CLAUDE.md` lines 405-417 — the `<!-- GSD:workflow-end -->` marker and the `## GSD Execution Directives` bullet list that follows it.
    - `.claude/CLAUDE.md` line 119 — the existing `docs/CODE_STYLE.md` reference; copy its phrasing shape (backticked relative link, em-dash, then when to consult it).
  </read_first>
  <action>
Append one bullet to the end of the `## GSD Execution Directives` list in `.claude/CLAUDE.md`, after the existing "NEVER auto-execute code blocks" bullet. The bullet states that operational lessons from past GSD sessions — git hygiene during phase execution — are recorded in `docs/SESSION_LESSONS.md`, linked as `../docs/SESSION_LESSONS.md` (relative to `.claude/`, matching the existing `../docs/CODE_STYLE.md` link form), and that it should be read before starting or resuming a phase-execution session.

Placement is the point of this task: the bullet must land *after* the `<!-- GSD:workflow-end -->` marker, inside `## GSD Execution Directives`. Content above that marker is regenerated by GSD updates and the pointer would be silently dropped — the same reasoning that put the `.dev/gsd-run.sh` bullet in this section during quick task 260804-oq0.

Use `Edit` with a scoped replacement anchored on the existing "NEVER auto-execute" bullet. Do not rewrite the file, and do not touch anything inside the `<!-- GSD:workflow-start -->` / `<!-- GSD:workflow-end -->` block or the `<!-- GSD:profile-start -->` block. Keep the addition purely descriptive — it must not grant a capability or relax a guardrail, since this file loads into every future agent's context.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; grep -q 'SESSION_LESSONS.md' .claude/CLAUDE.md &amp;&amp; awk '/GSD:workflow-end/{seen=1} seen &amp;&amp; /SESSION_LESSONS\.md/{found=1} END{exit !found}' .claude/CLAUDE.md &amp;&amp; test -f "$(sed -n 's#.*(\.\./\(docs/SESSION_LESSONS\.md\)).*#\1#p' .claude/CLAUDE.md | head -1)" &amp;&amp; test "$(git diff --numstat .claude/CLAUDE.md | cut -f2)" = "0" &amp;&amp; echo TASK2_OK</automated>
  </verify>
  <done>`.claude/CLAUDE.md` carries exactly one added line (zero deletions, confirmed by `git diff --numstat`), positioned after `<!-- GSD:workflow-end -->` inside `## GSD Execution Directives`, whose `../docs/SESSION_LESSONS.md` relative link resolves to a file that exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live infra session -> committed public doc | Narrative sourced from a session touching Oracle Cloud, Neon, and Redpanda crosses into a git-tracked, potentially published file |
| committed doc -> future agent context | `.claude/CLAUDE.md` is auto-loaded into every future AI agent session; text added there is executed as instruction, not read as prose |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-oy8-01 | Information Disclosure | `docs/SESSION_LESSONS.md` | medium | mitigate | Task 1 action constrains content to process mechanisms; no hostnames, connection strings, credentials, tokens, or absolute machine-local paths. Task 1 `<done>` asserts their absence; the human-check read-through is the second pass. |
| T-oy8-02 | Tampering | `.claude/CLAUDE.md` agent-instruction surface | low | mitigate | Task 2 adds a single descriptive pointer granting no capability and relaxing no guardrail; `git diff --numstat` gate in `<verify>` proves exactly one added line and zero deletions, so no existing directive can be silently altered under cover of this change. |
| T-oy8-03 | Repudiation | lesson claims (139 commits, exit 42, ~67 min) | low | accept | Figures are attributed to a dated session and cross-checkable against `.planning/STATE.md`'s Phase 04 record and git history; no signing or stronger provenance is warranted for an internal process doc. |
| T-oy8-SC | Tampering | supply chain | n/a | accept | No npm/pip/cargo installs and no dependency changes in this plan — `build.gradle` is not touched. Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
Run from the repo root:

1. `test "$(grep -c '^### ' docs/SESSION_LESSONS.md)" = "2"` — both lessons present, no more and no fewer.
2. Each of `^\*\*What happened:\*\*`, `^\*\*Why:\*\*`, `^\*\*The rule:\*\*` appears exactly twice — the three-part shape holds for both lessons.
3. `awk '/GSD:workflow-end/{seen=1} seen && /SESSION_LESSONS\.md/{found=1} END{exit !found}' .claude/CLAUDE.md` — pointer survives a GSD regeneration of the managed block.
4. `git diff --numstat .claude/CLAUDE.md` shows `1  0` — one insertion, zero deletions.
5. No Gradle verification is required or expected: `spotless` targets `src/**/*.java`, and this plan changes zero Java.

Commit note (this is lesson 2's own corollary, applied to itself): the commit fires `.githooks/pre-commit`, which runs `spotlessApply` then `./gradlew test --exclude-tests '*E2ETest'` — minutes of work on a docs-only change. Give the `git commit` call an explicit generous timeout (600000 ms) rather than a default. A hook killed mid-flight is precisely the failure this plan is documenting.
</verification>

<success_criteria>
- `docs/SESSION_LESSONS.md` exists, is committed, and holds both lessons in the **What happened** / **Why** / **The rule** shape.
- A reader with only this repository — no access to any agent's external memory — can derive both concrete rules and the mechanism behind each.
- The doc is reachable from `.claude/CLAUDE.md` via a relative link that resolves, placed where a GSD update will not delete it.
- `docs/CODE_STYLE.md` is unmodified: the scope split between the two documents is stated, not blurred.
- Zero Java files changed; zero build configuration changed.
</success_criteria>

<output>
Create `.planning/quick/260804-oy8-create-docs-session-lessons-md-capturing/260804-oy8-SUMMARY.md` when done.
</output>
