---
phase: quick-260813-k47
plan: 01
subsystem: testing
tags: [spring-security, mockmvc, session-management, docs, code-style]

# Dependency graph
requires: []
provides:
  - "docs/CODE_STYLE.md rule 4: documented `.with(user())` two-call-per-principal session ceiling, the MAX_CONCURRENT_SESSIONS mechanism, measured symptom, test-method boundary, and signinCookie() alternative"
  - "AbstractAppMockMvcTest class Javadoc: short pointer to the same ceiling, next to signinCookie()"
  - "Closed source todo (2026-08-10) with a Resolution section correcting its AuthorizationGatingTest premise"
  - "New pending todo tracking the unreconciled SessionManagementFilter attribution between the source todo/InjectionAttemptTest and SecurityConfiguration's own comment"
affects: [testing, security, code-style]

# Actuals (#2632)
actuals:
  tokens: 3200
  tasks: 1
  commits: 1

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Documentation-only fix that deliberately declines to repeat an unreconciled mechanism claim (SessionManagementFilter attribution), instead filing a tracked follow-up todo rather than guessing"

key-files:
  created:
    - .planning/todos/pending/2026-08-13-reconcile-contradictory-session-filter-attribution-in-two-.md
  modified:
    - docs/CODE_STYLE.md
    - src/test/java/com/vrudenko/kanban_board/support/fixtures/AbstractAppMockMvcTest.java
    - .planning/todos/completed/2026-08-10-decide-whether-to-close-the-with-user-vs-max-concurrent-se.md

key-decisions:
  - "D-01/D-02: full worked account lands in docs/CODE_STYLE.md rule 4 (new 5th paragraph, no new rule 14); AbstractAppMockMvcTest's class Javadoc gets a short pointer only, deferring to rule 4."
  - "D-03: neither new paragraph names a servlet filter as the enforcer of the session ceiling — the repo carries two mutually inconsistent claims on that exact point (source todo/InjectionAttemptTest vs. SecurityConfiguration's own comment), and this task declines to mint a third unverified copy."
  - "D-05: corrected the source todo's premise during planning (grep-confirmed) — AuthorizationGatingTest calls signinCookie() zero times and stays correctly on the .with(user()) shortcut; InjectionAttemptTest is the sole cookie-replay reference (25 calls)."
  - "D-09: filed a new pending todo for the SessionManagementFilter attribution contradiction rather than adjudicating it here — resolving it needs a filter-chain probe, not a doc edit."

patterns-established:
  - "When two existing claims in the repo contradict each other on a mechanism, new documentation states only what is directly verifiable and files a tracked todo for the contradiction rather than repeating either claim."

requirements-completed: [QUICK-260813-K47-SESSIONCEILINGDOC]

coverage:
  - id: D1
    description: "docs/CODE_STYLE.md rule 4 documents the .with(user()) two-call-per-principal session ceiling: mechanism, MAX_CONCURRENT_SESSIONS refusal, measured 200/200/401/401 symptom, per-test-method boundary, signinCookie() alternative, and correct InjectionAttemptTest/AuthorizationGatingTest worked examples — naming no servlet filter"
    requirement: "QUICK-260813-K47-SESSIONCEILINGDOC"
    verification:
      - kind: other
        ref: "automated verify block (grep gates for MAX_CONCURRENT_SESSIONS, 200/200/401/401, InjectionAttemptTest, AuthorizationGatingTest, signinCookie, zero SessionManagementFilter, 13 rule headings) — all passed"
        status: pass
    human_judgment: false
  - id: D2
    description: "AbstractAppMockMvcTest's class Javadoc carries a new short <p> paragraph stating the ceiling and pointing at signinCookie() and rule 4"
    requirement: "QUICK-260813-K47-SESSIONCEILINGDOC"
    verification:
      - kind: other
        ref: "grep gates on the class Javadoc (MAX_CONCURRENT_SESSIONS present, zero SessionManagementFilter, signinCookie references >=5) plus ./gradlew spotlessCheck compileTestJava — all passed"
        status: pass
    human_judgment: false
  - id: D3
    description: "Source todo closed with a Resolution section; new follow-up todo filed for the unreconciled filter-attribution contradiction"
    requirement: "QUICK-260813-K47-SESSIONCEILINGDOC"
    verification:
      - kind: other
        ref: "file-existence gates: source todo absent from pending/, present in completed/ with ## Resolution; new todo present in pending/"
        status: pass
    human_judgment: false

duration: 40min
completed: 2026-08-13
status: complete
---

# Phase quick-260813-k47 Plan 01: Document the `.with(user())` vs. `MAX_CONCURRENT_SESSIONS` session ceiling Summary

**Documented that `.with(user(userId))` may authenticate at most two requests per principal per test method before Spring Security's `MAX_CONCURRENT_SESSIONS = 2` ceiling rejects the third with a wrong-password-shaped 401 — in both `docs/CODE_STYLE.md` rule 4 (full account) and `AbstractAppMockMvcTest`'s class Javadoc (short pointer) — while deliberately declining to repeat an unreconciled claim about which component enforces it.**

## Performance

- **Duration:** ~40 min
- **Tasks:** 1
- **Files modified:** 4 (1 `.java`, 1 `.md` rule file, 1 todo relocated+edited, 1 new todo)

## Accomplishments

- `docs/CODE_STYLE.md` rule 4 grew a new bolded paragraph (5th in the rule, positioned between the base-class paragraph and the pre-commit-gate paragraph) documenting: the mechanism (`SecurityMockMvcRequestPostProcessors.user(userId)` creates a brand-new session per call), the ceiling (`MAX_CONCURRENT_SESSIONS = 2` + `maxSessionsPreventsLogin(true)`), the measured symptom (`200, 200, 401, 401`, deliberately indistinguishable from a wrong password), the per-test-method boundary (`AbstractAppTest`'s `@BeforeEach` mints a fresh principal each method), the `signinCookie()` alternative for 3+ calls, and corrected worked examples (`InjectionAttemptTest` for cookie-replay, `AuthorizationGatingTest` for the shortcut done correctly).
- `AbstractAppMockMvcTest`'s class Javadoc grew a short `<p>` paragraph next to `signinCookie()`'s own definition, stating the same ceiling and deferring to rule 4.
- The source todo (`2026-08-10-decide-whether-to-close-the-with-user-vs-max-concurrent-se.md`) was moved to `completed/` with a `## Resolution` section recording the chosen option, both landing sites, and the `AuthorizationGatingTest` premise correction.
- A new pending todo was filed tracking the unreconciled `SessionManagementFilter` attribution between the source todo/`InjectionAttemptTest`'s Javadoc and `SecurityConfiguration`'s own comment — deliberately not adjudicated by this task.

## Task Commits

1. **Task 1: Document the `.with(user())` session ceiling in rule 4 and `AbstractAppMockMvcTest`, then close the source todo** - `703e232` (docs)

**Plan metadata:** committed separately by the orchestrator per execution contract (STATE.md/ROADMAP.md/REQUIREMENTS.md doc commit).

## Files Created/Modified

- `docs/CODE_STYLE.md` - rule 4 gained one new bolded paragraph (no rule 14, no renumbering; still 13 `### ` headings)
- `src/test/java/com/vrudenko/kanban_board/support/fixtures/AbstractAppMockMvcTest.java` - class Javadoc gained one new `<p>` paragraph; no code, field, or method touched
- `.planning/todos/completed/2026-08-10-decide-whether-to-close-the-with-user-vs-max-concurrent-se.md` - relocated from `pending/`, `resolved` date added, `## Resolution` section appended
- `.planning/todos/pending/2026-08-13-reconcile-contradictory-session-filter-attribution-in-two-.md` - new follow-up todo

## Decisions Made

- D-01/D-02 (locked in plan): full account in rule 4, short pointer in `AbstractAppMockMvcTest`'s Javadoc — not a new rule 14, not a Javadoc-only fix. Followed exactly.
- D-03 (locked in plan): named no servlet filter as the enforcer anywhere in either new paragraph — attributed enforcement to the ceiling constant and the `sessionAuthenticationStrategy` bean's live-session count instead. Verified by the automated gate (`grep -c 'SessionManagementFilter'` = 0 in both files).
- D-05 (locked in plan, grep-reconfirmed during execution): cited `InjectionAttemptTest` as the sole cookie-replay reference and `AuthorizationGatingTest` as the counterpart that correctly keeps the shortcut, correcting the source todo's premise rather than transcribing it.
- D-09 (locked in plan): filed the filter-attribution contradiction as its own pending todo instead of resolving or silently absorbing it.

## Deviations from Plan

None — plan executed exactly as written. All four edits (rule 4 paragraph, Javadoc paragraph, source-todo closure, follow-up todo) match the plan's specified content, anchors, and prose voice.

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Cleared a stale, locked `build/test-results/fastTest/binary` directory blocking the pre-commit hook**
- **Found during:** Task 1 commit (the `.githooks/pre-commit` hook's `fastTest` run)
- **Issue:** Two consecutive commit attempts failed with `java.io.IOException: Unable to delete directory ... build\test-results\fastTest\binary` — a stale Gradle test-results directory from an earlier interrupted invocation held a Windows file lock that a fresh `fastTest` run couldn't clear on its own.
- **Fix:** Ran `./gradlew --stop` to release any daemon-held file handles, then `rm -rf build/test-results/fastTest/binary` to clear the stale directory by hand. The retried commit then ran `fastTest` to completion and succeeded.
- **Files modified:** none (build-output directory only, not version-controlled)
- **Verification:** Third commit attempt completed with exit code 0; `git log --oneline -1` shows `703e232` landed.
- **Committed in:** n/a (pre-commit environment fix, not a source change)

---

**Total deviations:** 1 auto-fixed (1 blocking, environment-only — no source or doc content changed as a result)
**Impact on plan:** None on the plan's actual deliverables; this was purely a local Windows file-lock issue in the pre-commit hook's test run, unrelated to the documentation content.

## Issues Encountered

The `.githooks/pre-commit` hook runs `./gradlew test --exclude-tests '*E2ETest'` (aliased `fastTest`) on every commit, per `docs/SESSION_LESSONS.md`/CLAUDE.md convention. The first two commit attempts failed at the `fastTest` task itself with a Windows-specific `IOException` deleting a leftover `output.bin` from a prior interrupted Gradle invocation (the very first `./gradlew spotlessCheck compileTestJava` verification run, and an earlier 2-minute-timeout commit attempt, both left the daemon holding an open handle). Resolved by stopping the Gradle daemon and manually clearing the stale directory before retrying; the third attempt ran the full `fastTest` suite clean and committed successfully.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The `.with(user())` session-ceiling interaction is now documented in both places a test author will plausibly be standing: `docs/CODE_STYLE.md` rule 4 (before writing) and `AbstractAppMockMvcTest`'s class Javadoc (while writing, next to `signinCookie()`).
- One new pending todo (`2026-08-13-reconcile-contradictory-session-filter-attribution-in-two-.md`) remains open for a future session: reconciling whether `SessionManagementFilter` or the explicit `AuthenticationController.authenticate` call site is the actual enforcer of the session ceiling on a MockMvc-injected-principal request. Needs a filter-chain probe, not a doc edit.
- No blockers for continuing Phase 5 (Infra Migration), which remains the project's current focus per STATE.md.

---
*Phase: quick-260813-k47*
*Completed: 2026-08-13*

## Self-Check: PASSED

All 4 claimed files found on disk; commit `703e232` found in `git log --oneline --all`.
