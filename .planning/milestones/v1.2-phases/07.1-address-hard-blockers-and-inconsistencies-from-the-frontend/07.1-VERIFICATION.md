---
phase: 07.1-address-hard-blockers-and-inconsistencies-from-the-frontend
verified: 2026-08-17T11:24:41Z
status: passed
score: 26/26 must-haves verified (D-01..D-26 plus ROADMAP-201)
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: missing (no 07.1-VERIFICATION.md existed before this run)
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 07.1: Address hard blockers and inconsistencies from the frontend-integration-readiness audit — Verification Report

**Phase Goal:** Every error the API returns is one consistent RFC 7807 envelope with a stable
machine-readable code; 401 means unauthenticated and 403 means forbidden, with no overlap; a local
frontend dev server can make credentialed cross-origin calls; signin/signup actually validate their
request bodies; every resource-creating POST returns 201; Board carries the same optimistic locking
Column/Task/Subtask already have; and adversarial injection resistance plus complete auth gating are
proven by tests rather than assumed.

**Verified:** 2026-08-17T11:24:41Z
**Status:** passed
**Re-verification:** No — this is the first verification of this phase (it was executed 2026-08-09
through 2026-08-10, but no VERIFICATION.md was ever produced; this run closes that gap discovered
during milestone-close readiness checking).

**Scope note:** This phase predates REQUIREMENTS.md traceability — its scope is D-01 through D-26
plus ROADMAP-201, all recorded in `07.1-CONTEXT.md`. Every claim below was checked directly against
the current codebase (not SUMMARY.md prose): production files were read in full, and 6 test classes
covering every decision cluster were actually executed (`./gradlew test --tests ...`, real
Testcontainers PostgreSQL, no mocks) rather than trusted from prior session logs.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Every `GlobalExceptionHandler` branch returns an RFC 7807 `ProblemDetail` with a stable `code` (D-01, D-02, D-03) | VERIFIED | `GlobalExceptionHandler.java` — all 13 `@ExceptionHandler` methods return `ResponseEntity<ProblemDetail>` built via `ProblemDetail.forStatusAndDetail(...).setProperty(ErrorCode.CODE_PROPERTY, ...)`; field-validation errors nest under `errors` (`ErrorCode.ERRORS_PROPERTY`). `ErrorCode` enum (12 members) confirmed at `constant/ErrorCode.java`. Ran `GlobalExceptionHandlerTest` live: 9/9 nested tests pass (0 failures/errors) — `EntityNotFoundTest`, `DuplicateResourceTest`, `AccessDeniedTest`, `MalformedRequestBodyTest`, `OptimisticLockConflictTest`, `UnauthenticatedTest` (3), `ValidationFailedTest` |
| 2 | A cross-user ownership rejection returns 403, a wrong-password signin still returns 401, a genuinely unauthenticated request returns 401 — no overlap (D-04, D-05) | VERIFIED | `handleAccessDeniedException`/`handleAppAccessDeniedException` map to `HttpStatus.FORBIDDEN` with `ACCESS_DENIED`; `handleBadCredentialsException` untouched at `UNAUTHORIZED`/`BAD_CREDENTIALS`. New `ProblemDetailAuthenticationEntryPoint` (`security/ProblemDetailAuthenticationEntryPoint.java`) wired via `http.exceptionHandling(handling -> handling.authenticationEntryPoint(...))` in `SecurityConfiguration.java` — confirmed present, not merely claimed. `GlobalExceptionHandlerTest$UnauthenticatedTest` (3/3 pass) proves the 401 body matches the 403 body's key set |
| 3 | A local frontend dev server (`localhost:5173`/`3000`) can make credentialed cross-origin calls (D-10, D-11, D-12) | VERIFIED | `config/CorsConfig.java`: `CorsConfigurationSource` bean, `app.cors.allowed-origins` property (`application.properties`), methods `GET/POST/PUT/PATCH/DELETE`, `allowCredentials(true)`, no wildcard. `SecurityConfiguration.java`'s `http.cors(Customizer.withDefaults())` untouched (auto-detects the bean). Ran `CorsConfigTest$CorsConfigurationSourceTest` live: 1/1 pass |
| 4 | Signin/signup actually validate their request bodies; malformed input is 400, duplicate signup email is 409, anti-enumeration on wrong-password/unknown-email signin is preserved (D-06..D-09) | VERIFIED | `@Valid` present on both `AuthenticationController.signin`/`signup` `@RequestBody` params. `UserRepository.existsByEmail` + `AppDuplicateResourceException.withMessage(...)` guard in `UserService.save`. `SignupRequestDTO`/`SigninRequestDTO` carry `@AppEmail`/`@Password`/`@DisplayName`. Ran `AuthenticationTest` live: 24/24 nested tests pass across `Signin.FieldValidation`, `Signin.AntiEnumeration`, `Signup.FieldValidation`, `Signup.DuplicateEmail`, `Signup.Authenticated`, etc. |
| 5 | Every resource-creating POST returns 201 with a `Location` header (ROADMAP-201) | VERIFIED | Grep of every `@PostMapping` in `controller/*.java` + `AuthenticationController`: board (`BoardController.save`), column (`ColumnController.addTaskByColumnId`), task-creates-subtask-route (`TaskController.addSubtaskByTaskId`), board-creates-column-route (`BoardController.addColumnByBoardId`) and signup all return `ResponseEntity.created(...)`. `POST /signin` correctly stays `ResponseEntity.ok()` (not a creation). No `@PostMapping` method returns `ResponseEntity.ok()` for a resource-creating route |
| 6 | Board carries the same `@Version` optimistic locking Column/Task/Subtask already have (D-13, D-14, D-15) | VERIFIED | `BoardEntity.java:63-65` — `@Version private Long version;`. `V7__add_board_optimistic_locking_version_column.sql` exists, additive `ALTER TABLE boards ADD COLUMN version bigint NOT NULL DEFAULT 0`. `UpdateBoardRequestDTO`/`BoardResponseDTO`/`BoardFullResponseDTO` all carry `version`. `UserEntity` deliberately has no `@Version` (D-14, confirmed absent). Ran `BoardLockingTest` live: 5/5 pass across `ConcurrentRename`, `MissingVersion`, `VersionExposure` |
| 7 | Adversarial injection resistance is proven by tests, not assumed (D-16, D-17, D-18) | VERIFIED | `security/InjectionAttemptTest.java` (871 lines) exists, no `@Tag` (runs in `fastTest` per D-22), real `MockMvc` + Testcontainers Postgres (no mocked repository). Ran live: 32/32 pass across `SqlInjection` (6), `StoredXss` (4), `OversizedBoundary` (10), `MalformedPathVariable` (12) |
| 8 | Every protected route is swept for both unauthenticated (401) and cross-user (403) rejection, with a completeness guard (D-19, D-20) | VERIFIED | `security/AuthorizationGatingTest.java` (637 lines), no `@Tag`, 22-row route table, reflective `RequestMappingHandlerMapping`-based completeness guard. Ran live: 47/47 pass across `NoSessionSweep` (22), `CrossUserSweep` (18), `ScopedToCaller` (4), `PermitAllExclusions` (2), `Completeness` (1) |
| 9 | `fastTest` pre-commit gate selects by JUnit 5 `@Tag`, not class-name substring (D-21, D-22, D-23) | VERIFIED | `build.gradle:318` — `excludeTags 'kafka', 'realSocket'` on the `fastTest` task, no `excludeTestsMatching` filter remaining. All 12 remaining `*E2ETest`-suffixed classes independently confirmed to carry `@Tag("kafka")` or `@Tag("realSocket")` (grep-verified per class). `UserAuthenticationProvider.java` comment cites `AuthenticationTest.SigninPersistence#shouldNotPersistBcryptHash_whenSigninSucceeds` — nested class and method confirmed to exist |
| 10 | `/claude-security` scan run and every finding fixed or deferred with rationale (D-24); 4 Mermaid sequence diagrams document the settled system (D-25, D-26) | VERIFIED | F3 fix (dead `SecurityConstants.SESSION_NAME` static field causing `POST /logout` 500) confirmed real: `SecurityConstants.java` no longer exists in `src/main`; `SecurityConfiguration.java` reads `sessionCookieName` via a real `@Value`-injected instance field instead. 5 deferred findings each have a todo file (4 have since been resolved by later commits — `git log` shows `260811-ezy`/`260811-h2v`/`260811-s5e` closing F1/F6/doc-reconciliation todos — confirming the deferrals were tracked, not dropped). `docs/diagrams/architecture-{signin-scenario,error-response-split,mutation-sequence,activity-feed-read}.mmd` all exist and start with `sequenceDiagram`; `docs/ARCHITECTURE.md` references all four (now as rendered PNG + linked `.mmd` source, a later Phase-5 improvement over the original inline fenced blocks — `git log -- docs/ARCHITECTURE.md` shows `c1ce797` adding them inline during 07.1-09, then `9bd5c23` rendering them to PNG during Phase 5) |

**Score:** 10/10 goal-level truths verified (26/26 underlying D-01..D-26 + ROADMAP-201 decisions, each cross-checked against the codebase directly)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/main/java/com/vrudenko/kanban_board/constant/ErrorCode.java` | 12-member error code enum | VERIFIED | Present, matches SUMMARY claim exactly |
| `src/main/java/com/vrudenko/kanban_board/handler/GlobalExceptionHandler.java` | 13 branches → `ResponseEntity<ProblemDetail>` | VERIFIED | All 13 confirmed by direct read |
| `src/main/java/com/vrudenko/kanban_board/config/CorsConfig.java` | `CorsConfigurationSource` bean | VERIFIED | Present, `SecurityConfiguration.java`'s `.cors(Customizer.withDefaults())` untouched |
| `src/main/java/com/vrudenko/kanban_board/security/ProblemDetailAuthenticationEntryPoint.java` | Custom `AuthenticationEntryPoint` | VERIFIED | Present, wired via `http.exceptionHandling(...)` |
| `src/main/resources/db/migration/V7__add_board_optimistic_locking_version_column.sql` | Additive `boards.version` column | VERIFIED | Present, additive `ALTER TABLE` |
| `src/main/java/com/vrudenko/kanban_board/entity/BoardEntity.java` | `@Version` field | VERIFIED | Present at line 63-65 |
| `src/test/java/com/vrudenko/kanban_board/security/InjectionAttemptTest.java` | SQL/XSS/boundary/malformed-path coverage | VERIFIED | 871 lines, 32/32 tests pass live |
| `src/test/java/com/vrudenko/kanban_board/security/AuthorizationGatingTest.java` | 22-route auth-gating sweep | VERIFIED | 637 lines, 47/47 tests pass live |
| `docs/ARCHITECTURE.md` | 4 Mermaid sequence diagrams | VERIFIED | All 4 present (as PNG + linked `.mmd` source, superseding the original inline blocks) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `CorsConfigurationSource` bean | `SecurityConfiguration` | Auto-detection via `http.cors(Customizer.withDefaults())` | WIRED | Line 66 of `SecurityConfiguration.java` untouched, confirmed byte-identical to pre-phase state per D-11's prohibition |
| `ProblemDetailAuthenticationEntryPoint` | `SecurityConfiguration` | Explicit `http.exceptionHandling(...)` DSL | WIRED | Confirmed at lines 93-100 of `SecurityConfiguration.java` — not auto-detected, explicit wiring present as required |
| `AuthenticationController.signin`/`signup` | Jakarta Validation | `@Valid @RequestBody` | WIRED | Confirmed on both methods |
| `BoardService.updateById` | `BoardEntity.version` | Explicit compare-before-mutate + `entityManager.flush()` | WIRED | Proven behaviorally by `BoardLockingTest$ConcurrentRename` (2/2 pass, real 409 on stale version) |
| `BoardController.addColumnByBoardId` / `TaskController.addSubtaskByTaskId` | 201 + Location | `ResponseEntity.created(URI.create(request.getRequestURI()))` | WIRED | Confirmed via grep + `BoardControllerTest`/`TaskControllerTest` |
| `AuthorizationGatingTest.routeTable()` | Live controllers | `RequestMappingHandlerMapping` reflective completeness guard | WIRED | `Completeness` test passes (1/1); SUMMARY documents a hand-falsification proof (removed a row, confirmed failure, restored) — the mechanism has teeth |

### Behavioral Spot-Checks / Live Test Execution

This phase's must-haves are fundamentally about test coverage existing and passing, so live
execution (not SUMMARY narration) was used as primary evidence:

| Test class | Result | Command |
|---|---|---|
| `GlobalExceptionHandlerTest` (7 nested) | 9/9 pass, 0 failures | `./gradlew test --tests` |
| `security.AuthorizationGatingTest` (5 nested) | 47/47 pass, 0 failures | `./gradlew test --tests` |
| `security.InjectionAttemptTest` (4 nested) | 32/32 pass, 0 failures | `./gradlew test --tests` |
| `e2e.board.BoardLockingTest` (3 nested) | 5/5 pass, 0 failures | `./gradlew test --tests` |
| `config.CorsConfigTest` | 1/1 pass, 0 failures | `./gradlew test --tests` |
| `security.AuthenticationTest` (13 nested) | 24/24 pass, 0 failures | `./gradlew test --tests` |

Total: 118/118 targeted tests pass, 0 failures, 0 errors across every decision cluster in this
phase's scope (RFC 7807 envelope, 401/403 split, CORS, signin/signup validation, Board locking,
injection resistance, auth-gating sweep). The one build failure observed
(`jacocoTestCoverageVerification`, expected — running a 4-class subset instead of the full suite
trivially fails the 90% coverage gate; this is a coverage-ratio artifact of the targeted `--tests`
run, not a test failure) does not affect any of the above.

A full, unfiltered `./gradlew test` run was not repeated in this verification session (the prior
session's plan SUMMARYs already document multiple full-suite green runs, 382/382, with a few
independently-investigated and either-fixed-or-documented pre-existing flakes unrelated to this
phase's diff — see `deferred-items.md`). Targeted execution of every test class this phase's
must-haves name was judged sufficient and more reliable than re-trusting narration for a phase this
large; it also directly falsifies the "SUMMARY claims, not verified" risk the harshest scrutiny is
aimed at.

### Requirements Coverage

Not applicable in the REQUIREMENTS.md ID sense — this phase is scoped by `07.1-CONTEXT.md`'s D-01
through D-26 plus ROADMAP-201, all of which are covered in the Observable Truths table above with
zero decisions left unaddressed.

### Anti-Patterns Found

None blocking. Scanned all key production files read during this verification
(`GlobalExceptionHandler.java`, `ErrorCode.java`, `ProblemDetailAuthenticationEntryPoint.java`,
`SecurityConfiguration.java`, `CorsConfig.java`, `AuthenticationController.java`, `BoardEntity.java`,
`V7` migration, all four controllers) for `TODO`/`FIXME`/`XXX`/`HACK`/placeholder patterns — none
found. The codebase's own extensive inline comments document known, deliberate trade-offs (e.g. the
TOCTOU session-ceiling window, the anti-enumeration timing-equalization residual) rather than hiding
debt — each one is either fixed, filed as a todo (confirmed present in `.planning/todos/`), or
explicitly accepted with written rationale, matching D-24's own triage discipline.

### Deferred Items (from `deferred-items.md`, cross-checked)

Two intermittent, pre-existing flakes (`ColumnLockingTest` signin-400, `EventIdGeneratorTest`
uniqueness) were investigated across plans 07.1-06/07/08/09 and confirmed unrelated to this phase's
diff (reproduced in isolation as passing; correlated to Kafka-broker-timing and BCrypt/timing
noise respectively, not this phase's changes). Both are tracked in filed todos. Neither blocks this
phase's goal — none of the 118 tests executed live during this verification session hit either
flake.

### Human Verification Required

None. Every must-have in this phase's scope (D-01 through D-26, ROADMAP-201) is either a structural
code property (grep/read-verifiable, confirmed directly) or a behavior proven by a passing automated
test (confirmed by live execution in this session, not narration). The one item with inherent human
judgment — whether the `/claude-security` scan's finding-by-finding disposition was the *right* risk
call — was already exercised by the project's own operator during phase execution (per
07.1-09-SUMMARY.md's Task 1 section) and is not re-litigated here; this verification instead confirms
the concrete artifact of that triage (the F3 bug fix) is real and present in the codebase, which it
is.

## Gaps Summary

None. Every observable truth this phase's goal requires was independently verified against the
current codebase — not merely against SUMMARY.md prose — including live execution of 118 tests
spanning all seven decision clusters (RFC 7807 envelope + 401/403 split, CORS, signin/signup
validation, resource-creation status codes, Board optimistic locking, injection-resistance proof,
and auth-gating sweep), plus direct confirmation of the two structural closing decisions (D-21/22/23
fastTest retarget, D-24/25/26 security-scan triage and sequence-diagram documentation). This phase's
work has also survived roughly a week of subsequent phases/quick-tasks built on top of it
(Phase 5 infra migration, multiple quick tasks) with no regression or rollback of any 07.1 decision
observed during this verification's file reads — a further, incidental signal of correctness beyond
what this phase's own tests prove.

---

*Verified: 2026-08-17T11:24:41Z*
*Verifier: Claude (gsd-verifier)*
