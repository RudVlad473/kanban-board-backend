---
context: research
phase: milestone-init (v1.3, pre-phase)
task: 8
total_tasks: 10
status: in_progress
last_updated: 2026-08-17T12:13:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

None. This is a clean pause at a natural boundary (research just finished and got
pushed), not a recovery scenario.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Research synthesizer #222 false-refusal | The `gsd-research-synthesizer` agent for this milestone returned its full synthesis as chat text instead of writing `.planning/research/SUMMARY.md` and committing, per a documented known LLM false-refusal (issue #222). Already recovered this session — SUMMARY.md was hand-written from the returned content plus the four individual researchers' findings, committed and pushed (`311e2b4`). | advisory | If this recurs on a future synthesizer dispatch: check `.planning/research/SUMMARY.md`'s content/mtime is actually fresh before trusting it exists — a stale prior-milestone file with the same name will `ls` successfully but be wrong. |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
v1.2 (Infra Migration & Schema Registry) is fully shipped, archived to `.planning/milestones/v1.2-*`, tagged `v1.2`, and pushed. v1.3 ("Nonprod Environment & CI Hardening") has been started: `PROJECT.md`'s Current Milestone section is written, `STATE.md` has been reset for the new milestone, and all 4 parallel research agents (Stack, Features, Architecture, Pitfalls) plus a synthesized `SUMMARY.md` are complete, committed, and pushed. Everything through this point is on `origin/master` at commit `311e2b4`.

We are mid-way through the `/gsd-new-milestone` workflow, specifically about to start **Step 9: Define Requirements**.
</current_state>

<completed_work>

Completed Tasks (this session, in order):
1. Resumed a prior session, found Plan 05-06's checkpoint bookkeeping stale relative to two already-live, already-pushed commits — reconciled `05-06-SUMMARY.md`, closed the Docker Hub todo, fixed `WINDOWS.md` ledger entry #2, corrected `REQUIREMENTS.md`'s stale INFRA-08 wording, dispatched a real `gsd-verifier` agent (not self-declared) which confirmed Phase 5's 5/5 success criteria and 8/8 requirements — Done
2. Marked Phase 5 complete via `phase.complete`; caught its next-integer auto-advance incorrectly targeting "Phase 06" (already done from an earlier session — a known false-positive class this project hit once before) and corrected STATE.md — Done
3. User asked to complete v1.2 and start v1.3. Ran `/gsd-complete-milestone`: pre-close artifact audit surfaced 10 open items (mostly old minor todos, acknowledged and carried forward) plus a genuine tracking bug (SEED-001 stuck at `dormant` despite being fully shipped by Phase 4 — corrected to `implemented`) — Done
4. Readiness check (`init.manager`) found Phase 07.1 was `phase_complete: true` but `verification_status: missing` — the exact "execution completeness != verification completeness" gap this project's own v1.1 retrospective already named once. Dispatched a real `gsd-verifier` agent rather than overriding past it: confirmed 10/10 goal truths, ran 118 tests live across 6 classes, 0 gaps — Done
5. Archived v1.2 (ROADMAP.md/REQUIREMENTS.md to `.planning/milestones/v1.2-*`, phase dirs archived), full `PROJECT.md` evolution review, `RETROSPECTIVE.md` updated with the v1.2 milestone section and cross-milestone trends, `STATE.md` trimmed of ~130 entries of now-archived per-plan history, tagged `v1.2`, pushed — Done
6. Started v1.3: user wants a nonprod/staging environment + CI gate for a future frontend repo's Playwright E2E suite, plus bundled in several smaller CI/deploy hardening todos that were unblocked by v1.2 shipping. Confirmed milestone name, scope bundle, and "research first" via `AskUserQuestion`. Updated `PROJECT.md`'s Current Milestone section and reset `STATE.md` via the SDK handler — Done
7. Dispatched 4 parallel research agents (Stack/Features/Architecture/Pitfalls) scoped to the nonprod-environment unknowns, then a synthesizer. Synthesizer hit a documented #222 false-refusal (returned text instead of writing the file) — recovered by hand-writing `SUMMARY.md` from its returned content. All 5 research files committed and pushed (`311e2b4`) — Done

</completed_work>

<remaining_work>

- **Task 8 (next action):** `new-milestone.md` Step 9 — Define Requirements. Read `.planning/research/SUMMARY.md` and `.planning/research/FEATURES.md`, present feature categories via `AskUserQuestion` (table stakes / differentiators / anti-features), incorporate the already-confirmed bundle of todos (see Decisions below), write `.planning/REQUIREMENTS.md`, commit. Two open decision points research flagged that need explicit user confirmation, not silent assumption:
  1. Colocate nonprod on the existing Netcup VPS (research's recommendation) vs. provision a second VPS from the start
  2. Whether the backend's own production deploy should gate on the (not-yet-existing) frontend repo's E2E results — research flags this as a probable anti-pattern and recommends the frontend gate itself on nonprod reachability instead
- Task 9: `new-milestone.md` Step 10 — spawn `gsd-roadmapper` (model=opus) to turn the requirements into `ROADMAP.md` phases. Research already suggests a 4-phase build order (bootstrap → deploy+measure → Kafka isolation+reset → CI automation), with a 5th phase (cross-repo dispatch) explicitly deferred out of scope.
- Task 10: `new-milestone.md` Step 10.5 — scan pending todos against the approved roadmap phases, tag matches with `resolves_phase` in frontmatter, commit.

</remaining_work>

<decisions_made>

- Milestone v1.3 = "Nonprod Environment & CI Hardening" — user-confirmed.
- Scope bundle confirmed (user chose "Bundle in other ready todos"): nonprod env + CI gate, plus Dependabot `github-actions` ecosystem entry, TruffleHog CI pass, GitHub Actions digest-pinning, gradle cache in `run-tests`, gitleaks-in-worktree fix, `security-scan.yml` stale-comment cleanup, session cookie `Secure` flag (now that real TLS exists), and the README architecture-showcase expansion.
- Research recommends: colocate on existing VPS (not second VPS) as default, with a documented fallback; a second Redpanda broker instance (not topic-prefixing) for Kafka isolation, because the Schema Registry's `RecordNameStrategy` is keyed by class name not topic; long-lived static nonprod (not per-PR ephemeral); backend should NOT gate its own prod promotion on frontend E2E results; cross-repo `repository_dispatch` explicitly deferred as blocked on the frontend repo not existing.
- **None of the research recommendations above have been confirmed by the user yet** — they need to be presented and confirmed/adjusted during Step 9 (Define Requirements), not assumed as locked.

</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/research/SUMMARY.md` — the synthesized findings and suggested 4-phase build order; read this first
2. `.planning/research/FEATURES.md` — the table-stakes/differentiators/anti-features breakdown needed to present categories in Step 9
3. `.planning/research/PITFALLS.md` — the 5 safety-critical findings (deploy-job overwrite risk, no GitHub Environments, `cleanup-old-images` tag-deletion risk, resource contention, DuckDNS wildcard scope) that must shape the requirements, not just the implementation
4. `$HOME/.claude/gsd-core/workflows/new-milestone.md` — Step 9 onward (Define Requirements → Create Roadmap → Link Pending Todos) is the remaining process to follow

## Infrastructure State
- Production: unchanged, live, healthy — nothing this session touched production infra, only planning docs and research.
- Git: `origin/master` at `311e2b4`, clean tree except pre-existing harness noise (`.gsd/dispatch-isolation-sentinel.json`, `.planning/config.json` — not from this session, do not touch).
- `.planning/research/*.md` — all 5 files exist, fresh (2026-08-17), committed and pushed.
- No `.planning/phases/` directory exists yet for v1.3 — Step 10 (roadmap creation) is what creates the first phase directories.

<context>
This session did a lot: closed out two genuine verification gaps in v1.2 (Phase 5's stale checkpoint bookkeeping, Phase 07.1's missing VERIFICATION.md), fully archived and tagged v1.2, then ran the entire front half of `/gsd-new-milestone` for v1.3 including a full 4-agent research sweep. The research came back rich and specific — it's grounded in this project's own actual infra (read directly from `docs/INFRA_RUNBOOK.md`, `deploy.yml`, `docker-compose.prod.yml`), not generic advice. The pause here is a deliberate choice at a clean boundary (research just landed) rather than pushing into Define Requirements, which needs multiple `AskUserQuestion` rounds and a `gsd-roadmapper` dispatch — better done with fresh context than continued in an already-long session.
</context>

<next_action>
Start with: read `.planning/research/SUMMARY.md` and `.planning/research/FEATURES.md`, then proceed to `new-milestone.md` Step 9 (Define Requirements) — present feature categories via `AskUserQuestion`, surface the two open research-flagged decision points explicitly, incorporate the confirmed todo bundle, write `.planning/REQUIREMENTS.md`.
</next_action>
