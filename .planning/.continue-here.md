---
context: default
phase: none
task: null
total_tasks: null
status: paused
last_updated: 2026-09-05T19:21:56.182Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Report a diff, not a location | Twice this session the user could not find committed work — once looking in a worktree, once in the "Uncommitted Changes" view. Both times the state was correct and the vantage point was wrong. | advisory | Lead every report with `N commits on branch X` and the exact `git log main..HEAD` command, not a prose description of the change. |
| Session worktree vs. shell cwd mismatch | This session's header names branch `worktree-agent-a2d9d4573207ef980`, but every command ran in the primary checkout. Work landed correctly but not where the header implied. | advisory | Run `pwd` and `git branch --show-current` at session start and state the answer explicitly if they disagree with the header. |
| `pkill -f <pattern>` matching its own command | `pkill -f net-forensics/sample.sh` inside an SSH command string matched the SSH command itself and killed the session (exit 255). | advisory | Kill by PID from `pgrep -af`, never by a pattern that appears in the running command. |
| Probe whose success and failure look identical | `docker ps -q \| wc -l` prints `0` both when Docker works and when it is unreachable; PowerShell aliases `echo`, so an `echo` canary passes on a shell that can run nothing else. | advisory | Choose canaries that fail loudly: `uname -s`, `docker version --format '{{.Server.Version}}'`. |
| Growing the always-loaded context tier | An addition to `TOOLING_PREFERENCES.md` pushed the frozen tier from 523 to 546 lines; `context-budget.js` rejected it twice. | advisory | To add there, say what comes out. Fold into an existing entry, compress narrative into rule shape, or move to the triggered tier. |

<current_state>
No active phase. `main` is at `4f11c7c`, pushed, and deployed green (all 14 CI/CD jobs succeeded,
including `deploy-to-netcup` and `deploy-to-nonprod`). Working tree is clean apart from
long-standing untracked `.planning/debug/`, `.planning/research/.cache/` and a `.gitkeep`.

Three threads ran this session: a network debug that ended in a support ticket being sent; quick
task `260905-qxi` taken through plan → execute → three-way review → remediation → merge → deploy;
and toolchain repairs (agy, ripgrep).
</current_state>

<completed_work>
- **Netcup connect-timeout investigation** — root cause identified as total inbound SYN loss upstream
  of the VM, lasting ~134s per episode. Confirmed empirically: a blackholed control target
  (`192.0.2.1:443`, RFC 5737) gave up in **134.71s** against the real failure's **134.812s**. Server
  exonerated by measurement (`ListenOverflows=0` over 19 days, eth0 drops 0, conntrack 9/262144,
  iptables ACCEPT 443, Caddy logged no errors in any window). Ticket **sent** to netcup.
- **Quick task 260905-qxi** — `scripts/verify-compose-ports.py` + self-test, wired into
  `.github/workflows/invariant-checks.yml` as a blocking per-PR job. 7 invariants. Merged `--ff-only`
  to `main` at `4f11c7c`, pushed, deployed.
- **Three-way review** — Claude/Gemini/Codex, sequential, one shared brief, one worktree each. Seven
  findings, all independently re-verified by the orchestrator, all fixed, all re-verified after fixing.
  Worktrees torn down; main checkout confirmed byte-identical by sha256.
- **agy migrated to the native Linux build (1.1.27)** — was a Chocolatey Windows `.exe` whose shell
  ran as PowerShell on a `\\wsl.localhost\` UNC path, unable to reach WSL's Docker or any POSIX tool.
  That was the cause of the long-standing review wedge. Token copied from the Windows profile, so no
  re-auth was needed.
- **ripgrep wired in** — `REVIEW_BRIEF.md` § *Search with a tool, not by eye* and a folded
  `TOOLING_PREFERENCES.md` entry (LSP → meaning, `ast-grep` → structure, `rg` → text), landed at
  exactly 523/523 of the frozen budget.
- **Memory written** — `infra-diagrams-go-stale-silently` (project tier).
</completed_work>

<remaining_work>
1. **Infra diagrams** (next, and deliberately before the iptables work):
   - Pin the `.mmd` → `.png` renderer. **There is no committed render command anywhere in the repo**
     and `mmdc` is not installed. Without it, the layout rules are unverifiable and the committed
     PNGs can drift from their sources.
   - Add layout rules to `docs/DIAGRAM_CONVENTIONS.md`: `flowchart TB` always (repo currently mixes
     `TB`/`TD`), one vertical spine with outcomes as leaves, `~~~` invisible links for rank
     alignment, a shared init block.
   - New Scenario (+1): the inbound packet path, with `DOCKER-USER` drawn **EMPTY and dated**.
   - Annotate the Physical/Deployment view with named trust boundaries, including
     `[1] Netcup Cloud Firewall (external — not in this repo)`.
2. **`DOCKER-USER` iptables rules** on the VM + a systemd unit ordered `After=docker.service`,
   idempotent. Verify by publishing a throwaway port and confirming it is **unreachable** from
   off-box — not by confirming 443 still works.
3. Playwright retries on the frontend e2e job (different repo).
4. `scripts/review-worktrees.sh` — the manual worktree dance has now recurred, so per CLAUDE.md it
   earns a script. Teardown must only ever touch `.worktrees/review-*`, never `.claude/worktrees/*`.
5. Uptime check: fires ~5x/day against the 96x its `*/15` cron requests. Cannot detect a 134s outage.
</remaining_work>

<decisions_made>
- Detect published ports by **key presence**, not by parsing entries — covers short and long mapping
  syntax for free; a parser that returns "allowed" on an unrecognised shape is the silent gate the
  task existed to remove.
- **Rejected** the reviewer's proposed self-test fix (import `ALLOWED_PUBLISHERS`): it would make the
  test a self-referential oracle. Fixtures kept literal; generalised I4 cases added instead.
- Draw the packet-path diagram **before** the iptables work, so `DOCKER-USER` is recorded empty. That
  empty box is the finding.
- `docker-compose.yml` (local dev) deliberately excluded from the gate — it must publish 5433/9092/8081.
</decisions_made>

<blockers>
- The upstream-discard conclusion is **by elimination, not observation**. Two correlated probes are
  running to capture direct proof; no episode caught in ~2.3h (546 local samples, 621 VM samples,
  zero failures).
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — structured state, decisions, pending human actions.
2. `docs/DIAGRAM_CONVENTIONS.md` — Kruchten 4+1; mandatory before authoring any diagram.
3. `docs/INFRA_ARCHITECTURE.md` — existing Physical/Deployment + Scenario views and the Maintenance
   Note pattern the diagram work must extend.
4. `.planning/todos/pending/2026-09-05-docker-user-chain-empty-on-the-vm.md` — the iptables follow-up.
5. `scripts/verify-compose-ports.py` docstring — the gate's KNOWN HOLES, including one the fix itself
   introduces (`DELIBERATELY_EXCLUDED` is editable in the same PR).

## Infrastructure State
- `main` = `4f11c7c`, pushed, deployed. Production and nonprod both healthy.
- **Forensics probes running, 12h bounded, started ~16:47Z (expire ~04:47Z):**
  - VM: `/var/tmp/net-forensics/samples.csv` (read-only kernel counters, 15s interval)
  - Local: `~/dev/kanban-net-forensics/probe.csv` (connect prober, 15s interval)
  - **Read these first on resume.** A failed client row against a *flat* `passive_opens` is direct
    proof the SYN never reached the host. Stop either with `pkill -f net-forensics`.
- `/tmp/netcup-ticket.txt` — local source of the sent ticket. `/tmp` is tmpfs and is wiped on reboot.
- `~/dev/kanban-worktree-salvage/` — preserved draft from a reaped stale worktree.
- One worktree left alone: `.claude/worktrees/agent-a2d9d4573207ef980` (clean at `819fa81`; this
  session's own header names that branch, so it may be harness-owned).

<context>
The through-line was that measurement kept inverting confident conclusions. The "127s SYN ladder"
arithmetic was close enough to feel right and wrong enough to matter — a control target settled it
to within 0.1s. A reviewer's most severe finding (I5, interpolation) came from the reviewer with the
*fewest* findings. Two of the seven findings existed only because a docstring generalised where the
code specialised. Prefer running something over reasoning about it; nearly everything here was
settleable in one second.
</context>

<next_action>
Start the infra diagrams task by pinning the renderer: `pnpm dlx @mermaid-js/mermaid-cli` to render
one existing `.mmd` and confirm the output matches its committed `.png`. If it does not, the sources
and images have already drifted and that is the first finding to report, before writing any new
diagram.
</next_action>
