---
phase: 13-introduce-kubernetes
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - infra/vm/k3s/config.yaml
  - infra/vm/k3s/install.sh
  - infra/vm/README.md
  - k8s/flux-system/gotk-components.yaml
  - k8s/flux-system/gotk-sync.yaml
  - k8s/flux-system/kustomization.yaml
  - k8s/flux-system/platform.yaml
  - k8s/flux-system/data.yaml
  - k8s/flux-system/apps-nonprod.yaml
  - k8s/flux-system/cert-manager.yaml
  - k8s/flux-system/edge.yaml
  - k8s/flux-system/apps-prod.yaml
  - k8s/flux-system/monitoring-controllers.yaml
  - k8s/flux-system/monitoring.yaml
  - k8s/flux-system/image-automation.yaml
  - k8s/platform/kustomization.yaml
  - k8s/platform/namespaces.yaml
  - k8s/platform/traefik/helmchartconfig.yaml
  - k8s/data/postgres-bridge/kustomization.yaml
  - k8s/data/postgres-bridge/postgres-bridge.yaml
  - docker-compose.prod.yml
  - scripts/verify-compose-ports.py
  - scripts/verify-compose-ports-selftest.py
  - docs/INFRA_RUNBOOK.md
autonomous: false
requirements: [D-02, D-10, D-11, D-12, D-15, D-16, D-17]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "k3s v1.36.4+k3s1 runs on netcup-prod with secrets encryption on, ServiceLB and metrics-server disabled, and a root-only kubeconfig; Caddy remains the only process serving public 80/443 (D-10, D-12)."
    - "Flux reconciles ./k8s/flux-system from main over an SSH deploy key scoped to this one repo — no PAT, no new CI secret (D-15)."
    - "A pod in kanban-nonprod authenticates to kanban_nonprod through postgres.kanban-data.svc.cluster.local (Compose Postgres via 172.17.0.1), and the same credentials are refused on kanban_prod — Phase 11 D-01 isolation survives the bridge (D-11, assumption A2)."
    - "Flux ImageUpdateAutomation has pushed at least one tag-bump commit to main, and that commit did not start deploy.yml; two ImagePolicies, one per Docker Hub repo, track main independently (D-15, D-16)."
    - "The k3s + Traefik + Flux memory footprint is measured on the box and recorded against the interim budget before any workload moves (A1)."
    - "All later-phase Flux Kustomizations exist but stay suspended, so nothing production-facing reconciles before 13-06/13-07 (D-02, D-17)."
  artifacts:
    - "infra/vm/k3s/config.yaml, infra/vm/k3s/install.sh — pinned, checksum-verified install"
    - "k8s/flux-system/ — Flux controllers, sync, 9 Kustomization CRs, image automation"
    - "k8s/platform/ — namespaces + interim Traefik HelmChartConfig; k8s/data/postgres-bridge/ — Service + EndpointSlice"
    - "docs/INFRA_RUNBOOK.md section `## k3s install and interim bridge — Plan 13-02 (<date>)`"
  key_links:
    - "Service kanban-data/postgres (clusterIP 10.43.54.32, no selector) + EndpointSlice -> 172.17.0.1:5432 -> Compose postgres `ports: 172.17.0.1:5432:5432`"
    - "HelmChartConfig kube-system/traefik `web` NodePort 30080 -> Caddy's nonprod upstream (13-05)"
    - "image-automation.yaml ImageUpdateAutomation update.path ./k8s/overlays -> setter markers in both overlays"
---

<objective>
Install k3s and Flux on the live VPS alongside Compose, and prove the one piece of plumbing the nonprod half depends on: a k3s pod reaching Compose Postgres. Nothing production-facing changes except one scoped Postgres port on the docker0 gateway.

Purpose: removes the two riskiest unknowns (A1 memory, A2 cross-runtime routing) before any workload moves.
Output: running k3s + Flux, the interim bridge, suspended Kustomizations for every later plan, and a measured baseline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-introduce-kubernetes/13-CONTEXT.md
@.planning/phases/13-introduce-kubernetes/13-RESEARCH.md
@.planning/phases/13-introduce-kubernetes/13-01-PLAN.md
@infra/vm/docker-user-firewall.sh
@docs/SESSION_LESSONS.md
</context>

<artifacts_this_phase_produces>
| Kind | Name | Where |
|---|---|---|
| VM config | k3s server config + pinned install wrapper | infra/vm/k3s/ |
| k8s resources | Namespaces kanban-prod/kanban-nonprod/kanban-data/monitoring; HelmChartConfig kube-system/traefik (interim NodePort 30080) | k8s/platform/ |
| k8s resources | Service + EndpointSlice kanban-data/postgres (bridge) | k8s/data/postgres-bridge/ |
| Flux | gotk-components/gotk-sync; Kustomizations platform, data, apps-nonprod*, cert-manager*, edge*, apps-prod*, monitoring-controllers*, monitoring* (*suspended); ImageRepository ×2, ImagePolicy ×2, ImageUpdateAutomation `kanban-images` | k8s/flux-system/ |
| Secrets (VM only) | flux-system/flux-system (deploy key), kanban-nonprod/app-env | cluster |
| GitHub | Deploy key `flux-netcup-prod` (write, this repo only) | repo settings |
| Compose change | postgres publishes `172.17.0.1:5432` (interim, removed in 13-10) | docker-compose.prod.yml |
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Authorize installing k3s on netcup-prod and adding a write deploy key to the repo</name>
  <read_first>
    - 13-RESEARCH.md "Q8 — Memory Budget" and Assumptions A1/A2.
    - docs/INFRA_RUNBOOK.md "Firewall — three layers" (DOCKER-USER section).
  </read_first>
  <decision>Install k3s + Flux on the live production VPS now, recreate the Compose postgres container once with a docker0-only published port, and add a write-scoped deploy key to RudVlad473/kanban-board-backend.</decision>
  <context>
Before presenting, gather over `ssh netcup-prod` (read-only) and include in the message:
- `free -m`: MemAvailable.
- `docker ps --format '{{.Names}}'` count.
- `iptables -S DOCKER-USER`.
- `docker inspect kanban-board-backend-postgres-1 --format '{{json .Config.Cmd}}'` compared against docker-compose.prod.yml's postgres `command:`. Recreating the container applies any compose change not yet live, e.g. 260911-gkz's deferred `shared_preload_libraries=pg_stat_statements`; say so if it differs.
- `gh api repos/RudVlad473/kanban-board-backend/branches/main/protection`: a 404 means no bypass is needed for the Flux key.

What changes:
- The k3s service with its own containerd, flannel and iptables chains.
- No change to 80/443: ServiceLB is disabled, so Traefik binds no host port.
- One ~10 s prod DB blip when deploy-to-netcup recreates postgres. The same class of disruption as the app recreate every deploy already causes.
- Projected +0.7–1.0 GiB RSS; research budget A1 is unmeasured until Task 3.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed now</name>
      <pros>Unblocks the nonprod half; A1/A2 get measured on the real box.</pros>
      <cons>First change to the live host's container runtime set; one prod Postgres restart.</cons>
    </option>
    <option id="proceed-quiet-hour">
      <name>Proceed at a named low-traffic time</name>
      <pros>Moves the Postgres blip into a quiet window.</pros>
      <cons>Delays the phase until then.</cons>
    </option>
    <option id="postpone">
      <name>Postpone</name>
      <pros>Nothing changes on the host.</pros>
      <cons>The phase is blocked; 13-03/13-04 repo work can still proceed.</cons>
    </option>
  </options>
  <resume-signal>Reply proceed, proceed-quiet-hour &lt;time&gt;, or postpone.</resume-signal>
  <acceptance_criteria>
    - The operator's reply is recorded verbatim in the SUMMARY, together with the pre-flight readings above.
    - If branch protection exists, execution stops and the bypass for the Flux key is raised as a new decision.
  </acceptance_criteria>
</task>

<task type="tracer">
  <name>Task 2: Tracer — k3s + Flux + bridge, end to end: a kanban-nonprod pod authenticates to Compose Postgres through postgres.kanban-data.svc</name>
  <files>infra/vm/k3s/config.yaml, infra/vm/k3s/install.sh, k8s/flux-system/gotk-components.yaml, k8s/flux-system/gotk-sync.yaml, k8s/flux-system/kustomization.yaml, k8s/flux-system/platform.yaml, k8s/flux-system/data.yaml, k8s/platform/kustomization.yaml, k8s/platform/namespaces.yaml, k8s/platform/traefik/helmchartconfig.yaml, k8s/data/postgres-bridge/kustomization.yaml, k8s/data/postgres-bridge/postgres-bridge.yaml, docker-compose.prod.yml, scripts/verify-compose-ports.py, scripts/verify-compose-ports-selftest.py</files>
  <precondition>Task 1 answered proceed (or its named time has arrived); `ssh netcup-prod true` succeeds.</precondition>
  <read_first>
    - 13-RESEARCH.md "Installation", "Pattern 1", "Flux bootstrap without a GitHub PAT".
    - scripts/verify-compose-ports.py (+ selftest) — invariant numbering and the exception style to extend.
    - docker-compose.prod.yml `postgres:` block; infra/vm/docker-user-firewall.sh (its `check` subcommand).
  </read_first>
  <action>
**Repo (commit before touching the VM):**

- `infra/vm/k3s/config.yaml` (D-10, D-12): `secrets-encryption: true`, `write-kubeconfig-mode: "0600"`, and `disable: [servicelb, metrics-server]`. Add a dated comment: servicelb is off only for the interim; 13-06 re-enables it once Caddy stops.
- `infra/vm/k3s/install.sh`:
  - Copy the config to `/etc/rancher/k3s/config.yaml`.
  - Fetch `install.sh` from the k3s repo at tag `v1.36.4+k3s1` (URL-encode `+`). Verify it against a committed sha256, then run it with `INSTALL_K3S_VERSION=v1.36.4+k3s1`.
  - Idempotent; refuses to run without root.
- `k8s/flux-system/gotk-components.yaml`: output of a pinned `flux install --export --components-extra=image-reflector-controller,image-automation-controller`. Use the latest Flux 2.x release when you write it; download the CLI with a checksum check and record its version in a header comment.
- `k8s/flux-system/gotk-sync.yaml`:
  - GitRepository `flux-system`: url `ssh://git@github.com/RudVlad473/kanban-board-backend`, branch main, secretRef `flux-system`, interval 1m.
  - Kustomization `flux-system`: path `./k8s/flux-system`, prune true, interval 10m.
- `platform.yaml` / `data.yaml`: Flux Kustomizations with paths `./k8s/platform` and `./k8s/data/postgres-bridge`, prune true, `wait: true`.
- `kustomization.yaml` lists these files.
- `k8s/platform/namespaces.yaml`: the four phase namespaces.
- `k8s/platform/traefik/helmchartconfig.yaml`: HelmChartConfig `traefik` in kube-system. Values:
  - service type NodePort;
  - `web` nodePort 30080;
  - websecure not exposed;
  - access logs enabled (13-08 needs client IPs);
  - resources labelled `PROVISIONAL (restart-ladder in 13-09)`.

  Confirm the value keys against the packaged chart version with `k3s kubectl get helmchart traefik -n kube-system -o yaml`.
- `k8s/data/postgres-bridge/postgres-bridge.yaml`:
  - Service `postgres` in kanban-data: no selector, `clusterIP: 10.43.54.32`, port 5432 `postgres`.
  - EndpointSlice `postgres-bridge` (discovery.k8s.io/v1), label `kubernetes.io/service-name: postgres`, addressType IPv4, endpoint `172.17.0.1`, port 5432 `postgres`. EndpointSlice, not the deprecated v1 Endpoints: at the 13-06 path switch the controller-managed slices do not collide with this one.
- `docker-compose.prod.yml` postgres: `ports: ["172.17.0.1:5432:5432"]`, with a dated comment stating it is interim (13-02) and deleted with the file in 13-10.
- `scripts/verify-compose-ports.py`: add a narrow exception allowing exactly service `postgres` to publish `172.17.0.1:5432:5432`. Any other host IP (empty, `0.0.0.0`, eth0), port or service still fails. Selftest: that binding passes; postgres on `0.0.0.0:5432` fails; another service on `172.17.0.1` fails.
- Run `bash scripts/verify-k8s-manifests.sh` and `python3 scripts/verify-k8s-invariants.py`.
- Push with `git pull --rebase` first. The compose change triggers deploy-to-netcup, which recreates postgres; wait for it green.

**VM (over `ssh netcup-prod`, root):**
1. Run install.sh from a copy of the committed files.
2. Confirm:
   - `k3s kubectl get nodes` Ready;
   - `ss -ltnp` shows no k3s/containerd listener on :80/:443;
   - `iptables-legacy -S 2>/dev/null` holds no `KUBE-` chains (both runtimes on the nft backend);
   - `/usr/local/sbin/docker-user-firewall.sh check` passes, i.e. the DOCKER-USER chain is unchanged.
3. Deploy key:
   - `ssh-keygen -t ed25519 -N ''` into a 0700 temp dir on the VM.
   - Copy only the `.pub` back and run `gh repo deploy-key add <pub> --repo RudVlad473/kanban-board-backend --title flux-netcup-prod --allow-write`.
   - Build known_hosts from `ssh-keyscan github.com`, checked against `gh api meta --jq .ssh_keys`.
   - `k3s kubectl create secret generic flux-system -n flux-system --from-file=identity --from-file=identity.pub --from-file=known_hosts`, then `shred -u` the private key file.
4. `k3s kubectl apply --server-side -f gotk-components.yaml`, then `gotk-sync.yaml`. Wait for Kustomizations flux-system, platform and data to be Ready.
5. Create `kanban-nonprod/app-env` from `/opt/deploy/kanban-board-nonprod/.env.nonprod`, filtered to the keys `DB_NAME DB_USER DB_PASS SPRING_PROFILES_ACTIVE APP_CORS_ALLOWED_ORIGINS APP_RESET_TOKEN`. Use `--from-env-file` on a 0600 temp file that you then shred. Never `--from-literal` (D-10).
6. A2 proof: a throwaway Pod in kanban-nonprod (image `postgres:16`, envFrom `app-env`, restartPolicy Never). It runs `pg_isready -h postgres.kanban-data.svc.cluster.local`, then `psql` as `$DB_USER`: `select current_database()` against `$DB_NAME` must succeed; the same against `kanban_prod` must be refused. Capture the logs, then delete the pod.
7. **If A2 fails, stop.** Report which chain dropped the traffic (`iptables -L -v -n` counters, `conntrack -L`). Present these fixes as a decision:
   - a scoped accept for cni0 → docker0 tcp/5432 in the committed firewall script;
   - an EndpointSlice pointing at the postgres container's kanban-db IP.

   Never use hostNetwork.
  </action>
  <verify>
    <automated>
set -e
cd "$(git rev-parse --show-toplevel)"
python3 scripts/verify-compose-ports-selftest.py && python3 scripts/verify-compose-ports.py
bash scripts/verify-k8s-manifests.sh && python3 scripts/verify-k8s-invariants.py
ssh netcup-prod 'set -e
k3s --version | grep -q "v1.36.4+k3s1"
grep -q "secrets-encryption: true" /etc/rancher/k3s/config.yaml
k3s secrets-encrypt status | grep -qi "Encryption Status: Enabled"
test "$(stat -c %a /etc/rancher/k3s/k3s.yaml)" = 600
k3s kubectl get nodes --no-headers | grep -qw Ready
! ss -ltnpH "( sport = :80 or sport = :443 )" | grep -vq docker-proxy
test -z "$(k3s kubectl get svc -A --no-headers | awk "\$3!=\"ClusterIP\" && !(\$1==\"kube-system\" && \$2==\"traefik\")")"
for k in flux-system platform data; do k3s kubectl get kustomization $k -n flux-system -o jsonpath="{.status.conditions[?(@.type==\"Ready\")].status}" | grep -qx True; done
k3s kubectl get svc postgres -n kanban-data -o jsonpath="{.spec.clusterIP}" | grep -qx 10.43.54.32
cat <<EOF | k3s kubectl apply -f - >/dev/null
apiVersion: v1
kind: Pod
metadata: {name: a2-probe, namespace: kanban-nonprod}
spec:
  restartPolicy: Never
  automountServiceAccountToken: false
  containers:
  - name: probe
    image: postgres:16
    envFrom: [{secretRef: {name: app-env}}]
    command: ["sh","-c","export PGPASSWORD=\"\$DB_PASS\"; psql -h postgres.kanban-data.svc.cluster.local -U \"\$DB_USER\" -d \"\$DB_NAME\" -tAc \"select current_database()\" && ! psql -h postgres.kanban-data.svc.cluster.local -U \"\$DB_USER\" -d kanban_prod -tAc \"select 1\" && echo A2_OK"]
EOF
k3s kubectl wait -n kanban-nonprod pod/a2-probe --for=jsonpath="{.status.phase}"=Succeeded --timeout=120s
k3s kubectl logs -n kanban-nonprod a2-probe | grep -q A2_OK
k3s kubectl delete pod -n kanban-nonprod a2-probe --wait=false
for u in https://kanban-board-rud-vlad-473.duckdns.org/api/actuator/health https://kanban-board-rud-vlad-473-nonprod.duckdns.org/api/actuator/health; do curl -fsS "$u" | grep -q "\"status\":\"UP\""; done
echo TASK2_REMOTE_OK'
echo "TASK 2 PASS"
    </automated>
    <fails_when>Non-zero exit, or `TASK2_REMOTE_OK` absent. Specifically:
- k3s is not v1.36.4+k3s1, or secrets encryption is not Enabled;
- the kubeconfig mode is not 600, or the node is not Ready;
- something other than docker-proxy (Caddy) listens on :80/:443;
- a non-ClusterIP Service exists other than kube-system/traefik;
- a Flux Kustomization is not Ready, or the bridge ClusterIP is not 10.43.54.32;
- the A2 probe does not print `A2_OK`, i.e. the nonprod role reached kanban_prod or could not reach kanban_nonprod;
- either public health endpoint is not UP;
- the compose-ports or k8s gates fail locally.</fails_when>
  </verify>
  <acceptance_criteria>
    - The A2 probe log shows `kanban_nonprod`, then a refusal on `kanban_prod` (permission denied for database), then `A2_OK`.
    - `/usr/local/sbin/docker-user-firewall.sh check` exits 0 after the install.
    - `iptables-legacy -S` holds no `KUBE-` chain, so all k3s rules are on the same nft backend as Docker's.
    - GitHub lists exactly one deploy key titled `flux-netcup-prod` with write access; no private-key file remains on disk (`find / -name identity -path '*flux*' 2>/dev/null` is empty).
    - The deploy-to-netcup run for the compose commit is green; `docker inspect kanban-board-backend-postgres-1` shows RestartCount 0 after its recreate.
  </acceptance_criteria>
  <done>k3s and Flux run beside Compose; the cross-runtime bridge is proven with isolation intact, and production's edge is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Suspended Kustomizations for every later plan, image automation for both repos, measured baseline, runbook</name>
  <files>k8s/flux-system/apps-nonprod.yaml, k8s/flux-system/cert-manager.yaml, k8s/flux-system/edge.yaml, k8s/flux-system/apps-prod.yaml, k8s/flux-system/monitoring-controllers.yaml, k8s/flux-system/monitoring.yaml, k8s/flux-system/image-automation.yaml, k8s/flux-system/kustomization.yaml, infra/vm/README.md, docs/INFRA_RUNBOOK.md</files>
  <read_first>
    - 13-01-PLAN.md `<phase_contracts>` — Kustomization names, paths and suspension points.
    - 13-RESEARCH.md "ImagePolicy for D-15's sortable tag scheme".
    - docs/INFRA_RUNBOOK.md "Observability stack resource measurement — Plan 12-05": section shape and the restart-ladder method.
    - docs/plans/backend-modernization/07-kubernetes-stretch.md lines 16-17: the explanation depth bar.
  </read_first>
  <action>
**Flux Kustomization CRs:** every one below is prune true with interval 10m, listed in `k8s/flux-system/kustomization.yaml`, and `suspend: true` with a comment naming the plan that unsuspends it (D-02, D-17).

| CR | path | dependsOn | healthChecks | unsuspended by |
|---|---|---|---|---|
| apps-nonprod | ./k8s/overlays/nonprod | platform, data | Deployment app, StatefulSet redpanda (`wait: true`) | 13-05 |
| cert-manager | ./k8s/platform/cert-manager | platform | — | 13-06 |
| edge | ./k8s/platform/edge | cert-manager | — | 13-06 |
| apps-prod | ./k8s/overlays/prod | data, edge | — | 13-06 |
| monitoring-controllers | ./k8s/monitoring/controllers | platform | — | 13-07 |
| monitoring | ./k8s/monitoring/configs | monitoring-controllers | — | 13-07 |

**image-automation.yaml (D-15, D-16):**
- ImageRepository `kanban-board-backend` and `kanban-board-backend-nonprod`: `docker.io/rudenkovladimir/<repo>`, interval 5m, no credentials (public repos).
- ImagePolicy `kanban-board-backend-prod` and `kanban-board-backend-nonprod`: filterTags pattern `^main-(?P<num>[0-9]+)-[a-f0-9]{7}$`, extract `$num`, `policy.numerical.order: asc`.
- ImageUpdateAutomation `kanban-images`:
  - sourceRef GitRepository flux-system; checkout and push branch main;
  - author `fluxcdbot` / `fluxcdbot@users.noreply.github.com`;
  - messageTemplate beginning `chore(flux): bump images`;
  - update path `./k8s/overlays`, strategy Setters.

  Bumping the prod overlay marker before 13-06 is harmless: apps-prod is suspended.

Push with `git pull --rebase` first. Wait until Flux pushes its first bump commit, then `git pull --rebase` again.

**Measured baseline (A1), over `ssh netcup-prod`:**
- `free -m` MemAvailable versus the Task 1 reading.
- `systemctl show k3s -p MemoryCurrent -p MemoryPeak`.
- Per-pod `memory.peak` from the pod cgroups (`/sys/fs/cgroup/kubepods*`) for traefik, coredns, local-path-provisioner and each Flux controller.
- `dmesg` OOM lines since install: none expected.

Gate: if MemAvailable after install is under 2048 MiB, or any OOM kill appeared, stop and report before 13-05.

**docs/INFRA_RUNBOOK.md** — new section `## k3s install and interim bridge — Plan 13-02 (<date>)`, subsections:
1. What was installed and pinned (versions, checksums, config).
2. Interim topology, with the ServiceLB-disabled rationale.
3. A2 proof output.
4. Measured baseline table (component | RSS/peak MiB | source command).
5. Flux operations (suspend/resume a Kustomization via `spec.suspend`, force reconcile via the `reconcile.fluxcd.io/requestedAt` annotation, rebase before push).
6. k3s operations primer at the Epic 7 depth bar: `kubectl get pods`, `logs`, `describe`; Service vs Deployment vs StatefulSet vs ConfigMap/Secret; what Recreate does on a rollout.
7. Rollback note: `/usr/local/bin/k3s-uninstall.sh`, which also removes its iptables chains.

`infra/vm/README.md`: list the k3s files next to the firewall files.
  </action>
  <verify>
    <automated>
set -e
cd "$(git rev-parse --show-toplevel)"
bash scripts/verify-k8s-manifests.sh && python3 scripts/verify-k8s-invariants.py
python3 - <<'PY'
import sys, yaml
fails = []
want = {'apps-nonprod': './k8s/overlays/nonprod', 'cert-manager': './k8s/platform/cert-manager', 'edge': './k8s/platform/edge',
        'apps-prod': './k8s/overlays/prod', 'monitoring-controllers': './k8s/monitoring/controllers', 'monitoring': './k8s/monitoring/configs'}
listed = yaml.safe_load(open('k8s/flux-system/kustomization.yaml'))['resources']
for name, path in want.items():
    f = f'k8s/flux-system/{name}.yaml'
    if f'{name}.yaml' not in listed: fails.append(f"{f} not listed")
    d = yaml.safe_load(open(f))
    if d['spec'].get('path') != path: fails.append(f"{name} path {d['spec'].get('path')!r}")
    if d['spec'].get('suspend') is not True: fails.append(f"{name} is not suspended")
docs = list(yaml.safe_load_all(open('k8s/flux-system/image-automation.yaml')))
pols = {d['metadata']['name']: d for d in docs if d and d['kind'] == 'ImagePolicy'}
for p in ('kanban-board-backend-prod', 'kanban-board-backend-nonprod'):
    s = pols.get(p, {}).get('spec', {})
    if s.get('filterTags', {}).get('pattern') != '^main-(?P<num>[0-9]+)-[a-f0-9]{7}$' or s.get('policy', {}).get('numerical', {}).get('order') != 'asc':
        fails.append(f"ImagePolicy {p} wrong")
rb = open('docs/INFRA_RUNBOOK.md').read()
if '## k3s install and interim bridge — Plan 13-02' not in rb: fails.append("runbook section missing")
if fails:
    print("\n".join("FAIL: " + f for f in fails)); sys.exit(1)
PY
BUMP=$(git log --author=fluxcdbot --format=%H -1)
test -n "$BUMP" || { echo "FAIL: no Flux bump commit on main"; exit 1; }
test -z "$(gh run list --workflow deploy.yml --commit "$BUMP" --json databaseId --jq '.[].databaseId')" || { echo "FAIL: deploy.yml ran for Flux commit"; exit 1; }
ssh netcup-prod 'k3s kubectl get imagepolicy -n flux-system -o jsonpath="{range .items[*]}{.metadata.name}={.status.latestRef.tag}{\"\n\"}{end}"' | tee /dev/stderr | grep -cE '=main-[0-9]+-[0-9a-f]{7}$' | grep -qx 2
echo "TASK 3 PASS"
    </automated>
    <fails_when>Non-zero exit or any `FAIL:` line. Specifically:
- a later-plan Kustomization is missing, unlisted, on the wrong path, or not suspended;
- an ImagePolicy's regex or ordering differs from the stated values;
- the runbook section is absent;
- no fluxcdbot commit exists on main, or deploy.yml ran for it;
- fewer than two ImagePolicies report a `main-N-sha7` latest tag.</fails_when>
  </verify>
  <acceptance_criteria>
    - `k3s kubectl get kustomizations -n flux-system` shows the six later-phase CRs as Suspended and flux-system/platform/data as Ready.
    - A `chore(flux): bump images` commit by fluxcdbot exists on main, with no deploy.yml run for it.
    - The runbook section records MemAvailable before and after, and a per-component table with the command each figure came from, marks every figure MEASURED or reported-by-cgroup, and states the go/no-go result.
    - No 32-or-more-character hex string appears in the new runbook section; no key material is recorded.
  </acceptance_criteria>
  <done>Flux owns the cluster from git with every later stage staged but inert, and the real platform footprint replaces research's assumed figures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub ↔ Flux | Flux holds a write key to this repo and applies whatever main says |
| Internet (eth0) ↔ k3s NodePorts | Forwarded traffic, guarded by DOCKER-USER (eth0 → 80/443 only) during the interim |
| k3s pods ↔ Compose Postgres | New cross-runtime path through 172.17.0.1:5432 |
| VM local users ↔ k3s API | CI SSH users `deploy`/`deploy-nonprod` live on this host |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-06 | Elevation of Privilege | /etc/rancher/k3s/k3s.yaml admin kubeconfig | high | mitigate | `write-kubeconfig-mode: "0600"` (root only); verified by `stat` in Task 2. CI users cannot read prod Secrets. |
| T-13-07 | Spoofing / Tampering | Flux write deploy key | high | mitigate | Deploy key scoped to this one repo; private half generated on the VM, stored only as a Secret under `--secrets-encryption`, file shredded; github.com host keys verified against `gh api meta`. |
| T-13-08 | Information Disclosure | Secret creation on the VM | medium | mitigate | `--from-env-file` over a filtered 0600 temp file, never `--from-literal` (argv/history); only the six keys the nonprod app needs. |
| T-13-09 | Elevation of Privilege | Compose Postgres published port | medium | mitigate | Bound to 172.17.0.1 only; eth0 traffic dropped by DOCKER-USER; verify-compose-ports exception limited to that exact binding; roles still least-privilege (A2 proof shows the nonprod role refused on kanban_prod). |
| T-13-10 | Denial of Service | k3s ServiceLB hijacking 80/443 from Caddy | high | mitigate | ServiceLB disabled at install; Task 2 asserts only docker-proxy listens on 80/443 and prod health is UP. |
| T-13-11 | Denial of Service | Memory pressure on a no-swap host | medium | mitigate | Task 3 measured gate (≥2048 MiB available, no OOM); k8s pods carry higher `oom_score_adj` than Compose containers. |
| T-13-SC | Tampering | k3s install script, Flux manifests | medium | mitigate | install.sh fetched at a release tag and sha256-verified; k3s binary pinned via INSTALL_K3S_VERSION; Flux CLI checksum-verified, output committed and reviewed. |
</threat_model>

<verification>
1. k3s Ready, secrets encryption enabled, kubeconfig 0600, no k3s listener on 80/443.
2. A2 proof passes with isolation intact; both public health endpoints UP.
3. Flux Ready on flux-system/platform/data; six later Kustomizations suspended; one Flux bump commit, no loop.
4. The measured baseline is recorded and the go/no-go gate passed.
</verification>

<success_criteria>
- The cluster, GitOps loop and interim bridge exist and are proven, with production unaffected beyond one Postgres recreate.
- A1 and A2 are resolved by measurement, not assumption.
</success_criteria>

<output>
Create `.planning/phases/13-introduce-kubernetes/13-02-SUMMARY.md` when done.
</output>
