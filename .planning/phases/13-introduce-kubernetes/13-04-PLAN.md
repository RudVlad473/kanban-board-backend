---
phase: 13-introduce-kubernetes
plan: 04
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - k8s/overlays/prod/kustomization.yaml
  - k8s/overlays/prod/patches.yaml
  - k8s/overlays/prod/ingressroute.yaml
  - k8s/overlays/prod/certificate.yaml
  - k8s/data/postgres/kustomization.yaml
  - k8s/data/postgres/postgres.yaml
  - k8s/data/postgres/networkpolicy.yaml
  - k8s/data/postgres/init/01-create-databases-and-roles.sh
  - k8s/data/postgres/init/02-create-monitoring-role.sh
  - k8s/platform/cert-manager/kustomization.yaml
  - k8s/platform/cert-manager/cert-manager.yaml
  - k8s/platform/edge/kustomization.yaml
  - k8s/platform/edge/clusterissuers.yaml
  - k8s/platform/edge/certificate-monitoring.yaml
  - scripts/cutover/row-counts.sql
  - scripts/verify-k8s-invariants.py
  - scripts/verify-k8s-invariants-selftest.py
autonomous: true
requirements: [D-05, D-06, D-08, D-09, D-11, D-13]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The prod overlay renders the same base with prod flags (Redpanda --memory 2G / 2200Mi), prod resources carried from Compose measurements, a websecure IngressRoute with TLS, HTTP→HTTPS redirect, and rate limiting on /api/signin and /api/signup (auth) and all other paths (general) on the prod hostname only (D-13)."
    - "Postgres 16 is a StatefulSet in kanban-data on local-path with the Compose-measured -c flags and memory cap; its first boot creates both least-privilege roles/databases and the monitoring role before any restore, from init scripts byte-identical to the Compose one (D-05)."
    - "A NetworkPolicy admits TCP 5432 to Postgres only from kanban-prod, kanban-nonprod and the labelled exporter pod in monitoring (D-11)."
    - "cert-manager and two ClusterIssuers (staging, production) exist, with every Certificate starting on staging; the monitoring hostname has its own Certificate so all three hostnames can validate in the 13-06 window (D-13)."
    - "A committed SQL file produces diff-able row counts for users, boards, columns, tasks, subtasks, activity_log (D-08.2)."
  artifacts:
    - "k8s/overlays/prod/ — app, redpanda, IngressRoutes, Middlewares, Certificate"
    - "k8s/data/postgres/ — StatefulSet, Services, NetworkPolicy, init scripts"
    - "k8s/platform/cert-manager/, k8s/platform/edge/ — HelmRelease, ClusterIssuers, monitoring Certificate"
    - "scripts/cutover/row-counts.sql"
  key_links:
    - "Service kanban-data/postgres clusterIP 10.43.54.32 — identical in bridge (13-02) and here, so the 13-06 path switch updates in place"
    - "init/01-create-databases-and-roles.sh == docker/postgres-init/01-create-databases-and-roles.sh (invariant I9 until 13-10)"
    - "Certificate issuerRef letsencrypt-staging -> flipped to letsencrypt-production in 13-06 W3"
---

<objective>
Author everything production needs on k3s — prod overlay, final Postgres, cert-manager and issuers — as validated manifests that stay inert until the 13-06 window activates them.

Purpose: the maintenance window then only flips switches and moves data; nothing is authored under time pressure.
Output: k8s/overlays/prod, k8s/data/postgres, k8s/platform/{cert-manager,edge}, and scripts/cutover/row-counts.sql.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-introduce-kubernetes/13-CONTEXT.md
@.planning/phases/13-introduce-kubernetes/13-RESEARCH.md
@.planning/phases/13-introduce-kubernetes/13-01-PLAN.md
@docker-compose.prod.yml
@Caddyfile
@docker/postgres-init/01-create-databases-and-roles.sh
</context>

<artifacts_this_phase_produces>
| Kind | Name | Where |
|---|---|---|
| k8s resources | Deployment/Service app, StatefulSet/Service redpanda, IngressRoutes app-https/app-http, Middlewares auth-rate-limit/general-rate-limit/redirect-https, Certificate app (kanban-prod) | k8s/overlays/prod/ |
| k8s resources | StatefulSet postgres, Services postgres (10.43.54.32) + postgres-headless, ConfigMap postgres-init, NetworkPolicy postgres-ingress (kanban-data) | k8s/data/postgres/ |
| HelmRelease | cert-manager (+ HelmRepository jetstack) | k8s/platform/cert-manager/ |
| k8s resources | ClusterIssuers letsencrypt-staging/-production, Certificate monitoring/grafana | k8s/platform/edge/ |
| SQL | row-counts.sql | scripts/cutover/ |
| Invariant | I9 init-script byte identity | scripts/verify-k8s-invariants.py |
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — prod overlay rendered end to end: app + Redpanda + TLS route + rate-limit Middlewares, validated and invariant-clean</name>
  <reversibility rating="costly">D-13: the rate-limit scope and the Caddy-to-Traefik translation become the only edge protection once Caddy is deleted.</reversibility>
  <files>k8s/overlays/prod/kustomization.yaml, k8s/overlays/prod/patches.yaml, k8s/overlays/prod/ingressroute.yaml, k8s/overlays/prod/certificate.yaml</files>
  <read_first>
    - k8s/overlays/nonprod/* (from 13-01) — the structure to mirror.
    - docker-compose.prod.yml `app:` and `redpanda:` blocks — `mem_limit` 3g / 2200m, `--memory 2G`, the dated measurement comments.
    - Caddyfile `{$APP_DOMAIN}` block — the `auth` and `general` zones (events, window, match, ipv6_prefix).
    - 13-RESEARCH.md Pattern 2; docs/INFRA_RUNBOOK.md "Redpanda resource caps, measured" (05-04) and "Self-hosted Postgres resource measurement" (11-03) for `requests` figures.
  </read_first>
  <action>
**kustomization.yaml:**
- `namespace: kanban-prod`, resources `../../base/app` and `../../base/redpanda`.
- Images: `rudenkovladimir/kanban-board-backend`, `newTag` set to the tag in the nonprod overlay today, ending with the marker `# {"$imagepolicy": "flux-system:kanban-board-backend-prod:tag"}` (D-16).
- Once 13-02's image automation is live, Flux may bump this line concurrently. On a rebase conflict, keep the higher run number.

**patches.yaml:**
- app: limits 3Gi; requests = the measured prod steady RSS rounded up to a 64Mi multiple.
- redpanda: limits 2200Mi; requests measured.
- A JSON6902 patch replaces the `--memory` argument value with `2G`.
- Every `resources` block carries a `MEASURED (carried from <source>, <date>) — re-confirmed in 13-09` comment.

**ingressroute.yaml:**
- Middleware `auth-rate-limit`: `rateLimit` average 20, period 5m, burst 20, `sourceCriterion.ipStrategy.ipv6Subnet: 56`.
- Middleware `general-rate-limit`: average 120, period 1m, burst 120, same source criterion.
- Middleware `redirect-https`: redirectScheme https, permanent.
- Both rate-limit Middlewares carry the comment `PROVISIONAL translation of Caddy zone <name> (events/window); re-derived from token-bucket semantics in 13-08`.
- IngressRoute `app-https`, entryPoint websecure, `tls.secretName: app-tls`:
  - route ``Host(`kanban-board-rud-vlad-473.duckdns.org`) && (PathPrefix(`/api/signin`) || PathPrefix(`/api/signup`))`` → auth-rate-limit → `app:8080`;
  - route ``Host(`kanban-board-rud-vlad-473.duckdns.org`)`` → general-rate-limit → `app:8080`.
- IngressRoute `app-http`, entryPoint web, ``Host(…)`` → redirect-https. It must not shadow cert-manager's HTTP-01 solver, whose Ingress rule is longer and so wins on priority.

The scope matches Caddy exactly: prod hostname only; nonprod stays unlimited (D-13).

**certificate.yaml:** Certificate `app`, secretName `app-tls`, dnsNames [prod hostname], issuerRef ClusterIssuer `letsencrypt-staging` (D-13 staging first).
  </action>
  <verify>
    <automated>
set -e
cd "$(git rev-parse --show-toplevel)"
bash scripts/verify-k8s-manifests.sh && python3 scripts/verify-k8s-invariants.py
python3 - <<'PY'
import subprocess, sys, yaml
kubectl = subprocess.check_output(['bash', 'scripts/verify-k8s-manifests.sh', '--tool', 'kubectl'], text=True).strip()
docs = [d for d in yaml.safe_load_all(subprocess.check_output([kubectl, 'kustomize', 'k8s/overlays/prod'], text=True)) if d]
by = {(d['kind'], d['metadata']['name']): d for d in docs}
fails = []
if any(d['metadata'].get('namespace') != 'kanban-prod' for d in docs): fails.append("object outside kanban-prod")
c = by[('StatefulSet', 'redpanda')]['spec']['template']['spec']['containers'][0]
a = c['args']
if a[a.index('--memory') + 1] != '2G' or c['resources']['limits']['memory'] != '2200Mi': fails.append("prod redpanda memory flags/limit wrong")
app = by[('Deployment', 'app')]['spec']['template']['spec']
if not app['containers'][0]['image'].startswith('rudenkovladimir/kanban-board-backend:main-'): fails.append("prod app image wrong")
if app['containers'][0]['resources']['limits']['memory'] != '3Gi': fails.append("prod app limit != 3Gi")
mw = {n: d['spec'].get('rateLimit') for (k, n), d in by.items() if k == 'Middleware'}
if mw.get('auth-rate-limit', {}).get('average') is None or mw.get('general-rate-limit', {}).get('average') is None: fails.append("rate-limit Middlewares missing")
https = by.get(('IngressRoute', 'app-https'))
routes = https['spec']['routes'] if https else []
auth = [r for r in routes if '/api/signin' in r['match'] and '/api/signup' in r['match']]
if not auth or [m['name'] for m in auth[0].get('middlewares', [])] != ['auth-rate-limit']: fails.append("signin/signup route lacks auth-rate-limit")
if not https or https['spec']['tls']['secretName'] != 'app-tls' or https['spec']['entryPoints'] != ['websecure']: fails.append("app-https tls/entrypoint wrong")
if any('kanban-board-rud-vlad-473.duckdns.org' not in r['match'] for r in routes): fails.append("a prod route matches a non-prod host")
cert = by.get(('Certificate', 'app'))
if not cert or cert['spec']['issuerRef']['name'] != 'letsencrypt-staging': fails.append("certificate not on staging issuer")
raw = open('k8s/overlays/prod/kustomization.yaml').read()
if '"$imagepolicy": "flux-system:kanban-board-backend-prod:tag"' not in raw: fails.append("prod setter marker missing")
nonprod = subprocess.check_output([kubectl, 'kustomize', 'k8s/overlays/nonprod'], text=True)
if 'rateLimit' in nonprod: fails.append("nonprod renders a rate limit")
if fails:
    print("\n".join("FAIL: " + x for x in fails)); sys.exit(1)
print("TASK 1 PASS")
PY
    </automated>
    <fails_when>Non-zero exit or any `FAIL:` line. Specifically:
- an object renders outside kanban-prod;
- prod Redpanda `--memory` is not 2G or its limit is not 2200Mi;
- the app image is not the prod repo with a `main-` tag, or its limit is not 3Gi;
- a rate-limit Middleware is missing, or the signin/signup route does not carry exactly auth-rate-limit;
- the https route's entryPoint/TLS is wrong, or any route matches a non-prod host;
- the Certificate is not on letsencrypt-staging, or the setter marker is absent;
- the nonprod overlay renders any rateLimit.</fails_when>
  </verify>
  <acceptance_criteria>
    - `kubectl kustomize k8s/overlays/prod` validates with kubeconform -strict, including the IngressRoute, Middleware and Certificate schemas (no `-skip` added in this plan).
    - The ipv6Subnet field is accepted by the pinned Traefik CRD schema. If it is not, drop it and record why in the Middleware comment.
  </acceptance_criteria>
  <done>The production workload and its public edge are fully described and gated, ready to unsuspend.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Final Postgres tier — StatefulSet, pinned Services, init scripts (roles before restore), NetworkPolicy, invariant I9</name>
  <reversibility rating="costly">D-05/D-11: moving data out again is another dump/restore; namespaces and DNS are baked into every client.</reversibility>
  <files>k8s/data/postgres/kustomization.yaml, k8s/data/postgres/postgres.yaml, k8s/data/postgres/networkpolicy.yaml, k8s/data/postgres/init/01-create-databases-and-roles.sh, k8s/data/postgres/init/02-create-monitoring-role.sh, scripts/verify-k8s-invariants.py, scripts/verify-k8s-invariants-selftest.py</files>
  <read_first>
    - docker-compose.prod.yml `postgres:` block — `-c` flags, `mem_limit` 256m and its OOM-ladder comment, env names, healthcheck.
    - docker/postgres-init/01-create-databases-and-roles.sh; scripts/verify-postgres-init-quoting.sh (its usage line).
    - docs/INFRA_RUNBOOK.md "The monitoring role, and why it is not an init script" — the grants to reproduce.
    - k8s/data/postgres-bridge/postgres-bridge.yaml (13-02) — Service name/port/clusterIP that must match exactly.
  </read_first>
  <behavior>
    - I9: when docker/postgres-init/01-create-databases-and-roles.sh exists, k8s/data/postgres/init/01-create-databases-and-roles.sh must be byte-identical to it. A one-byte difference → `FAIL: I9`.
    - I6 (from 13-01) evaluates the rendered postgres StatefulSet and passes at shared_buffers=64MB, max_connections=25, work_mem=4MB, limit 256Mi.
  </behavior>
  <action>
**init/01:** byte-identical copy of the Compose script.

**init/02-create-monitoring-role.sh:** same guard and quoting conventions as 01.
- Require `MONITORING_DB_PASS`, `PROD_DB_NAME`, `NONPROD_DB_NAME`.
- `CREATE ROLE monitoring WITH LOGIN PASSWORD :'monitoring_pass'`; `GRANT CONNECT` on both databases; `GRANT pg_monitor TO monitoring`.
- It runs on the empty PVC's first boot, so the role exists before the restore. Phase 12 had to do this by hand because its volume was already populated.

Run `bash scripts/verify-postgres-init-quoting.sh k8s/data/postgres/init` (hostile-credential harness, needs Docker locally). Extend its assertions only if it cannot see the 02 script's role.

**postgres.yaml:**
- ConfigMap `postgres-init`, generated from `init/` by configMapGenerator.
- Service `postgres`: ClusterIP `10.43.54.32`, selector `app.kubernetes.io/name: postgres`, port 5432 `postgres`.
- Service `postgres-headless`: `clusterIP: None`.
- StatefulSet `postgres`:
  - serviceName postgres-headless, replicas 1, image `postgres:16` (tag parity with Compose and Testcontainers, Phase 11 D-02).
  - args `-c shared_buffers=64MB -c work_mem=4MB -c max_connections=25 -c shared_preload_libraries=pg_stat_statements`.
  - envFrom Secret `postgres-init` (keys `POSTGRES_USER POSTGRES_PASSWORD PROD_DB_NAME PROD_DB_USER PROD_DB_PASS NONPROD_DB_NAME NONPROD_DB_USER NONPROD_DB_PASS MONITORING_DB_PASS`, created in 13-06 — D-10); env `POSTGRES_DB=postgres`, `PGDATA=/var/lib/postgresql/data/pgdata`.
  - `postgres-init` ConfigMap mounted at `/docker-entrypoint-initdb.d`.
  - Readiness exec `pg_isready -h 127.0.0.1 -U $(POSTGRES_USER) -d postgres`.
  - `terminationGracePeriodSeconds: 60`.
  - Resources: limits 256Mi and requests from 11-03's measured peak, with a MEASURED comment citing 11-03/11-08.
  - volumeClaimTemplate `data`: local-path, 5Gi.

**networkpolicy.yaml:** NetworkPolicy `postgres-ingress`, podSelector `app.kubernetes.io/name: postgres`, policyTypes [Ingress], TCP 5432, from:
- (a) namespaceSelector `kubernetes.io/metadata.name` In [kanban-prod, kanban-nonprod];
- (b) namespaceSelector `kubernetes.io/metadata.name: monitoring` AND podSelector `kanban-board/postgres-client: "true"`, in one peer.

A comment states that node-local sources (the CI SSH forward in 13-06) rely on kube-router admitting node-originated traffic. 13-06 proves that live.

**Invariants:** add I9 to verify-k8s-invariants.py, and to the selftest a fixture pair: identical passes, a 1-byte diff fails.
  </action>
  <verify>
    <automated>
set -e
cd "$(git rev-parse --show-toplevel)"
cmp docker/postgres-init/01-create-databases-and-roles.sh k8s/data/postgres/init/01-create-databases-and-roles.sh
bash scripts/verify-postgres-init-quoting.sh k8s/data/postgres/init
python3 scripts/verify-k8s-invariants-selftest.py && python3 scripts/verify-k8s-invariants.py
bash scripts/verify-k8s-manifests.sh
python3 - <<'PY'
import subprocess, sys, yaml
kubectl = subprocess.check_output(['bash', 'scripts/verify-k8s-manifests.sh', '--tool', 'kubectl'], text=True).strip()
docs = [d for d in yaml.safe_load_all(subprocess.check_output([kubectl, 'kustomize', 'k8s/data/postgres'], text=True)) if d]
by = {(d['kind'], d['metadata']['name']): d for d in docs}
bridge = {(d['kind'], d['metadata']['name']): d for d in yaml.safe_load_all(open('k8s/data/postgres-bridge/postgres-bridge.yaml')) if d}
fails = []
svc, bsvc = by.get(('Service', 'postgres')), bridge.get(('Service', 'postgres'))
if not svc or svc['spec'].get('clusterIP') != '10.43.54.32' or svc['spec']['ports'][0]['port'] != 5432: fails.append("postgres Service wrong")
if bsvc and (bsvc['spec'].get('clusterIP') != svc['spec'].get('clusterIP') or bsvc['spec']['ports'][0].get('name') != svc['spec']['ports'][0].get('name')):
    fails.append("bridge and final Service differ in clusterIP/port name")
c = by[('StatefulSet', 'postgres')]['spec']['template']['spec']['containers'][0]
j = ' '.join(c.get('args', []))
for f in ('shared_buffers=64MB', 'work_mem=4MB', 'max_connections=25', 'shared_preload_libraries=pg_stat_statements'):
    if f not in j: fails.append(f"missing -c {f}")
if c['image'] != 'postgres:16' or c['resources']['limits']['memory'] != '256Mi': fails.append("image/limit drift")
np = by.get(('NetworkPolicy', 'postgres-ingress'))
if not np: fails.append("NetworkPolicy missing")
else:
    peers = [p for r in np['spec']['ingress'] for p in r.get('from', [])]
    ports = [p['port'] for r in np['spec']['ingress'] for p in r.get('ports', [])]
    if set(ports) != {5432}: fails.append(f"ports {ports}")
    ns_vals = [v for p in peers for e in p.get('namespaceSelector', {}).get('matchExpressions', []) for v in e.get('values', [])]
    if sorted(ns_vals) != ['kanban-nonprod', 'kanban-prod']: fails.append(f"app namespaces {ns_vals}")
    exp = [p for p in peers if p.get('podSelector', {}).get('matchLabels', {}).get('kanban-board/postgres-client') == 'true'
           and p.get('namespaceSelector', {}).get('matchLabels', {}).get('kubernetes.io/metadata.name') == 'monitoring']
    if len(exp) != 1: fails.append("exporter peer not a single AND-combined selector")
    if len(peers) != 2: fails.append(f"unexpected extra peers: {peers}")
if fails:
    print("\n".join("FAIL: " + x for x in fails)); sys.exit(1)
print("TASK 2 PASS")
PY
    </automated>
    <fails_when>Non-zero exit, a `cmp` difference, a quoting-harness failure, or any `FAIL:` line. Specifically:
- the Service clusterIP/port or port name diverges from the bridge;
- a `-c` flag, the image or the 256Mi limit drifted;
- the NetworkPolicy admits any port other than 5432, any namespace outside the two app namespaces, a non-AND exporter peer, or an extra peer;
- I6/I9 or their selftests fail.</fails_when>
  </verify>
  <acceptance_criteria>
    - The quoting harness passes against `k8s/data/postgres/init` with 02 present.
    - The selftest shows I9 firing on a one-byte difference.
    - The NetworkPolicy has exactly two peers and one port.
  </acceptance_criteria>
  <done>In-cluster Postgres is declared with roles-before-restore semantics and a least-privilege network boundary.</done>
</task>

<task type="auto">
  <name>Task 3: cert-manager HelmRelease, staging/production ClusterIssuers, monitoring Certificate, row-count SQL</name>
  <reversibility rating="costly">D-13: cert-manager becomes the only certificate lifecycle once Caddy is deleted.</reversibility>
  <files>k8s/platform/cert-manager/kustomization.yaml, k8s/platform/cert-manager/cert-manager.yaml, k8s/platform/edge/kustomization.yaml, k8s/platform/edge/clusterissuers.yaml, k8s/platform/edge/certificate-monitoring.yaml, scripts/cutover/row-counts.sql</files>
  <read_first>
    - 13-RESEARCH.md "cert-manager staging → production issuer switch".
    - src/main/resources/db/migration/V1__init.sql and V3__add_activity_log.sql — the exact table names.
  </read_first>
  <action>
**cert-manager.yaml:**
- HelmRepository `jetstack` (flux-system), `https://charts.jetstack.io`.
- HelmRelease `cert-manager`: targetNamespace `cert-manager`, `install.createNamespace: true`, chart version pinned to the latest v1.x at authoring time.
- Values: `crds.enabled: true`, `crds.keep: true`, `prometheus.enabled: false` (no ServiceMonitor CRD exists before 13-07), and controller/webhook/cainjector resources labelled PROVISIONAL (13-09).

**clusterissuers.yaml:** ClusterIssuers `letsencrypt-staging` (acme-staging-v02) and `letsencrypt-production` (acme-v02).
- Each has a privateKeySecretRef of the same name and a solver `http01.ingress.ingressClassName: traefik`.
- Omit `email`: the Caddy edge registered with ACME without one, and an address should not be sent to Let's Encrypt unless the operator supplies one.

**certificate-monitoring.yaml:** Certificate `grafana` in namespace monitoring, secretName `grafana-tls`, the monitoring hostname, issuer `letsencrypt-staging`. It lives here rather than in k8s/monitoring so the monitoring hostname validates in the 13-06 window, before Grafana exists (D-13 "all three hostnames").

**edge/kustomization.yaml** lists the issuers and the Certificate. **cert-manager/kustomization.yaml** lists the HelmRepository and HelmRelease.

**scripts/cutover/row-counts.sql:** one `SELECT '<table>' AS t, count(*) … UNION ALL …` over users, boards, columns, tasks, subtasks, activity_log, `ORDER BY t`. The header comment gives its use: `psql -XAt -F'|' -f scripts/cutover/row-counts.sql`, before (Compose) and after (in-cluster); the two outputs are `diff`ed (D-08.2).
  </action>
  <verify>
    <automated>
set -e
cd "$(git rev-parse --show-toplevel)"
bash scripts/verify-k8s-manifests.sh && python3 scripts/verify-k8s-invariants.py
python3 - <<'PY'
import re, sys, yaml
fails = []
iss = {d['metadata']['name']: d for d in yaml.safe_load_all(open('k8s/platform/edge/clusterissuers.yaml')) if d}
for n, url in (('letsencrypt-staging', 'acme-staging-v02'), ('letsencrypt-production', 'acme-v02.api')):
    a = iss.get(n, {}).get('spec', {}).get('acme', {})
    if url not in a.get('server', ''): fails.append(f"{n} server {a.get('server')}")
    if 'email' in a: fails.append(f"{n} sends an email address")
    if a.get('solvers', [{}])[0].get('http01', {}).get('ingress', {}).get('ingressClassName') != 'traefik': fails.append(f"{n} solver not traefik http01")
cm = yaml.safe_load(open('k8s/platform/edge/certificate-monitoring.yaml'))
if cm['metadata']['namespace'] != 'monitoring' or cm['spec']['secretName'] != 'grafana-tls' or cm['spec']['issuerRef']['name'] != 'letsencrypt-staging':
    fails.append("monitoring certificate wrong")
hr = [d for d in yaml.safe_load_all(open('k8s/platform/cert-manager/cert-manager.yaml')) if d and d['kind'] == 'HelmRelease'][0]
v = hr['spec']['chart']['spec']['version']
if not re.fullmatch(r'v?1\.\d+\.\d+', str(v)): fails.append(f"cert-manager chart not pinned: {v}")
sql = open('scripts/cutover/row-counts.sql').read().lower()
for t in ('users', 'boards', 'columns', 'tasks', 'subtasks', 'activity_log'):
    if not re.search(rf"from\s+{t}\b", sql): fails.append(f"row-counts.sql misses {t}")
if fails:
    print("\n".join("FAIL: " + x for x in fails)); sys.exit(1)
print("TASK 3 PASS")
PY
docker run --rm -v "$PWD/scripts/cutover:/s:ro" -e POSTGRES_PASSWORD=x --name rc-check -d postgres:16 >/dev/null
trap 'docker rm -f rc-check >/dev/null 2>&1' EXIT
until docker exec rc-check pg_isready -q; do sleep 1; done
for f in src/main/resources/db/migration/V*.sql; do docker exec -i rc-check psql -q -v ON_ERROR_STOP=1 -U postgres < "$f"; done
OUT=$(docker exec rc-check psql -XAt -F'|' -U postgres -f /s/row-counts.sql)
echo "$OUT"
printf '%s\n' "$OUT" | python3 -c "import sys; rows=[l.split('|')[0] for l in sys.stdin.read().split() if '|' in l]; assert rows == sorted(['activity_log','boards','columns','subtasks','tasks','users']), rows"
echo "TASK 3 SQL PASS"
    </automated>
    <fails_when>Non-zero exit or any `FAIL:` line. Specifically:
- an issuer points at the wrong ACME server, carries an `email`, or lacks the traefik HTTP-01 solver;
- the monitoring Certificate's namespace, secret or issuer is wrong;
- the cert-manager chart version is not an exact v1.x.y;
- row-counts.sql omits a table;
- against a scratch postgres:16 carrying all Flyway migrations, the SQL errors or prints anything other than exactly six rows.</fails_when>
  </verify>
  <acceptance_criteria>
    - Running row-counts.sql against a freshly migrated scratch database prints six `table|0` lines, sorted.
    - The edge and cert-manager roots validate under kubeconform -strict with the pinned CRD catalog.
  </acceptance_criteria>
  <done>Certificate issuance for all three hostnames and the D-08.2 parity check are ready for the window.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Traefik → app (from 13-06) | Public auth endpoints, rate limited |
| App namespaces → kanban-data Postgres | Cross-namespace DB access |
| cert-manager ↔ Let's Encrypt | ACME account + HTTP-01 challenges |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-17 | Denial of Service | /api/signin, /api/signup bcrypt cost | high | mitigate | auth-rate-limit on exactly those paths, general-rate-limit elsewhere on the prod host (Caddy scope preserved); thresholds re-derived and proven per client in 13-08. |
| T-13-18 | Elevation of Privilege | Direct Postgres access from a compromised pod | high | mitigate | NetworkPolicy postgres-ingress: 5432 from two app namespaces + one labelled exporter pod only; least-privilege roles recreated before restore (D-05, Phase 11 D-01). |
| T-13-19 | Tampering | Init scripts diverging from the reviewed, injection-hardened Compose script | medium | mitigate | I9 byte-identity gate; hostile-credential quoting harness run against the new init dir. |
| T-13-20 | Spoofing | Premature production ACME issuance / LE rate limits | medium | mitigate | Every Certificate starts on letsencrypt-staging; the production flip is a separate 13-06 commit after 3/3 validate. |
| T-13-21 | Information Disclosure | Operator email sent to the ACME CA | low | mitigate | `email` omitted, matching the current Caddy registration. |
| T-13-SC | Tampering | cert-manager chart | medium | mitigate | Pinned exact version from charts.jetstack.io (Research audit: Approved). |
</threat_model>

<verification>
1. Prod overlay, data/postgres, cert-manager and edge roots pass kubeconform -strict and I1-I9.
2. The init scripts pass the hostile-quoting harness; row-counts.sql runs against a migrated scratch DB.
3. Nothing here reconciles: all consuming Kustomizations stay suspended until 13-06 (13-02).
</verification>

<success_criteria>
- Every production-side manifest the window needs exists, is reviewed and is gated; the window only flips `suspend`/`path`/`issuerRef`.
</success_criteria>

<output>
Create `.planning/phases/13-introduce-kubernetes/13-04-SUMMARY.md` when done.
</output>
