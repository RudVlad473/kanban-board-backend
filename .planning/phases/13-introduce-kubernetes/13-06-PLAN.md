---
phase: 13-introduce-kubernetes
plan: 06
type: execute
wave: 4
depends_on: ["13-04", "13-05"]
files_modified:
  - k8s/flux-system/data.yaml
  - k8s/flux-system/cert-manager.yaml
  - k8s/flux-system/edge.yaml
  - k8s/flux-system/apps-prod.yaml
  - k8s/flux-system/apps-nonprod.yaml
  - k8s/platform/traefik/helmchartconfig.yaml
  - k8s/overlays/nonprod/kustomization.yaml
  - k8s/overlays/nonprod/ingressroute.yaml
  - k8s/overlays/nonprod/certificate.yaml
  - k8s/overlays/prod/certificate.yaml
  - k8s/platform/edge/certificate-monitoring.yaml
  - .github/workflows/deploy.yml
  - infra/vm/k3s/config.yaml
  - infra/vm/sshd/kanban-ci-tunnel.conf
  - infra/vm/README.md
  - docs/INFRA_RUNBOOK.md
autonomous: false
requirements: [D-01, D-03, D-05, D-06, D-08, D-10, D-11, D-13, D-14, D-16]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Production and nonprod are served by k3s: Traefik owns 80/443 via ServiceLB with externalTrafficPolicy Local, and all three hostnames present Let's Encrypt production certificates issued after staging validated all three (D-01, D-13)."
    - "Only one production stack was in memory at a time: every Compose container was stopped before in-cluster prod workloads started, inside one announced window whose start, end and downtime are recorded (D-03)."
    - "Both databases were restored with pg_restore into in-cluster Postgres whose roles (prod, nonprod, monitoring) were created by initdb before the restore, and row counts for users, boards, columns, tasks, subtasks and activity_log match the snapshot taken after the Compose app stopped (D-05, D-08.2)."
    - "The Compose prod broker was stopped only after activity-log lag 0 and an empty DLT; the fresh prod Redpanda got its schemas from the app pod's initContainer (D-06, D-14)."
    - "No workflow can deploy to or restart Compose: deploy-to-netcup, register-schemas-production and cleanup-unused-image are gone; flyway-verify and flyway-verify-nonprod pass against in-cluster Postgres through an SSH forward limited by PermitOpen to 10.43.54.32:5432 (D-14)."
    - "The cutover pg_dump files and pre-cutover counts sit in /root/k3s-cutover-<date>/ outside any volume, with recorded sha256 (D-08.4)."
    - "App pods reach Postgres and a non-exporter pod in monitoring cannot, proving the NetworkPolicy live (D-11)."
    - "Traefik's interim `web` NodePort 30080 pin is never absent while nonprod app replicas > 0 and Caddy still proxies to it. W1 keeps the pin while scaling nonprod to 0. W2 removes it, and is pushed only after a `docker ps` guard shows every Compose container, Caddy included, stopped. The per-commit render assertion in Task 2 checks this, not prose (D-02, D-03)."
    - "Every HTTP→HTTPS redirect on the prod and nonprod hosts excludes `/.well-known/acme-challenge/` in its own rule. I10 asserts this at every staged commit before the window stops Compose, and it is probed live after the window (D-13)."
  artifacts:
    - "Commits W1-W4 (below) on main"
    - "infra/vm/sshd/kanban-ci-tunnel.conf — Match block for the two CI users"
    - "docs/INFRA_RUNBOOK.md section `## Production cutover to k3s — Plan 13-06 (<date>)`"
  key_links:
    - "Flux Kustomization data path ./k8s/data/postgres-bridge -> ./k8s/data/postgres (same Service postgres, clusterIP 10.43.54.32)"
    - "HelmChartConfig traefik LoadBalancer + ETP Local + k3s servicelb re-enabled -> svclb hostPorts 80/443"
    - "CI flyway-verify -> ssh -L 15432:10.43.54.32:5432 -> kube-router node-originated allowance -> postgres-0"
---

<objective>
Execute the production cutover in one announced maintenance window: Compose stopped, data dumped and restored with parity, prod and nonprod workloads on k3s, Traefik and cert-manager owning the public edge, and the Compose deploy path removed from CI.

Purpose: D-01 is one-way. This plan crosses it only after the operator approves on 13-05's measured evidence, with an abort path that stays open until the prod apps start.
Output: production on k3s; the Compose runtime stopped (not deleted — D-04 waits for D-08).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-introduce-kubernetes/13-CONTEXT.md
@.planning/phases/13-introduce-kubernetes/13-01-PLAN.md
@.planning/phases/13-introduce-kubernetes/13-05-SUMMARY.md
@.github/workflows/deploy.yml
@docs/SESSION_LESSONS.md
</context>

<artifacts_this_phase_produces>
| Kind | Name | Where |
|---|---|---|
| Commit W1 (k8s only) | nonprod replicas 0 + TLS route/Certificate + ACME-safe redirect; data → ./k8s/data/postgres; cert-manager/edge unsuspended; Traefik LoadBalancer + ETP Local, `web` NodePort 30080 pin kept | k8s/ |
| Commit W2 (k8s only) | nonprod replicas restored; apps-prod unsuspended; interim `web` NodePort pin removed (Caddy stopped by then) | k8s/ |
| Commit W3 (k8s only) | 3 Certificates → letsencrypt-production | k8s/ |
| Commit W4 | Compose deploy jobs removed; flyway-verify via SSH forward; prod cleanup keeps 5; `workflow_dispatch`; k3s config (servicelb on); sshd Match block | .github/workflows/deploy.yml, infra/vm/ |
| Secrets (VM only) | kanban-data/postgres-init, kanban-prod/app-env | cluster |
| Cutover evidence | dumps, counts, sha256 | /root/k3s-cutover-<date>/ on the VM |
| Runbook | `## Production cutover to k3s — Plan 13-06` | docs/INFRA_RUNBOOK.md |
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: D-01 go/no-go — move production to k3s in a named maintenance window</name>
  <reversibility rating="one-way">D-01 combined with D-04: once 13-10 deletes the Compose volumes, returning means rebuilding Compose from git history and restoring from a dump.</reversibility>
  <read_first>
    - .planning/phases/13-introduce-kubernetes/13-05-SUMMARY.md and runbook `## Nonprod on k3s — Plan 13-05` (GitOps cycle proof, kubectl evidence, projected final budget and verdict).
  </read_first>
  <decision>Cut production over to k3s in a maintenance window at a stated time.</decision>
  <context>
The message must show:
1. 13-05's projected final `requests` sum against MemAvailable, with its PASS/FAIL verdict.
2. The D-02 cycle evidence (SHAs).
3. `dig +short AAAA` for all three hostnames. k3s here is IPv4 single-stack, so a non-empty AAAA means IPv6 clients lose access after cutover — surface that as its own decision if present.
4. `gh api repos/RudVlad473/kanban-board-backend/branches/main/protection`, still 404.
5. The planned window: expected 30–45 min of prod downtime (stop → restore → certs).
6. The abort rule: until W2 is pushed, abort = revert W1, re-disable ServiceLB, `docker compose start` both projects. After W2, only forward fixes.
7. The freeze: no pushes to main except W1-W4 during the window.

Approving also authorizes 13-07 (observability activation, D-17), which starts right after this plan.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed at &lt;time&gt; (window announced)</name>
      <pros>Completes D-01; production runs on the same GitOps path proven on nonprod.</pros>
      <cons>One-way once D-04 runs; ~30-45 min public downtime; monitoring is dark until 13-07 finishes.</cons>
    </option>
    <option id="postpone">
      <name>Postpone</name>
      <pros>The interim is stable: nonprod on k3s, prod on Compose.</pros>
      <cons>Two runtimes stay in memory; the phase stays open.</cons>
    </option>
    <option id="abandon">
      <name>Abandon the prod cutover (revisit D-01)</name>
      <pros>Production stays on Compose.</pros>
      <cons>Contradicts locked D-01; the phase needs re-scoping via /gsd-discuss-phase.</cons>
    </option>
  </options>
  <resume-signal>Reply proceed &lt;time&gt; (and confirm the window was announced), postpone, or abandon.</resume-signal>
  <acceptance_criteria>
    - The reply, the window time and the confirmation that the window was announced are recorded in the SUMMARY.
    - A non-empty AAAA answer for any hostname halts this plan until the operator decides on IPv6 separately.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Stage W1-W4 locally (unpushed), validate them, and rehearse the restore against a pre-window dump</name>
  <files>k8s/flux-system/data.yaml, k8s/flux-system/cert-manager.yaml, k8s/flux-system/edge.yaml, k8s/flux-system/apps-prod.yaml, k8s/flux-system/apps-nonprod.yaml, k8s/platform/traefik/helmchartconfig.yaml, k8s/overlays/nonprod/kustomization.yaml, k8s/overlays/nonprod/ingressroute.yaml, k8s/overlays/nonprod/certificate.yaml, k8s/overlays/prod/certificate.yaml, k8s/platform/edge/certificate-monitoring.yaml, .github/workflows/deploy.yml, infra/vm/k3s/config.yaml, infra/vm/sshd/kanban-ci-tunnel.conf, infra/vm/README.md</files>
  <read_first>
    - .github/workflows/deploy.yml — flyway-verify and flyway-verify-nonprod (env/secret names, pg_isready precheck), deploy-to-netcup, register-schemas-production, cleanup-old-images, cleanup-unused-image, the `on:` block.
    - docs/INFRA_RUNBOOK.md "Deploy user setup — Plan 05-05" and "Nonprod CI deploy identity … — Plan 09-01" — the two CI SSH user names and their authorized_keys options.
    - k8s/overlays/prod/certificate.yaml, k8s/platform/edge/certificate-monitoring.yaml (13-04); k8s/platform/traefik/helmchartconfig.yaml (13-02).
  </read_first>
  <action>
Build four commits on local branch `cutover-13-06`, rebased on origin/main. **Do not push.** W1-W3 touch only `k8s/**`, so they never start deploy.yml. W4 is the only commit that does, and by then deploy.yml no longer contains a Compose job.

**W1** (window step 4):
- `k8s/overlays/nonprod/kustomization.yaml`: `replicas: [{name: app, count: 0}]`.
- Nonprod ingressroute.yaml:
  - IngressRoute `app-https`, websecure, `tls.secretName: app-tls`, nonprod host → `app:8080`;
  - Middleware `redirect-https` in kanban-nonprod (redirectScheme https, permanent). Middlewares are namespaced, so the prod overlay's copy does not resolve here;
  - IngressRoute `app-http`, web, exactly one route ``Host(`kanban-board-rud-vlad-473-nonprod.duckdns.org`) && !PathPrefix(`/.well-known/acme-challenge/`)`` → `redirect-https`. The negation keeps cert-manager's HTTP-01 solver path out of the redirect by construction, not by Traefik's rule-length priority (D-13). It carries the same one-line reason comment as the prod route, and I10 gates its shape.

  No rate limit on nonprod (D-13).
- New nonprod `certificate.yaml`: Certificate `app` → `app-tls`, issuer `letsencrypt-staging`.
- `apps-nonprod.yaml` dependsOn gains `edge`.
- `data.yaml` path → `./k8s/data/postgres`.
- `cert-manager.yaml` and `edge.yaml`: `suspend: false`.
- `helmchartconfig.yaml`:
  - service type LoadBalancer, `service.spec.externalTrafficPolicy: Local` (D-13, Pitfall 2);
  - websecure exposed; **the `web` nodePort 30080 pin stays**. Caddy's nonprod upstream `172.17.0.1:30080` is its only consumer, and Caddy keeps running until window step 6;
  - access logs kept.

**W2** (window step 11):
- remove the replicas-0 entry;
- `apps-prod.yaml` `suspend: false`;
- `helmchartconfig.yaml`: remove the `web` nodePort pin.

The pin removal lives here and not in W1 so that no commit, split or retry can leave the pin gone while nonprod replicas are above 0 and Caddy still proxies to 30080. W2 restores the replicas, but it is pushed only after step 6 has stopped Caddy, and step 11's guard asserts that.

**W3** (window step 13): the three Certificates' `issuerRef.name` → `letsencrypt-production`.

**W4** (window step 15):
- deploy.yml:
  - delete jobs `deploy-to-netcup`, `register-schemas-production` and `cleanup-unused-image`;
  - rewrite `cleanup-old-images` exactly like 13-05's nonprod version (keep the 5 newest sortable tags; needs setup + build);
  - add `workflow_dispatch: {}`.
- Rewrite `flyway-verify` and `flyway-verify-nonprod`; each keeps its environment, secrets and vars.
  - Drop the SCP staging step.
  - Write `secrets.NETCUP_SSH_KEY` to a 0600 file.
  - Build known_hosts from `ssh-keyscan` and refuse unless `ssh-keygen -lf` matches `secrets.NETCUP_HOST_FINGERPRINT`.
  - Open `ssh -f -N -o ExitOnForwardFailure=yes -L 15432:10.43.54.32:5432`.
  - Precheck `docker run --rm --network host postgres:16 pg_isready -h 127.0.0.1 -p 15432 -t 5`, with a named error.
  - Run `flyway/flyway:11.7.2 migrate` with `--network host`, `FLYWAY_URL=jdbc:postgresql://127.0.0.1:15432/${DB_NAME}`, and the migrations mounted read-only from the runner checkout.
  - Close the forward in an `if: always()` step.

  The appleboy/* actions are no longer used by these jobs; update the top-of-file risk-acceptance comment to say so.
- `infra/vm/k3s/config.yaml`: drop `servicelb` from `disable` and update the dated comment.
- New `infra/vm/sshd/kanban-ci-tunnel.conf`:
  - `Match User <prod CI user>,<nonprod CI user>`;
  - `AllowTcpForwarding local`, `PermitOpen 10.43.54.32:5432`;
  - `X11Forwarding no`, `AllowAgentForwarding no`, `PermitTunnel no`, `GatewayPorts no`.
- `infra/vm/README.md` lists it and the install step (copy to `/etc/ssh/sshd_config.d/`, `sshd -t`, reload).
- If either user's authorized_keys line carries `restrict`, W4 also documents the needed `port-forwarding,permitopen="10.43.54.32:5432"` options; they are applied on the VM in Task 3.

**Validate every commit:** after each one, `bash scripts/verify-k8s-manifests.sh && python3 scripts/verify-k8s-invariants.py`.

**Restore rehearsal (VM, no production impact):**
1. `docker exec` pg_dump both databases now, `-Fc`, to `/root/k3s-cutover-rehearsal/`.
2. Start a scratch `postgres:16` container on the VM. Env comes from a 0600 file mapped the way `kanban-data/postgres-init` will be; the committed `k8s/data/postgres/init/` is mounted at `/docker-entrypoint-initdb.d`.
3. Run `pg_restore -U <superuser> -d <db> --exit-on-error --single-transaction` for both databases.
4. Run row-counts.sql against the scratch container, and confirm `\du` lists the prod, nonprod and monitoring roles.
5. Delete the scratch container and the rehearsal files.

If the restore needs extra flags (e.g. schema public ownership), fold them into Task 3's commands and record why.
  </action>
  <verify>
    <automated>
set -e
cd "$(git rev-parse --show-toplevel)"
test "$(git rev-parse --abbrev-ref HEAD)" = cutover-13-06
test "$(git rev-list --count origin/main..HEAD)" = 4
for c in $(git rev-list --reverse origin/main..HEAD); do git checkout -q "$c"; bash scripts/verify-k8s-manifests.sh >/dev/null && python3 scripts/verify-k8s-invariants.py >/dev/null || { echo "FAIL: gates red at $c"; exit 1; }; done
git checkout -q cutover-13-06
python3 - <<'PY'
import re, subprocess, sys, tempfile, yaml
fails = []
revs = subprocess.check_output(['git', 'rev-list', '--reverse', 'origin/main..HEAD'], text=True).split()
for i, r in enumerate(revs[:3], 1):
    files = subprocess.check_output(['git', 'show', '--name-only', '--format=', r], text=True).split()
    if any(not f.startswith('k8s/') for f in files): fails.append(f"W{i} touches non-k8s files {files}")
kubectl = subprocess.check_output(['bash', 'scripts/verify-k8s-manifests.sh', '--tool', 'kubectl'], text=True).strip()
def render(rev, root):
    with tempfile.TemporaryDirectory() as d:
        subprocess.run(['tar', '-x', '-C', d], input=subprocess.run(['git', 'archive', rev, 'k8s'], check=True, capture_output=True).stdout, check=True)
        return [x for x in yaml.safe_load_all(subprocess.check_output([kubectl, 'kustomize', f'{d}/{root}'], text=True)) if x]
def web_pinned(text):
    values = yaml.safe_load(yaml.safe_load(text)['spec']['valuesContent'])
    return '30080' in yaml.safe_dump(values)
def pinned_at(rev): return web_pinned(subprocess.check_output(['git', 'show', f'{rev}:k8s/platform/traefik/helmchartconfig.yaml'], text=True))
def app_replicas(docs): return next(d for d in docs if d['kind'] == 'Deployment' and d['metadata']['name'] == 'app')['spec'].get('replicas', 1)
n1, n2 = render(revs[0], 'k8s/overlays/nonprod'), render(revs[1], 'k8s/overlays/nonprod')
if not pinned_at(revs[0]): fails.append("W1 removes Traefik's web NodePort 30080 pin while Caddy still proxies nonprod to it; the removal belongs in W2")
if app_replicas(n1) != 0: fails.append("W1 does not render nonprod app replicas 0")
if pinned_at(revs[1]): fails.append("W2 does not remove the interim web NodePort pin")
if app_replicas(n2) == 0: fails.append("W2 does not restore nonprod app replicas")
acme = re.compile(r"Host\(`kanban-board-rud-vlad-473-nonprod\.duckdns\.org`\) && !PathPrefix\(`/\.well-known/acme-challenge/`\)")
http = next((d for d in n1 if d['kind'] == 'IngressRoute' and d['metadata']['name'] == 'app-http'), None)
hroutes = http['spec']['routes'] if http else []
if not http or http['spec']['entryPoints'] != ['web'] or len(hroutes) != 1 or not acme.fullmatch(hroutes[0]['match']) or [m['name'] for m in hroutes[0].get('middlewares', [])] != ['redirect-https']:
    fails.append("W1 nonprod app-http is not one web route excluding /.well-known/acme-challenge/ from its redirect")
if not any(d['kind'] == 'Middleware' and d['metadata']['name'] == 'redirect-https' and 'redirectScheme' in d['spec'] for d in n1):
    fails.append("W1 nonprod overlay lacks its own redirect-https redirectScheme Middleware")
jobs = yaml.safe_load(open('.github/workflows/deploy.yml'))['jobs']
for gone in ('deploy-to-netcup', 'register-schemas-production', 'cleanup-unused-image', 'deploy-to-nonprod'):
    if gone in jobs: fails.append(f"{gone} still in W4 deploy.yml")
for j in ('flyway-verify', 'flyway-verify-nonprod'):
    txt = yaml.safe_dump(jobs.get(j, {}))
    if '10.43.54.32:5432' not in txt or 'NETCUP_HOST_FINGERPRINT' not in txt or 'scp-action' in txt: fails.append(f"{j} not converted to fingerprint-pinned SSH forward")
wf = yaml.safe_load(open('.github/workflows/deploy.yml')); on = wf.get('on', wf.get(True))
if 'workflow_dispatch' not in on: fails.append("no workflow_dispatch")
if 'servicelb' in str(yaml.safe_load(open('infra/vm/k3s/config.yaml')).get('disable', [])): fails.append("servicelb still disabled in W4 config")
sshd = open('infra/vm/sshd/kanban-ci-tunnel.conf').read()
for k in ('PermitOpen 10.43.54.32:5432', 'AllowTcpForwarding local', 'X11Forwarding no', 'PermitTunnel no'):
    if k not in sshd: fails.append(f"sshd drop-in lacks {k}")
hcc = open('k8s/platform/traefik/helmchartconfig.yaml').read()
if 'externalTrafficPolicy: Local' not in hcc or web_pinned(hcc): fails.append("Traefik not LB/ETP Local, or still pins 30080 at the tip")
for f in ('k8s/overlays/prod/certificate.yaml', 'k8s/overlays/nonprod/certificate.yaml', 'k8s/platform/edge/certificate-monitoring.yaml'):
    if yaml.safe_load(open(f))['spec']['issuerRef']['name'] != 'letsencrypt-production': fails.append(f"{f} not on production issuer at W3+")
if fails:
    print("\n".join("FAIL: " + x for x in fails)); sys.exit(1)
print("TASK 2 PASS")
PY
test -z "$(ssh netcup-prod 'docker ps -a --format {{.Names}} | grep -i rehears; ls /root/k3s-cutover-rehearsal 2>/dev/null')"
    </automated>
    <fails_when>Non-zero exit or any `FAIL:` line. Specifically:
- the branch is not `cutover-13-06` with exactly four commits ahead of origin/main;
- the gates are red at any intermediate commit. That includes I10 firing on a redirect route whose rule does not exclude `/.well-known/acme-challenge/`;
- W1-W3 touch a file outside k8s/;
- the pin/replicas ordering is broken, judged on the parsed HelmChartConfig values and the rendered nonprod overlay:
  - W1's values drop the `web` NodePort 30080 pin, or W1 does not render nonprod app replicas 0;
  - W2's values still hold the pin, or W2 does not restore nonprod replicas;
- W1's rendered nonprod overlay lacks either piece of the ACME-safe redirect:
  - a single `app-http` web route whose rule is exactly ``Host(`<nonprod host>`) && !PathPrefix(`/.well-known/acme-challenge/`)`` → redirect-https;
  - its own redirectScheme Middleware;
- a Compose deploy job survives in W4;
- a flyway job lacks the pinned-fingerprint forward to 10.43.54.32:5432 or still uses scp-action;
- workflow_dispatch is missing, ServiceLB is still disabled, or the sshd drop-in lacks a restriction;
- Traefik is not LoadBalancer with ETP Local (or still pins 30080);
- a Certificate is not on the production issuer at the tip;
- rehearsal containers or files remain on the VM.</fails_when>
  </verify>
  <acceptance_criteria>
    - The rehearsal output is recorded in the SUMMARY: pg_restore exit 0 for both databases, a `\du` excerpt listing the three roles (names only), six-row counts per database.
    - Nothing is pushed: `git log origin/main..cutover-13-06 --oneline` shows the four staged commits.
  </acceptance_criteria>
  <done>The window's changes are reviewed, validated and rehearsed; the window itself only pushes and moves data.</done>
</task>

<task type="auto">
  <name>Task 3: Run the maintenance window — stop, dump, restore with parity, start on k3s, take over 80/443, staging then production certs, retarget CI</name>
  <reversibility rating="one-way">D-01: production leaves Compose. Abort stays available until step 11, per D-03's single-stack rule.</reversibility>
  <files>docs/INFRA_RUNBOOK.md</files>
  <precondition>Task 1 answered proceed and the named window time has arrived; Task 2's branch is staged; no deploy.yml run is in progress on main (`gh run list --workflow deploy.yml --status in_progress` empty).</precondition>
  <read_first>
    - scripts/cutover/row-counts.sql; k8s/data/postgres/postgres.yaml (Secret key names); Task 2's rehearsal notes in the SUMMARY.
    - docs/INFRA_RUNBOOK.md `## Nonprod on k3s — Plan 13-05` (the rpk commands already proven).
  </read_first>
  <action>
Run over `ssh netcup-prod` as root. Log a UTC timestamp at every step for the runbook. Always `git pull --rebase` before a push, because Flux commits too.

0. **Secrets (D-10).** Create `kanban-data/postgres-init` and `kanban-prod/app-env` from `/opt/deploy/kanban-board-backend/.env.prod`. Write 0600 temp env files, then shred them.
   - postgres-init renames keys: `POSTGRES_SUPERUSER`→`POSTGRES_USER`, `POSTGRES_SUPERUSER_PASS`→`POSTGRES_PASSWORD`, `DB_*`→`PROD_DB_*`; `NONPROD_DB_*` and `MONITORING_DB_PASS` pass through.
   - app-env gets `DB_NAME DB_USER DB_PASS` only.
   - Record `T_start`.
1. `docker compose -p kanban-board-backend stop app`.
2. On `kanban-board-backend-redpanda-1`: `rpk group describe activity-log` shows all LAG 0, and `rpk topic describe kanban.activity.dlt -p` shows HIGH-WATERMARK = LOG-START-OFFSET. Halt if either fails (D-06).
3. `docker compose -p kanban-board-backend stop redpanda`.
4. Push W1. Wait until the nonprod app pod is gone, postgres-0 is Ready, and HelmRelease cert-manager is Ready.
5. With no writers left: `mkdir -m 0700 /root/k3s-cutover-<date>`.
   - Run row-counts.sql against Compose Postgres for kanban_prod and kanban_nonprod → `counts-<db>-before.txt`.
   - `pg_dump -Fc` both → `<db>.dump`.
   - `pg_dumpall --globals-only --no-role-passwords` → `globals.sql`, for reference only.
   - `sha256sum * > SHA256SUMS` (D-05, D-08.4).
6. Stop every remaining Compose container in both projects (`docker compose -p … stop`). Caddy and the old observability stack stop here; D-03 holds, so only one prod stack is in memory.
7. Copy the W4 `infra/vm/k3s/config.yaml` to `/etc/rancher/k3s/config.yaml` and `systemctl restart k3s`. Confirm the edge by what actually carries it, not by `ss`: ServiceLB publishes through hostPort DNAT, which opens no listening socket, so `ss -ltn` shows nothing on 80/443 even when Traefik is serving. Check instead: the svclb-traefik pods are Ready; `iptables -t nat -S CNI-HOSTPORT-DNAT` holds `--dport 80` and `--dport 443` rules; no docker-proxy remains on 80/443 (`ss -ltnp` shows none); and an off-box `curl -sI http://$APP_DOMAIN/` (from the operator's machine, not the VM) gets an HTTP response whose headers carry no `Server: Caddy` (Caddy sets that header; Traefik sets none).
8. Check postgres-0: `\du` shows the prod, nonprod and monitoring roles, and each database is owned by its role with CONNECT revoked from PUBLIC (initdb ran before any restore, D-05).
9. Restore both dumps: `k3s kubectl exec -i -n kanban-data postgres-0 -- sh -c 'pg_restore -U "$POSTGRES_USER" -d <db> --exit-on-error --single-transaction'` < dump, plus any flags the rehearsal needed.
10. Counts after → `counts-<db>-after.txt`. `diff` against before must be empty for both databases (D-08.2).
    **Abort** if step 9 or 10 fails, or postgres-0 is not Ready within 10 min:
    - `git revert` W1 and push;
    - restore the Task 2-era k3s config (ServiceLB disabled) and restart k3s;
    - `docker compose -p kanban-board-backend start` and `docker compose -p kanban-board-nonprod start`;
    - confirm both health endpoints are UP, then stop and report.
11. **Guard, then push W2.**
    - Before pushing: `test -z "$(docker ps -q)"`. Halt if it fails.
    - This proves every Compose container, Caddy included, is stopped. W2 removes the `web` NodePort pin that Caddy's nonprod upstream used, so it must never land while Caddy runs.
    - Push W2, then wait for:
      - prod `redpanda-0` Ready;
      - the prod app's `register-schemas` init exit 0 (fresh registry, D-06/D-14);
      - prod app Ready;
      - nonprod app Ready (Flyway is a no-op);
      - step 7's edge check again: svclb-traefik pods Ready, CNI-HOSTPORT-DNAT holding 80/443, and the off-box `curl -sI` answered without `Server: Caddy`. W2 edits the Traefik Service.
12. Wait for the three Certificates to be Ready on staging. `curl -kv https://<host>` shows a `(STAGING)` issuer for all three. Validation runs through Traefik :80 now.
13. Push W3. Wait for the three Certificates to be Ready on production; if one is not re-issued within 5 min, delete its TLS Secret. `curl -fsS` (default CA validation) must succeed for the prod and nonprod health URLs, and the TLS handshake must succeed on the monitoring host. Record `T_end`.
14. Check the edge:
    - both public health endpoints are UP;
    - `/api/signin` on prod returns 401 for bad credentials — the route and the Middleware are attached;
    - the nonprod reset endpoint works against in-cluster Postgres (token via a header file);
    - on the prod and nonprod hosts, `http://<host>/` redirects to `https://<host>/`, while `http://<host>/.well-known/acme-challenge/<junk>` answers non-3xx. This is the live counterpart of I10: renewals keep reaching the solver.
15. Install `infra/vm/sshd/kanban-ci-tunnel.conf` to `/etc/ssh/sshd_config.d/`, apply any authorized_keys option change, `sshd -t`, `systemctl reload ssh`. Push W4. Its deploy.yml run must show flyway-verify and flyway-verify-nonprod green through the forward (which also proves kube-router admits node-originated traffic to postgres-0), and build a new tag that Flux rolls to both environments.
16. NetworkPolicy live proof (D-11): a throwaway `postgres:16` pod in `monitoring` without the client label fails `pg_isready -h postgres.kanban-data.svc.cluster.local -t 5`; the same pod in kanban-prod succeeds. Delete both.
17. Dispatch `uptime-check.yml` and watch it green.

**Runbook** `## Production cutover to k3s — Plan 13-06 (<date>)`, subsections:
1. Window timeline (T_start, T_end, downtime).
2. Broker drain evidence.
3. Dump inventory (paths, sizes, sha256).
4. Row-count parity (both diffs empty, counts table).
5. Certificate issuance (staging → production).
6. CI retarget (run IDs).
7. NetworkPolicy proof.
8. Compose state: stopped, volumes intact, deletion gated by D-08 in 13-10.

Delete the local `cutover-13-06` branch once main contains W1-W4.
  </action>
  <verify>
    <automated>
set -e
cd "$(git rev-parse --show-toplevel)"
git pull --rebase --quiet
for h in kanban-board-rud-vlad-473.duckdns.org kanban-board-rud-vlad-473-nonprod.duckdns.org; do curl -fsS "https://$h/api/actuator/health" | grep -q '"status":"UP"'; done
for h in kanban-board-rud-vlad-473.duckdns.org kanban-board-rud-vlad-473-nonprod.duckdns.org kanban-board-rud-vlad-473-monitoring.duckdns.org; do
  echo | openssl s_client -connect "$h:443" -servername "$h" 2>/dev/null | openssl x509 -noout -issuer | grep -v -i staging | grep -q "Let's Encrypt" || { echo "FAIL: $h not on a production LE cert"; exit 1; }
done
test "$(curl -s -o /dev/null -w '%{http_code}' -X POST -H 'Content-Type: application/json' -d '{"email":"probe@example.invalid","password":"NotAReal-Password1"}' https://kanban-board-rud-vlad-473.duckdns.org/api/signin)" = 401
for h in kanban-board-rud-vlad-473.duckdns.org kanban-board-rud-vlad-473-nonprod.duckdns.org; do
  case "$(curl -s -o /dev/null -w '%{http_code}' "http://$h/.well-known/acme-challenge/probe-not-a-token")" in 3*) echo "FAIL: $h redirects the ACME challenge path"; exit 1;; esac
  curl -s -o /dev/null -w '%{redirect_url}' "http://$h/" | grep -q "^https://$h/" || { echo "FAIL: $h http root does not redirect to https"; exit 1; }
done
ssh netcup-prod 'set -e
test -z "$(docker ps --format {{.Names}})"
D=$(ls -d /root/k3s-cutover-* | grep -v rehearsal | tail -1); cd "$D"; sha256sum -c SHA256SUMS >/dev/null
for db in kanban_prod kanban_nonprod; do diff "counts-$db-before.txt" "counts-$db-after.txt"; done
test "$(k3s kubectl get certificates -A -o jsonpath="{range .items[*]}{.spec.issuerRef.name}={.status.conditions[?(@.type==\"Ready\")].status}{\"\n\"}{end}" | sort -u)" = "letsencrypt-production=True"
test "$(k3s kubectl get certificates -A -o jsonpath="{range .items[*]}{.metadata.namespace}/{.metadata.name} {end}")" = "kanban-nonprod/app kanban-prod/app monitoring/grafana "
k3s kubectl get svc traefik -n kube-system -o jsonpath="{.spec.type} {.spec.externalTrafficPolicy}" | grep -qx "LoadBalancer Local"
for k in data cert-manager edge apps-prod apps-nonprod; do k3s kubectl get kustomization $k -n flux-system -o jsonpath="{.status.conditions[?(@.type==\"Ready\")].status}" | grep -qx True; done
echo REMOTE_OK'
python3 - <<'PY'
import sys, yaml
jobs = yaml.safe_load(open('.github/workflows/deploy.yml'))['jobs']
bad = [j for j in ('deploy-to-netcup', 'register-schemas-production', 'cleanup-unused-image') if j in jobs]
rb = open('docs/INFRA_RUNBOOK.md').read()
if bad or '## Production cutover to k3s — Plan 13-06' not in rb:
    print(f"FAIL: jobs {bad} / runbook section missing"); sys.exit(1)
PY
W4=$(git log -1 --format=%H -- infra/vm/sshd/kanban-ci-tunnel.conf)
RUN=$(gh run list --workflow deploy.yml --commit "$W4" --json databaseId --jq '.[0].databaseId'); gh run watch "$RUN" --exit-status
echo "TASK 3 PASS"
    </automated>
    <fails_when>Non-zero exit, `REMOTE_OK` absent, or a `FAIL:` line. Specifically:
- either public health endpoint is not UP, or any of the three hostnames serves a staging or non-LE certificate;
- prod `/api/signin` does not answer 401 to bad credentials;
- on the prod or nonprod host, `http://<host>/.well-known/acme-challenge/…` answers any 3xx, meaning the redirect shadows the HTTP-01 solver, or `http://<host>/` does not redirect to `https://<host>/`;
- any Docker container is still running;
- the dump checksums do not verify, or either row-count diff is non-empty;
- any Certificate is not Ready on letsencrypt-production;
- Traefik is not LoadBalancer with ETP Local;
- a Flux Kustomization is not Ready;
- a Compose deploy job still exists, or the runbook section is missing;
- W4's deploy.yml run (flyway-verify via the forward, image build) is not green.</fails_when>
  </verify>
  <acceptance_criteria>
    - The runbook records T_start, T_end and the downtime; the rpk drain output; dump sizes and sha256; both empty diffs plus the six-row count table per database; and the CI run IDs for W4.
    - `docker ps` on the VM is empty and `docker volume ls` still lists every Compose volume (nothing deleted before D-08).
    - The NetworkPolicy negative and positive probes are both recorded.
  </acceptance_criteria>
  <done>Production runs on k3s with verified data parity and production certificates; Compose is stopped but intact; CI no longer deploys over SSH.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → svclb/Traefik :80/:443 | The new public edge |
| CI runner → VM sshd → ClusterIP 10.43.54.32 | The new CI data path |
| Operator shell → dumps on disk | Full production data at rest outside Postgres |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-27 | Tampering | App starting against an empty DB and running Flyway before the restore | high | mitigate | W1 scales nonprod to 0 and leaves apps-prod suspended; W2 (apps start) is pushed only after the restore and a parity diff are both clean. |
| T-13-28 | Elevation of Privilege | CI SSH users forwarding anywhere | high | mitigate | sshd `Match User` block: `AllowTcpForwarding local`, `PermitOpen 10.43.54.32:5432` only, no agent/X11/tunnel; host key pinned via NETCUP_HOST_FINGERPRINT before connecting. |
| T-13-29 | Information Disclosure | Dump files | high | mitigate | 0700 directory under /root, no role passwords in globals.sql, sha256 recorded; retained per D-08.4 and disposed per the 13-10 decision. |
| T-13-30 | Denial of Service | Accidental Compose restart by an old workflow during the window | high | mitigate | W1-W3 touch only k8s/** (no deploy.yml run); W4 deletes the Compose jobs in the same commit that first starts deploy.yml; push freeze; no in-flight run at window start. |
| T-13-31 | Spoofing | Rate limit keyed on a proxy/LB address | high | mitigate | Traefik Service `externalTrafficPolicy: Local` via ServiceLB (no NAT in front); per-client proof in 13-08. |
| T-13-32 | Information Disclosure | Secret creation | medium | mitigate | Filtered, renamed 0600 env files, shredded; the prod app gets only DB_NAME/USER/PASS, never superuser, Grafana or nonprod keys. |
| T-13-33 | Elevation of Privilege | NetworkPolicy not enforced | medium | mitigate | Live negative probe from an unlabelled monitoring pod (step 16). |
</threat_model>

<verification>
1. Public prod/nonprod health UP on production LE certificates; the monitoring host handshakes with a production certificate.
2. Row-count parity empty diffs for both databases; dumps retained with valid checksums.
3. No Docker container running; Compose volumes intact.
4. CI: no Compose deploy jobs; flyway-verify jobs green through the SSH forward.
5. The Traefik `web` NodePort pin survives W1 and leaves in W2. This is asserted per staged commit, and W2 is pushed only behind the step-11 `docker ps` guard.
6. Prod and nonprod hosts redirect HTTP to HTTPS except `/.well-known/acme-challenge/`. This is gated by I10 at every staged commit and probed live after the window.
</verification>

<success_criteria>
- D-01/D-03/D-05/D-06 executed with evidence; the D-08.2 and D-08.4 inputs recorded; the rest of D-08 continues in 13-07..13-10.
</success_criteria>

<output>
Create `.planning/phases/13-introduce-kubernetes/13-06-SUMMARY.md` when done.
</output>
