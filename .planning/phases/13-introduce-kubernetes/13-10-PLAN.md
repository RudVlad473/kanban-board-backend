---
phase: 13-introduce-kubernetes
plan: 10
type: execute
wave: 8
depends_on: ["13-09"]
files_modified:
  - docker-compose.prod.yml
  - docker-compose.nonprod.yml
  - Caddyfile
  - docker/caddy/Dockerfile
  - docker/prometheus/prometheus.yml
  - docker/loki/loki-config.yaml
  - docker/promtail/promtail-config.yaml
  - docker/grafana/provisioning/datasources/datasources.yaml
  - docker/grafana/provisioning/dashboards/dashboards.yaml
  - docker/grafana/provisioning/dashboards/json/node-exporter-full.json
  - docker/grafana/provisioning/dashboards/json/cadvisor.json
  - docker/grafana/provisioning/dashboards/json/postgres-exporter.json
  - docker/postgres-init/01-create-databases-and-roles.sh
  - scripts/verify-compose-ports.py
  - scripts/verify-compose-ports-selftest.py
  - scripts/verify-deploy-scp-coverage.py
  - scripts/verify-deploy-scp-coverage-selftest.py
  - scripts/verify-caddy-image-tag.py
  - scripts/verify-postgres-memory-invariant.py
  - scripts/verify-postgres-init-quoting.sh
  - scripts/verify-k8s-invariants.py
  - scripts/verify-k8s-invariants-selftest.py
  - scripts/verify-public-dashboards.py
  - scripts/verify-public-dashboards-selftest.py
  - .github/workflows/invariant-checks.yml
  - .github/workflows/deploy.yml
  - .github/dependabot.yml
  - infra/vm/docker-user-firewall.sh
  - infra/vm/docker-user-firewall.service
  - infra/vm/README.md
  - docs/INFRA_RUNBOOK.md
  - docs/INFRA_ARCHITECTURE.md
  - .claude/CLAUDE.md
  - .planning/todos/pending/2026-09-08-cadvisor-grafana-and-caddy-mem-limits-need-a-longer-observation-window-re-ladder.md
  - .planning/todos/pending/2026-09-06-ipv6-published-ports-have-no-host-level-firewall.md
autonomous: false
requirements: [D-01, D-04, D-08, D-13, D-16]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Compose is deleted only after every D-08 item is PASS on evidence: health endpoints, uptime-check across the window, a fresh verify-rate-limit, verify-public-dashboards.py, a push-triggered GitOps deploy on prod, row-count parity, 24h (from T0) with zero restarts and zero OOM kills from kube-prometheus-stack and the kernel journal, and the dump present with valid checksums (D-04, D-08)."
    - "D-04's one-way deletion happens only after an explicit operator decision presented with that evidence (D-04)."
    - "After decommission no Compose container, volume, network or image remains; Docker Engine and its DOCKER-USER unit are stopped and disabled, and the k3s-era KANBAN-INGRESS filter alone keeps NodePorts closed, re-proven off-box (D-01, D-04)."
    - "The Caddy edge is gone entirely — Caddyfile, docker/caddy, the build-and-push-caddy-image job, verify-caddy-image-tag.py and its Dependabot entry (D-13)."
    - "Every Compose-only invariant script is deleted or ported, CI stays green, and both environments keep deploying from main through Flux (D-16)."
  artifacts:
    - "docs/INFRA_RUNBOOK.md sections `## D-08 gate — Plan 13-10 (<date>)` and `## Decommission Record — Plan 13-10 (<date>)`"
    - "Deleted Compose/Caddy/Promtail/old-Grafana artifacts and scripts (see files_modified)"
  key_links:
    - "runbook T0 (13-09) -> Prometheus queries evaluated at T0+24h"
    - "KANBAN-INGRESS (13-08) -> sole forward-path filter once docker-user-firewall.service is disabled"
---

<objective>
Evaluate the D-08 gate on evidence, ask the operator for the one-way D-04 decision, then remove Compose, Caddy and Docker from the VPS and the repository.

Purpose: D-04 has no rollback window, so the D-08 gate is the only safety margin. This plan must not weaken it.
Output: a gate record, a decommission record, and a repository and VPS with nothing Compose-shaped left.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-introduce-kubernetes/13-CONTEXT.md
@.planning/phases/13-introduce-kubernetes/13-06-SUMMARY.md
@.planning/phases/13-introduce-kubernetes/13-09-SUMMARY.md
@docs/INFRA_RUNBOOK.md
</context>

<artifacts_this_phase_produces>
| Kind | Name | Where |
|---|---|---|
| Runbook | `## D-08 gate — Plan 13-10`, `## Decommission Record — Plan 13-10` | docs/INFRA_RUNBOOK.md |
| Deletions | Compose manifests, Caddyfile, docker/*, Compose-only gate scripts, CI jobs, Dependabot caddy entry, docker-user-firewall unit | repo |
| VM state | Compose volumes/networks/images removed; docker, containerd and docker-user-firewall units disabled | netcup-prod |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Evaluate the D-08 gate on evidence and record it</name>
  <precondition>At least 24 hours have elapsed since T0, recorded in the runbook section `## k3s resource measurement — Plan 13-09`; halt otherwise.</precondition>
  <files>docs/INFRA_RUNBOOK.md</files>
  <read_first>
    - The 13-09 runbook section (T0, restart snapshot) and the 13-06 section (dump directory, counts files).
    - .github/workflows/uptime-check.yml (schedule), verify-rate-limit.yml.
  </read_first>
  <action>
Evaluate each item and record PASS/FAIL with its evidence. **Any FAIL stops the plan**: report it and do not present Task 2 as ready. Do not relax a threshold to make an item pass (D-04: the gate is the only safety margin).

**D-08.1 — automated checks:**
- (a) Both public health endpoints are UP now.
- (b) `gh run list --workflow uptime-check.yml --created ">=<T0 date>"`: every run in [T0, now] concluded success, and the count is ≥ 90 at the 15-min cadence. Any failure is a FAIL until explained by a GitHub-side outage shown in its own log.
- (c) `gh workflow run verify-rate-limit.yml`, then watch it: green.
- (d) `python3 scripts/verify-public-dashboards.py` passes.
- (e) GitOps: the prod pod's image tag equals ImagePolicy `kanban-board-backend-prod`'s latest; the tag reached the overlay through a fluxcdbot commit; the deploy.yml run that built it had event `push`; the pod is Ready.

**D-08.2 — parity:** re-run `diff` on the 13-06 counts files for both databases: empty.

**D-08.3 — 24h zero restarts and zero OOM kills**, via Prometheus (port-forward `kps-prometheus`) evaluated at `time=T0+24h`:
- `sum(increase(kube_pod_container_status_restarts_total[24h]))` = 0;
- `count(max_over_time(kube_pod_container_status_last_terminated_reason{reason="OOMKilled"}[24h]))` is empty.

Cross-check:
- the kernel journal: `journalctl -k --since "<T0>" | grep -c "Memory cgroup out of memory"` = 0. Use journalctl, not dmesg: dmesg timestamps are boot-relative.
- every current container's restartCount equals the T0 snapshot.

**D-08.4 — dump:** the directory exists and `sha256sum -c SHA256SUMS` passes.

Write `## D-08 gate — Plan 13-10 (<date>)`: a table (item | check | evidence | PASS/FAIL), the queries verbatim, and the evaluation timestamp.
  </action>
  <verify>
    <automated>
set -e
cd "$(git rev-parse --show-toplevel)"
python3 scripts/verify-public-dashboards.py
for u in https://kanban-board-rud-vlad-473.duckdns.org/api/actuator/health https://kanban-board-rud-vlad-473-nonprod.duckdns.org/api/actuator/health; do curl -fsS "$u" | grep -q '"status":"UP"'; done
python3 - <<'PY'
import re, sys
rb = open('docs/INFRA_RUNBOOK.md').read()
t0 = re.search(r'## k3s resource measurement — Plan 13-09.*?T0[^\n]*?(20\d\d-\d\d-\d\dT\d\d:\d\d(?::\d\d)?Z?)', rb, re.S)
sec = rb.split('## D-08 gate — Plan 13-10')[1].split('\n## ')[0] if '## D-08 gate — Plan 13-10' in rb else ''
fails = []
if not t0: fails.append("T0 not found in the 13-09 section")
if not sec: fails.append("D-08 gate section missing")
rows = re.findall(r'^\|\s*D-08\.(\d)[^|]*\|.*\|\s*(PASS|FAIL)\s*\|\s*$', sec, re.M)
if sorted({r[0] for r in rows}) != ['1', '2', '3', '4']: fails.append(f"gate rows cover {sorted({r[0] for r in rows})}")
if any(r[1] == 'FAIL' for r in rows): fails.append("a D-08 item is FAIL")
if 'increase(kube_pod_container_status_restarts_total[24h])' not in sec or 'journalctl -k' not in sec: fails.append("D-08.3 queries not recorded verbatim")
if fails:
    print("\n".join("FAIL: " + x for x in fails)); sys.exit(1)
print("GATE RECORD OK")
PY
ssh netcup-prod 'D=$(ls -d /root/k3s-cutover-* | grep -v rehearsal | tail -1); cd "$D" && sha256sum -c SHA256SUMS >/dev/null && for db in kanban_prod kanban_nonprod; do diff -q counts-$db-before.txt counts-$db-after.txt; done && echo DUMP_OK'
RUN=$(gh run list --workflow verify-rate-limit.yml --limit 1 --json databaseId --jq '.[0].databaseId'); gh run watch "$RUN" --exit-status
echo "TASK 1 PASS"
    </automated>
    <fails_when>Non-zero exit, `DUMP_OK` or `GATE RECORD OK` absent, or a `FAIL:` line. Specifically:
- the public-dashboard gate fails, or a health endpoint is not UP;
- T0 cannot be found;
- the gate section is missing, lacks a row for any of D-08.1–D-08.4, records any FAIL, or omits the verbatim restart/journal queries;
- the dump checksums fail or the counts differ;
- the latest verify-rate-limit run is not green.</fails_when>
  </verify>
  <acceptance_criteria>
    - All four D-08 items are PASS with evidence, and the evaluation time is ≥ T0 + 24h.
    - The uptime-check row states the run count and that every one succeeded.
    - The GitOps row names the build run ID (event push), the fluxcdbot commit SHA and the running pod image.
  </acceptance_criteria>
  <done>The D-08 gate is evaluated and recorded; Task 2 may only proceed on all-PASS.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: D-04 — delete Compose (volumes included), retire Docker Engine, and choose the dump's fate</name>
  <reversibility rating="one-way">D-04: Docker volumes postgres-data, redpanda-data, caddy-data, caddy-config, prometheus-data, grafana-data, loki-data and redpanda-nonprod-data are deleted. Returning means rebuilding Compose from git history and restoring from a dump.</reversibility>
  <read_first>
    - docs/INFRA_RUNBOOK.md `## D-08 gate — Plan 13-10` (Task 1's record).
  </read_first>
  <decision>Decommission Compose now, and decide what happens to the cutover dump.</decision>
  <context>
Present Task 1's gate table verbatim, plus `docker volume ls` and `du -sh` of the dump directory.

Stopping Docker retires the DOCKER-USER layer. 13-08's KANBAN-INGRESS already filters the forward path and will be re-proven off-box after Docker stops.

A reboot, if chosen, proves the boot-time posture: docker disabled, the k3s host firewall enabled, k3s up. It costs about 2 min of public downtime.

No automated backups exist (deferred). The dump is the only pre-k3s copy of the data.
  </context>
  <options>
    <option id="decommission-reboot-keep-dump">
      <name>Decommission, reboot-prove, keep the dump (recommended)</name>
      <pros>Boot-time posture proven; the only offline copy of pre-cutover data retained (~30 MB in /root/k3s-cutover-archive, 0600).</pros>
      <cons>~2 min downtime for the reboot.</cons>
    </option>
    <option id="decommission-keep-dump">
      <name>Decommission without reboot, keep the dump</name>
      <pros>No further downtime.</pros>
      <cons>Boot-time firewall/unit state unproven until the next unplanned reboot.</cons>
    </option>
    <option id="decommission-delete-dump">
      <name>Decommission and delete the dump (D-08.4 satisfied)</name>
      <pros>No stale data copy on disk.</pros>
      <cons>No offline copy of pre-cutover data remains anywhere.</cons>
    </option>
    <option id="extend">
      <name>Extend observation by N hours</name>
      <pros>More evidence before a one-way step.</pros>
      <cons>Compose volumes occupy disk longer; the phase stays open.</cons>
    </option>
  </options>
  <resume-signal>Reply with one option id (and N for extend).</resume-signal>
  <acceptance_criteria>
    - The reply is recorded verbatim in the SUMMARY and the Decommission Record.
    - Task 3 runs only on a decommission-* reply.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Decommission on the VM and in the repo; re-prove the edge without Docker; record it</name>
  <reversibility rating="one-way">D-04 — executed only after Task 2's approval.</reversibility>
  <files>docker-compose.prod.yml, docker-compose.nonprod.yml, Caddyfile, docker/caddy/, docker/prometheus/, docker/loki/, docker/promtail/, docker/grafana/, docker/postgres-init/, scripts/verify-compose-ports*.py, scripts/verify-deploy-scp-coverage*.py, scripts/verify-caddy-image-tag.py, scripts/verify-postgres-memory-invariant.py, scripts/verify-postgres-init-quoting.sh, scripts/verify-k8s-invariants*.py, scripts/verify-public-dashboards*.py, .github/workflows/invariant-checks.yml, .github/workflows/deploy.yml, .github/dependabot.yml, infra/vm/docker-user-firewall.*, infra/vm/README.md, docs/INFRA_RUNBOOK.md, docs/INFRA_ARCHITECTURE.md, .claude/CLAUDE.md, .planning/todos/</files>
  <read_first>
    - docs/INFRA_RUNBOOK.md "Decommission Record — Plan 11-06" (the record shape to mirror).
    - .github/workflows/invariant-checks.yml, deploy.yml (Caddy job, `DOCKERHUB_REPOSITORY_CADDY`, setup output `base_image_name_caddy`), .github/dependabot.yml (the `/docker/caddy` entry).
    - scripts/verify-postgres-init-quoting.sh usage; scripts/verify-k8s-invariants.py (I9), scripts/verify-public-dashboards.py (Compose scope).
    - .claude/CLAUDE.md "Platform Requirements" and "Key Dependencies" bullets naming Compose/Caddy/cAdvisor-era components.
  </read_first>
  <action>
**VM, over `ssh netcup-prod`:**
1. `docker compose -p kanban-board-backend -f /opt/deploy/kanban-board-backend/docker-compose.prod.yml down -v --remove-orphans`, and the same for `kanban-board-nonprod`.
2. `docker network rm kanban-edge kanban-db kanban-metrics`; `docker image prune -af`. `docker volume ls` and `docker ps -a` must be empty.
3. In `/opt/deploy/kanban-board-backend` and `/opt/deploy/kanban-board-nonprod`, delete everything except `.env.prod` / `.env.nonprod`. They stay 0600 as the Secret source of truth (D-10: rotation stays a manual SSH step). Delete `ci-flyway-verify/`.
4. `systemctl disable --now docker-user-firewall.service docker.service docker.socket containerd.service`. Docker's containerd, not k3s's embedded one. Leave the packages installed; this is reversible.
5. Re-prove the edge:
   - all pods Ready;
   - both health endpoints UP;
   - `iptables -t mangle -S PREROUTING` jump to KANBAN-INGRESS still first;
   - off-box `nc` to the Traefik NodePort fails, with the KANBAN-INGRESS DROP counter increasing;
   - 22/443 reachable.
6. If Task 2 chose a reboot variant: `systemctl reboot`, then repeat step 5, and confirm `systemctl is-enabled docker` = disabled and `k3s-host-firewall` = enabled.
7. Dump per Task 2: move it to `/root/k3s-cutover-archive/` (0700, files 0600) or `shred -u` it.

**Repo, one commit:**
- Delete every file under `files_modified` that belongs to the Compose/Caddy era: both prod/nonprod compose files, Caddyfile, `docker/caddy/`, `docker/prometheus/`, `docker/loki/`, `docker/promtail/`, `docker/grafana/`, `docker/postgres-init/`, the compose-ports / deploy-scp-coverage / caddy-image-tag / postgres-memory-invariant scripts with their selftests, `infra/vm/docker-user-firewall.*`. Local-dev `docker-compose.yml` stays.
- invariant-checks.yml: remove jobs `caddy-image-tag`, `compose-published-ports`, `deploy-scp-coverage`.
- deploy.yml: remove `build-and-push-caddy-image`, `DOCKERHUB_REPOSITORY_CADDY` and the `base_image_name_caddy` output. Adjust the header comments that describe Compose deploys.
- dependabot.yml: remove the `/docker/caddy` docker entry and its comment block.
- verify-k8s-invariants.py (+ selftest): drop I9, keeping I1-I8 numbering stable. Note in the docstring that I6 superseded verify-postgres-memory-invariant.py.
- verify-public-dashboards.py (+ selftest): drop the Compose scope.
- verify-postgres-init-quoting.sh: default/usage path → `k8s/data/postgres/init`.
- infra/vm/README.md: drop the DOCKER-USER install section and describe the k3s files.
- `.claude/CLAUDE.md`: invoke the `writing-for-agents` skill first (standing tooling preference). Then update only the bullets that name Compose/Caddy as the runtime or edge (deployment target, reverse proxy, Postgres "one container", "Docker Compose - Container deployment") to k3s/Flux/Traefik/cert-manager. No other restructuring.
- Todos:
  - move `2026-09-08-cadvisor-grafana-and-caddy-mem-limits…` to completed, with a Resolution: containers removed, successors measured in 13-09;
  - for `2026-09-06-ipv6-published-ports…`: run `ss -6 -ltnH` on the VM. If nothing listens publicly except sshd, move it to completed with that evidence; otherwise leave it pending with a dated note.
- `.env.prod.example` / `.env.nonprod.example` are not edited. Their keys remain the Secret inventory. Record in the Decommission Record that `APP_DOMAIN*` keys are now unused.

**Runbook** `## Decommission Record — Plan 13-10 (<date>)`, mirroring 11-06:
- gate verified (link to the D-08 section);
- decision (verbatim);
- Part A — what was deleted, VM and repo;
- Part B — credentials: none revoked; NETCUP_SSH_KEY is still used by flyway-verify; the Docker Hub `kanban-board-caddy` repo is no longer published to, its tags left in place;
- Part C — what was verified unaffected (step 5/6 evidence);
- Part D — what is not recoverable;
- the date.

Update `## Maintenance note` and `docs/INFRA_ARCHITECTURE.md`: turn the "removed-in-13-10" markers from 13-09 into removals.
  </action>
  <verify>
    <automated>
set -e
cd "$(git rev-parse --show-toplevel)"
for f in docker-compose.prod.yml docker-compose.nonprod.yml Caddyfile docker/caddy docker/prometheus docker/loki docker/promtail docker/grafana docker/postgres-init scripts/verify-compose-ports.py scripts/verify-deploy-scp-coverage.py scripts/verify-caddy-image-tag.py scripts/verify-postgres-memory-invariant.py infra/vm/docker-user-firewall.sh; do
  test ! -e "$f" || { echo "FAIL: $f still exists"; exit 1; }
done
test -f docker-compose.yml
python3 scripts/verify-k8s-invariants-selftest.py && python3 scripts/verify-k8s-invariants.py --no-provisional
python3 scripts/verify-public-dashboards-selftest.py && python3 scripts/verify-public-dashboards.py
bash scripts/verify-k8s-manifests.sh
python3 - <<'PY'
import sys, yaml
fails = []
inv = yaml.safe_load(open('.github/workflows/invariant-checks.yml'))['jobs']
for j in ('caddy-image-tag', 'compose-published-ports', 'deploy-scp-coverage'):
    if j in inv: fails.append(f"invariant job {j} remains")
wf = yaml.safe_load(open('.github/workflows/deploy.yml'))
if 'build-and-push-caddy-image' in wf['jobs'] or 'DOCKERHUB_REPOSITORY_CADDY' in (wf.get('env') or {}): fails.append("caddy CI remains")
if any(u.get('directory') == '/docker/caddy' for u in yaml.safe_load(open('.github/dependabot.yml'))['updates']): fails.append("dependabot caddy entry remains")
rb = open('docs/INFRA_RUNBOOK.md').read()
if '## Decommission Record — Plan 13-10' not in rb: fails.append("decommission record missing")
if fails:
    print("\n".join("FAIL: " + x for x in fails)); sys.exit(1)
PY
ssh netcup-prod 'set -e
test "$(systemctl is-enabled docker.service 2>/dev/null || true)" != enabled
test "$(systemctl is-active docker.service 2>/dev/null || true)" != active
systemctl is-enabled k3s-host-firewall >/dev/null
/usr/local/sbin/k3s-host-firewall.sh check
test -z "$(ls -A /var/lib/docker/volumes 2>/dev/null | grep -v -e "^backingFsBlockDev$" -e "^metadata.db$")"
k3s kubectl get pods -A --no-headers | awk "\$4 !~ /Running|Completed/ {print; bad=1} END {exit bad}"
echo REMOTE_OK'
PUB=$(dig +short kanban-board-rud-vlad-473.duckdns.org | head -1)
NP=$(ssh netcup-prod "k3s kubectl get svc traefik -n kube-system -o jsonpath='{.spec.ports[?(@.name==\"websecure\")].nodePort}'")
if nc -z -w 5 "$PUB" "$NP"; then echo "FAIL: NodePort reachable after Docker stop"; exit 1; fi
for u in https://kanban-board-rud-vlad-473.duckdns.org/api/actuator/health https://kanban-board-rud-vlad-473-nonprod.duckdns.org/api/actuator/health; do curl -fsS "$u" | grep -q '"status":"UP"'; done
RUN=$(gh run list --workflow invariant-checks.yml --branch main --limit 1 --json databaseId --jq '.[0].databaseId'); gh run watch "$RUN" --exit-status
RUN=$(gh run list --workflow deploy.yml --branch main --limit 1 --json databaseId --jq '.[0].databaseId'); gh run watch "$RUN" --exit-status
echo "TASK 3 PASS"
    </automated>
    <fails_when>Non-zero exit, `REMOTE_OK` absent, or a `FAIL:` line. Specifically:
- a Compose/Caddy-era file or directory still exists, or local-dev docker-compose.yml was deleted;
- any gate or selftest fails;
- a removed CI job, the Caddy env or the Dependabot entry remains;
- the decommission record is missing;
- docker is still enabled or active, the k3s host firewall is not enabled, or its check fails;
- any Docker volume remains;
- a pod is not Running/Completed;
- the Traefik NodePort is reachable off-box;
- a health endpoint is not UP;
- the latest invariant-checks or deploy.yml run on main is not green.</fails_when>
  </verify>
  <acceptance_criteria>
    - The Decommission Record names every deleted volume, network and repo path, and the dump's final location or its shredding.
    - After a reboot (if chosen), the same REMOTE_OK checks pass, with the uptime recorded.
    - `.claude/CLAUDE.md` no longer describes Docker Compose or Caddy as the production runtime/edge; the SUMMARY notes that `writing-for-agents` was invoked first.
  </acceptance_criteria>
  <done>Compose, Caddy and Docker are gone from production and the repo; k3s is the only runtime, and the edge is re-proven without Docker's firewall chain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator decision → irreversible deletion | D-04 |
| Internet → host after Docker's chains are gone | The forward path is now guarded only by KANBAN-INGRESS + Layer 1/2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-48 | Denial of Service | Irreversible data loss if k3s data is wrong | high | mitigate | D-08 gate on evidence with no threshold relaxation (Task 1 halts on any FAIL); explicit one-way checkpoint; dump retained by default. |
| T-13-49 | Information Disclosure | NodePorts exposed when DOCKER-USER disappears | high | mitigate | KANBAN-INGRESS re-proven off-box after Docker stops (and after a reboot if chosen), with counter attribution. |
| T-13-50 | Information Disclosure | Retained dump of production data | medium | mitigate | /root/k3s-cutover-archive 0700, files 0600, no role passwords (globals dumped with --no-role-passwords); or shredded, per operator choice. |
| T-13-51 | Tampering | Deleted gates silently leaving invariants unenforced | medium | mitigate | Each deleted gate is either obsolete (its subject deleted) or ported (I1/I2 exposure, I6 Postgres memory); CI green after deletion. |
| T-13-52 | Repudiation | Undocumented removal | low | mitigate | Decommission Record in the 11-06 shape; Maintenance note and INFRA_ARCHITECTURE updated in the same commit. |
</threat_model>

<verification>
1. The D-08 gate is all-PASS on recorded evidence at ≥ T0+24h.
2. The operator approved D-04 explicitly.
3. The VM has no Compose state; Docker is disabled; the edge is re-proven; CI is green.
</verification>

<success_criteria>
- The phase goal holds: both environments on k3s via Flux, Compose fully removed behind a passed D-08 gate.
</success_criteria>

<output>
Create `.planning/phases/13-introduce-kubernetes/13-10-SUMMARY.md` when done.
</output>
