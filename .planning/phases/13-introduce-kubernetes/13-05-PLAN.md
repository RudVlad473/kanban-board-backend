---
phase: 13-introduce-kubernetes
plan: 05
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - k8s/flux-system/apps-nonprod.yaml
  - Caddyfile
  - .github/workflows/deploy.yml
  - docs/INFRA_RUNBOOK.md
  - docs/INFRA_ARCHITECTURE.md
  - README.md
autonomous: false
requirements: [D-02, D-06, D-10, D-14, D-15, D-16, D-17]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Nonprod serves its public hostname from k3s (Caddy → Traefik NodePort 30080 → app pod), with the old Compose broker stopped only after activity-log lag was 0 and the DLT empty (D-02, D-06)."
    - "At least one full GitOps cycle completed on nonprod: push → CI builds main-N-sha7 → Flux commits the bump (no deploy.yml run for it) → new pod Ready with tag N → public health UP (D-02, D-15, D-16)."
    - "No SSH/Compose deploy path remains for nonprod: deploy-to-nonprod, health-check-nonprod and cleanup-unused-image-nonprod are deleted, and nonprod image pruning keeps the newest five sortable tags independent of any deploy job (D-14, D-15)."
    - "Interim health evidence is kubectl-based — restart counts, OOMKilling events, last-state — because no in-cluster Prometheus exists until 13-07 (D-17)."
    - "The interim memory steady state is measured on the box and the projected final budget is recomputed from measured figures before any production-touching plan starts (D-02, A1)."
  artifacts:
    - "docs/INFRA_RUNBOOK.md section `## Nonprod on k3s — Plan 13-05 (<date>)`"
    - "Caddyfile nonprod block → 172.17.0.1:30080; deploy.yml without nonprod SSH jobs"
  key_links:
    - "Caddy {$APP_DOMAIN_NONPROD} reverse_proxy 172.17.0.1:30080 -> kube-proxy NodePort -> Traefik web -> IngressRoute kanban-nonprod/app"
    - "ImagePolicy kanban-board-backend-nonprod -> setter marker in k8s/overlays/nonprod/kustomization.yaml -> Flux apps-nonprod reconcile"
---

<objective>
Move nonprod from Compose to k3s, prove one full GitOps deploy cycle end to end, and measure the interim before production is touched.

Purpose: D-02's gate. Everything 13-06 relies on — image automation, the initContainer, the bridge, memory headroom — is exercised by real traffic here first.
Output: nonprod on k3s behind the existing Caddy, nonprod SSH deploy jobs gone, and a measured interim budget.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-introduce-kubernetes/13-CONTEXT.md
@.planning/phases/13-introduce-kubernetes/13-01-PLAN.md
@.planning/phases/13-introduce-kubernetes/13-02-SUMMARY.md
@Caddyfile
@.github/workflows/deploy.yml
</context>

<artifacts_this_phase_produces>
| Kind | Name | Where |
|---|---|---|
| Flux | apps-nonprod unsuspended | k8s/flux-system/apps-nonprod.yaml |
| Edge config | nonprod upstream → `172.17.0.1:30080` | Caddyfile |
| CI change | nonprod SSH deploy/health/cleanup-on-failure jobs removed; `cleanup-old-images-nonprod` keeps 5 newest | .github/workflows/deploy.yml |
| Runbook | `## Nonprod on k3s — Plan 13-05` | docs/INFRA_RUNBOOK.md |
| Docs | dated interim-topology notes | README.md, docs/INFRA_ARCHITECTURE.md |
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Authorize the nonprod cutover (go/no-go on 13-02's A1/A2 evidence)</name>
  <read_first>
    - .planning/phases/13-introduce-kubernetes/13-02-SUMMARY.md — the A2 proof and measured baseline.
    - docs/INFRA_RUNBOOK.md `## k3s install and interim bridge — Plan 13-02`.
  </read_first>
  <decision>Cut nonprod over to k3s now.</decision>
  <context>
The message must include:
- current MemAvailable;
- 13-02's go/no-go result;
- the image tag the nonprod overlay currently names (Flux-bumped), and whether that tag still exists on Docker Hub.

Expected nonprod downtime is 5–15 min, from stopping Compose app-nonprod until Caddy points at Traefik. One scheduled uptime-check run will likely go red.

Rollback until 13-06 is cheap: the Compose nonprod containers are stopped, not removed. Restore the Caddyfile upstream and deploy.yml jobs by `git revert`, then `docker compose start`.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed</name>
      <pros>Satisfies D-02's precondition for production.</pros>
      <cons>Nonprod is briefly unavailable; e2e users of nonprod see 502s during the gap.</cons>
    </option>
    <option id="postpone">
      <name>Postpone</name>
      <pros>No change.</pros>
      <cons>Production stays blocked behind D-02.</cons>
    </option>
  </options>
  <resume-signal>Reply proceed or postpone.</resume-signal>
  <acceptance_criteria>
    - The reply and the readings above are recorded in the SUMMARY.
  </acceptance_criteria>
</task>

<task type="tracer">
  <name>Task 2: Tracer — stop Compose nonprod in D-06 order, activate apps-nonprod, and serve the public nonprod hostname through Caddy → Traefik → pod</name>
  <reversibility rating="costly">D-14/D-15: removes the nonprod SSH deploy path; returning needs the jobs restored from git history.</reversibility>
  <files>k8s/flux-system/apps-nonprod.yaml, Caddyfile, .github/workflows/deploy.yml</files>
  <precondition>Task 1 answered proceed; Secret kanban-nonprod/app-env exists (created in 13-02).</precondition>
  <read_first>
    - .github/workflows/deploy.yml — deploy-to-nonprod, health-check-nonprod, cleanup-old-images-nonprod (JWT login + pagination code to reuse), cleanup-unused-image-nonprod.
    - Caddyfile `{$APP_DOMAIN_NONPROD}` block.
    - docs/SESSION_LESSONS.md lessons 1-2 (push discipline).
  </read_first>
  <action>
**Sequence (D-06), over `ssh netcup-prod`:**
1. `docker compose -p kanban-board-nonprod stop app-nonprod`.
2. Check the old broker is drained:
   - `docker exec kanban-nonprod-redpanda rpk group describe activity-log`: every partition's LAG must be 0.
   - `docker exec kanban-nonprod-redpanda rpk topic describe kanban.activity.dlt -p`: every partition's HIGH-WATERMARK must equal its LOG-START-OFFSET.

   If either fails, stop and report; do not proceed with data still pending.
3. `docker compose -p kanban-board-nonprod stop redpanda-nonprod`. Stop, do not `down`: volumes stay until 13-10 (D-04).

**Commit A (k8s only, no deploy.yml run):** set `apps-nonprod.yaml` `suspend: false`. `git pull --rebase`, push, then force a reconcile via the `reconcile.fluxcd.io/requestedAt` annotation. Wait for:
- `redpanda-0` Ready;
- the app pod's init `register-schemas` to finish with exit code 0 (its logs show the registered subjects);
- `app` Ready.

From the VM, `curl -sf -H 'Host: kanban-board-rud-vlad-473-nonprod.duckdns.org' http://127.0.0.1:30080/api/actuator/health` must contain `"status":"UP"`.

**Commit B:**
- Caddyfile nonprod block: `reverse_proxy 172.17.0.1:30080`, with a dated comment: interim until 13-06; 172.17.0.1 is the docker0 gateway, where kube-proxy answers NodePorts.
- deploy.yml: delete jobs `deploy-to-nonprod`, `health-check-nonprod` and `cleanup-unused-image-nonprod`.
- Rewrite `cleanup-old-images-nonprod`:
  - `needs: [setup, build-and-push-docker-image]`;
  - reuse the JWT login, pagination and HTTP-status checks;
  - keep the five newest tags matching `^main-[0-9]+-[0-9a-f]{7}$`, ordered by the numeric run number;
  - delete every other tag, legacy bare-SHA tags included.

  Comment why: Flux needs recent tags to roll back via `git revert`, and CI no longer knows when a deploy happened.
- flyway-verify-nonprod stays unchanged: nonprod data is still in Compose Postgres until 13-06.

`git pull --rebase`, push. That deploy.yml run's deploy-to-netcup reloads Caddy and re-checks its rate_limit handler; it must be green.

**Commit C — only if the tracer fails at the public hop:** if public nonprod health is not UP within 5 min of Caddy's reload, capture Caddy's logs and `iptables -t nat -L KUBE-NODEPORTS -n -v` counters, and revert commit B.
  </action>
  <verify>
    <automated>
set -e
cd "$(git rev-parse --show-toplevel)"
python3 - <<'PY'
import sys, yaml
fails = []
if yaml.safe_load(open('k8s/flux-system/apps-nonprod.yaml'))['spec'].get('suspend') not in (False, None): fails.append("apps-nonprod still suspended")
jobs = yaml.safe_load(open('.github/workflows/deploy.yml'))['jobs']
for gone in ('deploy-to-nonprod', 'health-check-nonprod', 'cleanup-unused-image-nonprod'):
    if gone in jobs: fails.append(f"{gone} still present")
c = jobs.get('cleanup-old-images-nonprod', {})
if sorted(c.get('needs', [])) != ['build-and-push-docker-image', 'setup']: fails.append(f"cleanup-old-images-nonprod needs {c.get('needs')}")
run = ' '.join(str(s.get('run', '')) for s in c.get('steps', []))
if 'main-[0-9]+-[0-9a-f]{7}' not in run: fails.append("cleanup does not filter on the sortable tag pattern")
blk = open('Caddyfile').read().split('{$APP_DOMAIN_NONPROD}')[1].split('}')[0]
if '172.17.0.1:30080' not in blk: fails.append("Caddy nonprod upstream not 172.17.0.1:30080")
if fails:
    print("\n".join("FAIL: " + x for x in fails)); sys.exit(1)
PY
ssh netcup-prod 'set -e
for c in kanban-nonprod-app kanban-nonprod-redpanda; do test "$(docker inspect -f {{.State.Running}} $c)" = false; done
k3s kubectl get kustomization apps-nonprod -n flux-system -o jsonpath="{.status.conditions[?(@.type==\"Ready\")].status}" | grep -qx True
k3s kubectl rollout status deploy/app -n kanban-nonprod --timeout=300s
k3s kubectl get pod -n kanban-nonprod -l app.kubernetes.io/name=app -o jsonpath="{.items[0].status.initContainerStatuses[0].state.terminated.exitCode}" | grep -qx 0
echo REMOTE_OK'
curl -fsS https://kanban-board-rud-vlad-473-nonprod.duckdns.org/api/actuator/health | grep -q '"status":"UP"'
RUN=$(gh run list --workflow deploy.yml --branch main --limit 1 --json databaseId --jq '.[0].databaseId'); gh run watch "$RUN" --exit-status
echo "TASK 2 PASS"
    </automated>
    <fails_when>Non-zero exit or any `FAIL:` line. Specifically:
- apps-nonprod is still suspended, or any of the three removed jobs is still present;
- the nonprod cleanup still depends on a deploy job or does not filter on the sortable pattern;
- Caddy's nonprod upstream is not 172.17.0.1:30080;
- either Compose nonprod container is still running;
- the Flux Kustomization is not Ready, the rollout times out, or register-schemas exited non-zero;
- public nonprod health is not UP;
- the latest deploy.yml run on main is not green.</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY holds the verbatim `rpk group describe activity-log` (all LAG 0) and DLT partition offsets captured before the broker stopped.
    - The register-schemas init log lists the registered subjects against `http://redpanda:8081`.
    - The nonprod reset endpoint works against the bridged Compose DB: `POST /api/admin/reset?fullReset=true`, with the token passed through a 0600 header file (`curl -H @file`), never on argv, returns the documented success status.
  </acceptance_criteria>
  <done>Nonprod runs on k3s behind the unchanged Caddy edge; no SSH deploy path remains for it.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the full GitOps cycle (D-02), collect kubectl-only health evidence (D-17), measure the interim, and record it</name>
  <files>docs/INFRA_RUNBOOK.md, docs/INFRA_ARCHITECTURE.md, README.md</files>
  <read_first>
    - docs/INFRA_RUNBOOK.md `## k3s install and interim bridge — Plan 13-02` (baseline table to extend).
    - 13-RESEARCH.md "Q8 — Memory Budget" (ASSUMED rows to replace with measurements).
    - README.md "Production deployment" and "CI/CD pipeline & deploy strategy"; docs/INFRA_ARCHITECTURE.md "Maintenance Note".
  </read_first>
  <action>
**Full GitOps cycle (D-02):**
- Commit B built tag `main-N-<sha7>`. Wait for Flux's `chore(flux): bump images` commit setting the nonprod newTag to N, then `git pull --rebase`.
- Confirm `gh run list --workflow deploy.yml --commit <flux-sha>` is empty.
- Confirm the running pod's image ends in `:main-N-<sha7>`, it is Ready, and public health is UP.
- That is one complete push → build → Flux commit → reconcile → Ready cycle; record every SHA and timestamp.

**kubectl health evidence (D-17)**, at least 30 min after the rollout:
- `k3s kubectl get pods -A`: restartCount 0 for every kanban-nonprod pod.
- `k3s kubectl get events -A --field-selector reason=OOMKilling`: empty.
- `k3s kubectl describe pod` for app and redpanda-0: no lastState termination.
- `dmesg` shows no `Memory cgroup out of memory` since 13-02.

**Interim memory (A1):**
- MemAvailable.
- `memory.peak` per pod cgroup for app, redpanda and register-schemas (the init peak).
- k3s and Flux figures from 13-02 re-read.

Recompute the projected final steady-state budget: replace research's ASSUMED platform rows with measured ones; the observability rows keep the Phase 12-measured values plus research ranges for new components, labelled as such.

Go/no-go for 13-06: the projected final `requests` sum must be ≤ MemAvailable-at-full-Compose-stop minus 512 MiB margin. State the arithmetic. If it fails, stop and report; that input belongs to the D-01 decision.

**Docs:**
- Runbook section `## Nonprod on k3s — Plan 13-05 (<date>)`, subsections:
  1. Cutover sequence and rpk evidence.
  2. GitOps cycle proof (SHAs, timings).
  3. kubectl health evidence.
  4. Interim memory table plus projected final budget and verdict.
  5. Rollback (Compose nonprod stopped, not removed).
- README "Production deployment" and docs/INFRA_ARCHITECTURE.md: add a dated interim note — nonprod runs on k3s behind Caddy since <date>; prod stays on Compose until 13-06 — linking the runbook section. The full diagram redraw happens in 13-09. In README's CI/CD section, replace the nonprod SSH-deploy description with the Flux image-automation path.
  </action>
  <verify>
    <automated>
set -e
cd "$(git rev-parse --show-toplevel)"
git pull --rebase --quiet
BUMP=$(git log --author=fluxcdbot --format=%H -1 -- k8s/overlays/nonprod/kustomization.yaml)
test -n "$BUMP"
test -z "$(gh run list --workflow deploy.yml --commit "$BUMP" --json databaseId --jq '.[].databaseId')" || { echo "FAIL: deploy.yml ran for Flux bump"; exit 1; }
OVERLAY=$(git show "$BUMP":k8s/overlays/nonprod/kustomization.yaml)
TAG=$(printf '%s' "$OVERLAY" | python3 -c "import sys,yaml;print([i for i in yaml.safe_load(sys.stdin)['images'] if i['name']=='rudenkovladimir/kanban-board-backend'][0]['newTag'])")
test -n "$TAG"
ssh netcup-prod "k3s kubectl get pod -n kanban-nonprod -l app.kubernetes.io/name=app -o jsonpath='{.items[0].spec.containers[0].image} {.items[0].status.containerStatuses[0].ready} {.items[0].status.containerStatuses[0].restartCount}'" | grep -qx "rudenkovladimir/kanban-board-backend-nonprod:$TAG true 0"
test -z "$(ssh netcup-prod 'k3s kubectl get events -A --field-selector reason=OOMKilling -o name')"
curl -fsS https://kanban-board-rud-vlad-473-nonprod.duckdns.org/api/actuator/health | grep -q '"status":"UP"'
python3 - <<'PY'
import re, sys
rb = open('docs/INFRA_RUNBOOK.md').read()
sec = rb.split('## Nonprod on k3s — Plan 13-05')[1].split('\n## ')[0] if '## Nonprod on k3s — Plan 13-05' in rb else ''
fails = [] if sec else ["runbook section missing"]
for h in ('Cutover sequence', 'GitOps cycle', 'kubectl health', 'memory', 'Rollback'):
    if sec and h.lower() not in sec.lower(): fails.append(f"section lacks {h}")
if sec and re.search(r'[0-9a-f]{32,}', sec): fails.append("32+ hex chars in section")
for f in ('README.md', 'docs/INFRA_ARCHITECTURE.md'):
    if 'Plan 13-05' not in open(f).read(): fails.append(f"{f} lacks the dated interim note")
if fails:
    print("\n".join("FAIL: " + x for x in fails)); sys.exit(1)
print("TASK 3 PASS")
PY
    </automated>
    <fails_when>Non-zero exit or any `FAIL:` line. Specifically:
- no Flux bump commit touched the nonprod overlay, or deploy.yml ran for it;
- the running nonprod pod's image is not the bumped tag, is not ready, or has restarted;
- any OOMKilling event exists;
- public nonprod health is not UP;
- the runbook section or one of its five subsections is missing, or it contains a 32+ character hex string;
- README or INFRA_ARCHITECTURE lacks the dated interim note.</fails_when>
  </verify>
  <acceptance_criteria>
    - The runbook states the projected final `requests` sum, the margin arithmetic and a PASS/FAIL verdict; every platform figure is MEASURED with its source command.
    - The D-02 cycle is evidenced with four SHAs and timestamps: trigger push, build run, Flux commit, running pod image.
  </acceptance_criteria>
  <done>D-02 is satisfied with evidence, and 13-06's D-01 decision has measured inputs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caddy container → host NodePort 30080 | New internal hop from the Compose edge into k3s |
| Flux → main | Flux now deploys nonprod on every image |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-22 | Information Disclosure | NodePort 30080 reachable from the internet | high | mitigate | DOCKER-USER drops eth0 → anything but 80/443 (verified in 13-02 via `docker-user-firewall.sh check`); Caddy reaches it via 172.17.0.1 (not an internet-routable address). |
| T-13-23 | Tampering | Lost activity events at broker swap | medium | mitigate | D-06 order enforced: app stopped → lag 0 + DLT empty checked and captured → broker stopped; the task halts on non-zero lag. |
| T-13-24 | Repudiation | Deploys no longer visible as CI jobs | low | mitigate | Every deploy is a fluxcdbot commit on main; the runbook records the SHA chain. |
| T-13-25 | Information Disclosure | Reset token on argv | medium | mitigate | Header file `curl -H @file` (0600, removed after), never argv/history. |
| T-13-26 | Denial of Service | Tag pruning deleting a tag Flux still references | medium | mitigate | Keep the 5 newest sortable tags, not "all but current"; no dependency on deploy timing. |
</threat_model>

<verification>
1. Public nonprod health is UP through Caddy → Traefik → pod; Compose nonprod containers are stopped.
2. One full GitOps cycle proven, with no CI loop.
3. Zero restarts, zero OOM events (kubectl); the interim budget is measured and the go/no-go recorded.
</verification>

<success_criteria>
- D-02 satisfied on evidence; production untouched except Caddy's config reload.
</success_criteria>

<output>
Create `.planning/phases/13-introduce-kubernetes/13-05-SUMMARY.md` when done.
</output>
