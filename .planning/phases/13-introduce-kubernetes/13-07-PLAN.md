---
phase: 13-introduce-kubernetes
plan: 07
type: execute
wave: 5
depends_on: ["13-03", "13-06"]
files_modified:
  - k8s/flux-system/monitoring-controllers.yaml
  - k8s/flux-system/monitoring.yaml
  - k8s/platform/cert-manager/cert-manager.yaml
  - k8s/monitoring/configs/dashboards/node-exporter-full.json
  - k8s/monitoring/configs/dashboards/cadvisor.json
  - k8s/monitoring/configs/dashboards/postgres-exporter.json
  - scripts/verify-public-dashboards.py
  - README.md
  - docs/INFRA_RUNBOOK.md
  - .planning/todos/pending/2026-09-08-grafana-admin-password-drift-from-env-prod.md
  - .planning/todos/completed/2026-09-08-grafana-admin-password-drift-from-env-prod.md
  - .planning/todos/pending/2026-09-07-rotate-grafana-viewer-password-leaked-in-session.md
autonomous: false
requirements: [D-07, D-10, D-17, D-18]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "kube-prometheus-stack, Loki, Alloy and postgres-exporter run in namespace monitoring, activated at the prod cutover rather than before it (D-07, D-17)."
    - "Every Prometheus target is up, and no k3s-incompatible control-plane job exists; Redpanda (both envs), postgres-exporter, kubelet cAdvisor, node-exporter and kube-state-metrics are all scraped (D-07)."
    - "Loki returns logs for pods in kanban-prod, kanban-nonprod, kanban-data, monitoring, flux-system and kube-system, collected without docker.sock or hostPath (D-07)."
    - "Grafana is reachable only at the monitoring hostname over a production certificate, with its admin password taken from a Secret created from .env.prod (D-10)."
    - "Exactly the three public shares listed in verify-public-dashboards.py PUBLIC_DASHBOARDS (node-exporter-full, cadvisor, postgres-exporter/Postgres Internals) are recreated on the new Grafana, no more and no fewer. README keeps its two links, repointed in one commit at the new node-exporter-full and cadvisor tokens. The third share's URL is recorded only in the SUMMARY, and verify-public-dashboards.py passes (D-18, corrected to three shares)."
  artifacts:
    - "k8s/flux-system/monitoring{,-controllers}.yaml unsuspended"
    - "README.md public dashboard links (new access tokens)"
    - "docs/INFRA_RUNBOOK.md section `## Observability on k3s — Plan 13-07 (<date>)`"
  key_links:
    - "Secret monitoring/grafana-admin -> kube-prometheus-stack grafana.admin.existingSecret"
    - "Secret monitoring/postgres-exporter -> exporter -> NetworkPolicy peer kanban-board/postgres-client=true -> postgres-0"
    - "Grafana public-dashboard accessToken -> README link -> /public-dashboards/<token>"
---

<objective>
Activate the prepared observability stack on k3s, prove every target and log source live, and recreate the three public dashboard shares on the new Grafana (D-18, corrected to three; not a runtime choice).

Purpose: D-17 places this right after the cutover, and the D-08 24h zero-OOM/zero-restart gate is measured with this stack.
Output: working in-cluster metrics, logs and dashboards; public links recreated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-introduce-kubernetes/13-03-PLAN.md
@.planning/phases/13-introduce-kubernetes/13-06-SUMMARY.md
@scripts/verify-public-dashboards.py
@README.md
</context>

<artifacts_this_phase_produces>
| Kind | Name | Where |
|---|---|---|
| Secrets (VM only) | monitoring/grafana-admin, monitoring/postgres-exporter | cluster |
| Flux | monitoring-controllers + monitoring unsuspended | k8s/flux-system/ |
| Grafana state | recreated public-dashboard shares (tokens in the grafana PVC) | cluster |
| Docs | README public links; runbook `## Observability on k3s — Plan 13-07` | README.md, docs/INFRA_RUNBOOK.md |
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — Secrets → unsuspend monitoring → Grafana over HTTPS with the Secret's admin password → one public-dashboard panel query returns datapoints</name>
  <files>k8s/flux-system/monitoring-controllers.yaml, k8s/flux-system/monitoring.yaml</files>
  <precondition>13-06 is complete: the monitoring hostname presents a production certificate, and every Compose container is stopped (the old Grafana/Prometheus hold no memory).</precondition>
  <read_first>
    - k8s/monitoring/controllers/kube-prometheus-stack.yaml (Service names from fullnameOverride; the Secret key names).
    - k8s/monitoring/controllers/postgres-exporter.yaml (Secret name/key).
  </read_first>
  <action>
1. **Secrets (D-10), on the VM, from `/opt/deploy/kanban-board-backend/.env.prod`, via 0600 temp env files that are then shredded:**
   - `monitoring/grafana-admin`: keys `admin-user=admin`, `admin-password`=GRAFANA_ADMIN_PASSWORD;
   - `monitoring/postgres-exporter`: key `password`=MONITORING_DB_PASS.
2. Set `suspend: false` in both monitoring Kustomization files, `git pull --rebase`, push. Wait for HelmReleases kube-prometheus-stack, loki, alloy and postgres-exporter to be Ready, then for the Kustomization `monitoring` to be Ready.
3. Confirm:
   - `https://kanban-board-rud-vlad-473-monitoring.duckdns.org/api/health` returns 200 over the production certificate;
   - `/login` passes through the `grafana-login-rate-limit` Middleware (`k3s kubectl get ingressroute grafana -n monitoring -o yaml`);
   - `/api/user` authenticates as admin, with credentials in a 0600 netrc file (`curl --netrc-file`), never argv;
   - live, `grafana-http` redirects `http://<monitoring host>/` to HTTPS but not `/.well-known/acme-challenge/`. 13-03's negated rule is proven in Traefik's own parser, and later renewals reach cert-manager's solver.
4. POST `/api/ds/query` with a node-exporter-full panel's expression (e.g. its node load query) against datasource uid `PBFA97CFB590B2093`. At least one frame must hold values.
5. Enable cert-manager's ServiceMonitor now that the CRD exists: `prometheus.servicemonitor.enabled: true` in `k8s/platform/cert-manager/cert-manager.yaml`, then commit.
  </action>
  <verify>
    <automated>
set -e
cd "$(git rev-parse --show-toplevel)"
ssh netcup-prod 'set -e
for h in kube-prometheus-stack loki alloy postgres-exporter; do k3s kubectl get helmrelease $h -n monitoring -o jsonpath="{.status.conditions[?(@.type==\"Ready\")].status}" | grep -qx True; done
k3s kubectl get kustomization monitoring -n flux-system -o jsonpath="{.status.conditions[?(@.type==\"Ready\")].status}" | grep -qx True
test -z "$(k3s kubectl get pods -A -o jsonpath="{range .items[*]}{range .spec.volumes[*]}{.hostPath.path}{\"\n\"}{end}{end}" | grep -F docker.sock)"
echo REMOTE_OK'
test "$(curl -s -o /dev/null -w '%{http_code}' https://kanban-board-rud-vlad-473-monitoring.duckdns.org/api/health)" = 200
test "$(curl -s -o /dev/null -w '%{http_code}' https://kanban-board-rud-vlad-473-monitoring.duckdns.org/api/user)" = 401
MON=kanban-board-rud-vlad-473-monitoring.duckdns.org
case "$(curl -s -o /dev/null -w '%{http_code}' "http://$MON/.well-known/acme-challenge/probe-not-a-token")" in 3*) echo "FAIL: $MON redirects the ACME challenge path"; exit 1;; esac
curl -s -o /dev/null -w '%{redirect_url}' "http://$MON/" | grep -q "^https://$MON/" || { echo "FAIL: $MON http root does not redirect to https"; exit 1; }
echo "TASK 1 PASS"
    </automated>
    <fails_when>Non-zero exit, `REMOTE_OK` absent, or a `FAIL:` line:
- a monitoring HelmRelease or the monitoring Kustomization is not Ready;
- any pod mounts docker.sock;
- `/api/health` is not 200 over HTTPS with default CA validation;
- anonymous `/api/user` is anything but 401 (anonymous access must be off);
- `http://<monitoring host>/.well-known/acme-challenge/…` answers any 3xx: the redirect would shadow cert-manager's HTTP-01 solver at renewal;
- `http://<monitoring host>/` does not redirect to `https://<monitoring host>/`.</fails_when>
    <human-check>Log in to the monitoring hostname as admin with the .env.prod password: login succeeds and the three provisioned dashboards appear in the list.</human-check>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the authenticated `/api/user` response (login, isGrafanaAdmin) and the `/api/ds/query` frame length for the node panel, without the password.
    - `k3s kubectl get secret -n monitoring grafana-admin postgres-exporter` lists both; neither value appears in any committed file or transcript.
  </acceptance_criteria>
  <done>Grafana, Prometheus, Loki, Alloy and the exporter run in-cluster, and the metrics path to a dashboard panel is proven.</done>
</task>

<task type="auto">
  <name>Task 2: Every target up, logs from every namespace, every panel diagnosed; recreate the three public shares; README links; gate green</name>
  <files>k8s/monitoring/configs/dashboards/*.json (only on a diagnosed label mismatch), scripts/verify-public-dashboards.py, README.md, docs/INFRA_RUNBOOK.md, .planning/todos/pending/2026-09-08-grafana-admin-password-drift-from-env-prod.md, .planning/todos/pending/2026-09-07-rotate-grafana-viewer-password-leaked-in-session.md</files>
  <read_first>
    - docs/INFRA_RUNBOOK.md "Public Grafana dashboards rendered no data — debug session (2026-09-12)": the per-panel probe method via `/api/ds/query` and `/api/public/dashboards/<token>/panels/<id>/query`.
    - Both todo files named in `<files>`.
  </read_first>
  <action>
**Targets:** on the VM, `k3s kubectl port-forward -n monitoring svc/kps-prometheus 9090` (Service name from 13-03). Then `GET /api/v1/targets?state=active`:
- every target `health == "up"`;
- no job whose name contains kube-controller-manager, kube-scheduler, kube-etcd or kube-proxy (Pitfall 5);
- present: Redpanda pods from both namespaces with `env` prod/nonprod, the postgres-exporter job, kubelet `/metrics/cadvisor`, node-exporter, kube-state-metrics.

**Logs:** Loki `GET /loki/api/v1/label/namespace/values` includes kanban-prod, kanban-nonprod, kanban-data, monitoring, flux-system and kube-system.

**Panels:** for each of the three dashboards, run every panel's query via `/api/ds/query`. Record any empty panel with its diagnosis: uncollected metric (acceptable, named) / label mismatch (fix it in `k8s/monitoring/configs/dashboards/*.json`, commit, and re-run the gate) / datasource problem (hard failure).

**Public shares (D-18, corrected 2026-09-25 to three; fixed by the operator, not asked at runtime):**
- Enable a public share for exactly the three uids in `PUBLIC_DASHBOARDS`, through Grafana's public-dashboards API with Task 1's netrc credentials. Enable no other dashboard. This keeps today's public surface exactly. The uids are:
  - node-exporter-full `rYdddlPWk`;
  - cadvisor `pMEd7m0Mz`;
  - postgres-exporter / Postgres Internals `v5ciIbUZz`.
- Record each returned access token.
- For each share, `GET /api/public/dashboards/<token>` returns 200, and at least one panel's `/api/public/dashboards/<token>/panels/<id>/query` returns datapoints.
- README "Live" keeps its **two** links. In one commit together with the gate comment change, replace both old tokens:
  - Network & OS → node-exporter-full's new token;
  - CPU & Memory metrics → cadvisor's new token.
- Postgres Internals stays public with no README link, as it is today. Its new URL is recorded in the SUMMARY only, never in README.
- `PUBLIC_DASHBOARDS` keeps all three entries. Update only its "Verified against GET /api/dashboards/public-dashboards" comment to name the new Grafana and date.
- Run `python3 scripts/verify-public-dashboards.py`.

**Todos:**
- Move `2026-09-08-grafana-admin-password-drift-from-env-prod.md` to `.planning/todos/completed/`, with `## Resolution`: the new Grafana's admin password comes from Secret grafana-admin built from .env.prod, and login with it was proven in Task 1.
- For `2026-09-07-rotate-grafana-viewer-password-leaked-in-session.md`: if no viewer account was recreated on the new Grafana, move it to completed with a resolution saying the leaked credential no longer exists (fresh grafana.db). Otherwise leave it pending with a dated note.

**Runbook** `## Observability on k3s — Plan 13-07 (<date>)`, subsections:
1. Activation.
2. Targets (count up / down = 0).
3. Log namespaces.
4. Panel diagnosis table.
5. Public shares: the three dashboard uids, each with its README label. postgres-exporter is marked "no README link; URL in 13-07-SUMMARY.md". The runbook records no tokens beyond what the README already publishes.
6. Todo closures.
  </action>
  <verify>
    <automated>
set -e
cd "$(git rev-parse --show-toplevel)"
python3 scripts/verify-public-dashboards-selftest.py && python3 scripts/verify-public-dashboards.py
python3 - <<'PY'
import re, sys, urllib.request
readme = open('README.md').read()
links = re.findall(r'https://kanban-board-rud-vlad-473-monitoring\.duckdns\.org/public-dashboards/([0-9a-f]{32})', readme)
fails = []
if len(links) != 2: fails.append(f"README has {len(links)} public links, expected 2")
for old in ('7db62007a2114c988c21c4cf6eac9776', '5a72e5df7fd54618ae28972cabcd8846'):
    if old in readme: fails.append(f"README still links old token {old[:8]}")
for t in links:
    try:
        code = urllib.request.urlopen(f"https://kanban-board-rud-vlad-473-monitoring.duckdns.org/public-dashboards/{t}", timeout=20).status
    except Exception as e:
        code = str(e)
    if code != 200: fails.append(f"public link {t[:8]} -> {code}")
rb = open('docs/INFRA_RUNBOOK.md').read()
if '## Observability on k3s — Plan 13-07' not in rb: fails.append("runbook section missing")
import os
if os.path.exists('.planning/todos/pending/2026-09-08-grafana-admin-password-drift-from-env-prod.md'): fails.append("admin-drift todo not closed")
if fails:
    print("\n".join("FAIL: " + x for x in fails)); sys.exit(1)
PY
MON=kanban-board-rud-vlad-473-monitoring.duckdns.org
umask 077; NETRC=$(mktemp); PD=$(mktemp); trap 'shred -u "$NETRC"; rm -f "$PD"' EXIT
printf 'machine %s login admin password %s\n' "$MON" "$(ssh netcup-prod 'k3s kubectl get secret -n monitoring grafana-admin -o jsonpath="{.data.admin-password}" | base64 -d')" > "$NETRC"
curl -fsS --netrc-file "$NETRC" "https://$MON/api/dashboards/public-dashboards" > "$PD"
python3 - "$PD" "$MON" <<'PY'
import json, sys, urllib.request
raw = json.load(open(sys.argv[1])); mon = sys.argv[2]
items = raw.get('publicDashboards', []) if isinstance(raw, dict) else raw
expect = {'rYdddlPWk': 'Network & OS', 'pMEd7m0Mz': 'CPU & Memory metrics', 'v5ciIbUZz': None}
gate = open('scripts/verify-public-dashboards.py').read()
fails = [f"PUBLIC_DASHBOARDS no longer lists {u}" for u in expect if u not in gate]
uids = sorted(x.get('dashboardUid') for x in items)
if uids != sorted(expect): fails.append(f"Grafana public shares {uids} != exactly {sorted(expect)}")
if any(x.get('isEnabled') is False for x in items): fails.append("a public share is paused")
tok = {x.get('dashboardUid'): x.get('accessToken', '') for x in items}
readme = open('README.md').read()
for uid, label in expect.items():
    t = tok.get(uid, '')
    try:
        code = urllib.request.urlopen(f"https://{mon}/api/public/dashboards/{t}", timeout=20).status if t else 'no token'
    except Exception as e:
        code = str(e)
    if code != 200: fails.append(f"public API for {uid} -> {code}")
    if label is None:
        if t and t in readme: fails.append(f"README links the unlinked {uid} share")
    elif f"[{label}](https://{mon}/public-dashboards/{t})" not in readme:
        fails.append(f"README '{label}' does not link {uid}'s new token")
if fails:
    print("\n".join("FAIL: " + x for x in fails)); sys.exit(1)
print("SHARES_OK", len(items))
PY
ssh netcup-prod 'set -e
k3s kubectl -n monitoring port-forward svc/kps-prometheus 19090:9090 >/dev/null 2>&1 & PF=$!; sleep 3
curl -s "http://127.0.0.1:19090/api/v1/targets?state=active" | python3 -c "
import json,sys; t=json.load(sys.stdin)[\"data\"][\"activeTargets\"]
down=[x[\"labels\"].get(\"job\") for x in t if x[\"health\"]!=\"up\"]
bad=[x[\"labels\"].get(\"job\") for x in t if any(k in x[\"labels\"].get(\"job\",\"\") for k in (\"kube-controller-manager\",\"kube-scheduler\",\"kube-etcd\",\"kube-proxy\"))]
envs={x[\"labels\"].get(\"env\") for x in t if \"redpanda\" in x[\"labels\"].get(\"job\",\"\")+x[\"labels\"].get(\"pod\",\"\")}
assert not down, down; assert not bad, bad; assert {\"prod\",\"nonprod\"} <= envs, envs; print(\"TARGETS_OK\", len(t))"
kill $PF'
echo "TASK 2 PASS"
    </automated>
    <fails_when>Non-zero exit, `SHARES_OK` or `TARGETS_OK` absent, or a `FAIL:` line. Specifically:
- the public-dashboard gate or its selftest fails;
- README does not hold exactly two monitoring public links, still holds an old token, or a new link does not answer 200;
- Grafana's authoritative share list (`GET /api/dashboards/public-dashboards`, admin via a 0600 netrc) is not exactly the three uids rYdddlPWk, pMEd7m0Mz and v5ciIbUZz. That covers a missing share, an extra share and a paused share. It also fails if any of the three uids has left verify-public-dashboards.py;
- any of the three shares' `/api/public/dashboards/<token>` is not 200;
- README's "Network & OS" link is not node-exporter-full's new token, "CPU & Memory metrics" is not cadvisor's, or README links the Postgres Internals token;
- the runbook section is missing, or the admin-drift todo is still pending;
- any Prometheus target is down, a control-plane job exists, or Redpanda targets for both prod and nonprod are absent.</fails_when>
    <human-check>Open both README public links in a private browser window: every panel draws plotted data (no "No data", no "Datasource not found", no "Plugin not found").</human-check>
  </verify>
  <acceptance_criteria>
    - The runbook's panel table lists every panel of the three dashboards, each marked data-OK or diagnosed; no panel is left undiagnosed.
    - The Loki namespace list includes all six namespaces named in the must_haves.
    - The public surface matches D-18 (corrected) exactly. Grafana's API reports three enabled public shares, whose dashboard uids are the three in PUBLIC_DASHBOARDS (asserted in verify).
    - The SUMMARY records all three share URLs. The Postgres Internals URL appears there and in no README line.
  </acceptance_criteria>
  <done>In-cluster observability is proven complete, and D-18's public links work against the new Grafana.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Grafana login + public dashboards | Public, rate-limited login; unauthenticated public shares |
| Grafana → Prometheus/Loki | Internal ClusterIP only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-34 | Spoofing | Grafana admin credential | high | mitigate | Password from Secret grafana-admin (D-10), rate-limited `/login`, sign-up and anonymous access off (asserted: anonymous `/api/user` → 401); credentials only ever in a 0600 netrc, never argv. |
| T-13-35 | Information Disclosure | Public dashboards exposing more than intended | medium | mitigate | The share set is fixed by D-18 (corrected) to exactly the three PUBLIC_DASHBOARDS uids, which is today's surface, and asserted against Grafana's authoritative share list in Task 2's verify; dual-scope gate enforces literal UIDs, no variables, no Docker-era labels; human check of the rendered pages. |
| T-13-36 | Information Disclosure | Logs from all namespaces in Loki | low | accept | Same scope as Phase 12 D-08 (all-container logs); Loki is ClusterIP-only behind Grafana's login. |
| T-13-37 | Repudiation | Old public links silently dead | low | mitigate | README updated in the same commit as the recreated shares; links checked for 200 in verify. |
</threat_model>

<verification>
1. All monitoring HelmReleases Ready; all targets up; no control-plane jobs; no docker.sock.
2. Logs present for six namespaces.
3. Grafana holds exactly the three PUBLIC_DASHBOARDS shares, all answering 200. README's two links answer 200 and render data (human check). verify-public-dashboards.py is green.
4. The monitoring host redirects HTTP to HTTPS except `/.well-known/acme-challenge/`.
</verification>

<success_criteria>
- D-07's stack is live and complete, D-18's three shares are recreated with README's two links repointed, and the D-08 24h window (13-09) can be measured with kube-prometheus-stack.
</success_criteria>

<output>
Create `.planning/phases/13-introduce-kubernetes/13-07-SUMMARY.md` when done.
</output>
