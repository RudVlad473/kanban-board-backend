---
phase: 13-introduce-kubernetes
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - k8s/monitoring/controllers/kustomization.yaml
  - k8s/monitoring/controllers/repositories.yaml
  - k8s/monitoring/controllers/kube-prometheus-stack.yaml
  - k8s/monitoring/controllers/loki.yaml
  - k8s/monitoring/controllers/alloy.yaml
  - k8s/monitoring/controllers/postgres-exporter.yaml
  - k8s/monitoring/configs/kustomization.yaml
  - k8s/monitoring/configs/datasources.yaml
  - k8s/monitoring/configs/podmonitors.yaml
  - k8s/monitoring/configs/ingressroute.yaml
  - k8s/monitoring/configs/dashboards/node-exporter-full.json
  - k8s/monitoring/configs/dashboards/cadvisor.json
  - k8s/monitoring/configs/dashboards/postgres-exporter.json
  - scripts/verify-public-dashboards.py
  - scripts/verify-public-dashboards-selftest.py
autonomous: true
requirements: [D-07, D-09, D-17, D-18]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "kube-prometheus-stack, Loki (single-binary, 30d), Grafana Alloy and a chart-managed postgres-exporter are declared as Flux HelmReleases with pinned chart versions, under the suspended monitoring Kustomizations, so nothing runs before the prod cutover (D-07, D-09, D-17)."
    - "The chart replaces cAdvisor, node-exporter and postgres-exporter containers with kubelet cAdvisor, the chart's node-exporter (no hostNetwork/hostPID) and a chart exporter; no docker.sock mount exists anywhere (D-07)."
    - "Grafana keeps datasource UIDs PBFA97CFB590B2093/P8E80F9AEF21F6940 as literal values and runs the same Grafana image (13.2.1) the public-dashboard gate was measured against (D-07)."
    - "The three dashboards exist as k8s copies whose queries use Kubernetes labels (namespace/pod/container) and stable hard-coded values, and verify-public-dashboards.py gates both the Compose copies (still live until 13-10) and the k8s copies (D-07, D-18)."
    - "Alertmanager, kube-prometheus-stack default alert rules, and the k3s-incompatible control-plane ServiceMonitors are disabled (D-07; alerting deferred)."
  artifacts:
    - "k8s/monitoring/controllers/*.yaml — HelmRepositories + 4 HelmReleases"
    - "k8s/monitoring/configs/ — datasources ConfigMap, dashboards ConfigMaps (sidecar-labelled), Redpanda PodMonitor, Grafana IngressRoute + login rate-limit Middleware"
    - "scripts/verify-public-dashboards.py — dual-scope (Compose + k8s)"
  key_links:
    - "ConfigMap label grafana_datasource/grafana_dashboard -> kube-prometheus-stack Grafana sidecars"
    - "postgres-exporter pod label kanban-board/postgres-client=true -> NetworkPolicy kanban-data/postgres-ingress (13-04)"
    - "IngressRoute monitoring/grafana tls.secretName grafana-tls -> Certificate in k8s/platform/edge (13-04)"
---

<objective>
Author the whole in-cluster observability stack as committed, validated, suspended manifests, including the Kubernetes-label rewrite of the three public dashboards.

Purpose: D-17 moves observability at the prod cutover; the preparation must be finished and reviewed beforehand, so that 13-07 is an activation, not an authoring session.
Output: k8s/monitoring/{controllers,configs}, and a dual-scope public-dashboard gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-introduce-kubernetes/13-CONTEXT.md
@.planning/phases/13-introduce-kubernetes/13-RESEARCH.md
@.planning/phases/13-introduce-kubernetes/13-01-PLAN.md
@docker-compose.prod.yml
@docker/prometheus/prometheus.yml
@docker/grafana/provisioning/datasources/datasources.yaml
@scripts/verify-public-dashboards.py
</context>

<artifacts_this_phase_produces>
| Kind | Name | Where |
|---|---|---|
| HelmRepository | prometheus-community, grafana (flux-system) | k8s/monitoring/controllers/repositories.yaml |
| HelmRelease | kube-prometheus-stack, loki, alloy, postgres-exporter (namespace monitoring) | k8s/monitoring/controllers/ |
| ConfigMap | grafana-datasources, grafana-dashboard-{node-exporter-full,cadvisor,postgres-exporter} | k8s/monitoring/configs/ |
| PodMonitor | redpanda (kanban-prod + kanban-nonprod) | k8s/monitoring/configs/podmonitors.yaml |
| IngressRoute / Middleware | monitoring/grafana, monitoring/grafana-login-rate-limit, monitoring/redirect-https | k8s/monitoring/configs/ingressroute.yaml |
| Script change | verify-public-dashboards.py dual scope | scripts/ |
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — kube-prometheus-stack HelmRelease → literal-UID datasources → one rewritten dashboard → public-dashboard gate green on the k8s copy</name>
  <reversibility rating="reversible">Nothing reconciles until 13-07 unsuspends; every choice here is a file edit.</reversibility>
  <files>k8s/monitoring/controllers/kustomization.yaml, k8s/monitoring/controllers/repositories.yaml, k8s/monitoring/controllers/kube-prometheus-stack.yaml, k8s/monitoring/configs/kustomization.yaml, k8s/monitoring/configs/datasources.yaml, k8s/monitoring/configs/dashboards/node-exporter-full.json, scripts/verify-public-dashboards.py, scripts/verify-public-dashboards-selftest.py</files>
  <read_first>
    - scripts/verify-public-dashboards.py (full docstring; `PUBLIC_DASHBOARDS`, `find_grafana_version_drift`, `main`) and its selftest.
    - docker/grafana/provisioning/datasources/datasources.yaml — UIDs to carry verbatim.
    - docker-compose.prod.yml `prometheus:`, `grafana:`, `node-exporter:` blocks — the 30d retention flag, Grafana env (sign-up/anonymous off), the Grafana image tag, and the node-exporter host-root rationale.
    - 13-RESEARCH.md Pitfalls 5 and 7; project memory "Grafana public dashboards need literal UID + no vars".
  </read_first>
  <action>
**repositories.yaml:** HelmRepositories `prometheus-community` and `grafana` in flux-system, interval 1h.

**kube-prometheus-stack.yaml:** HelmRelease `kube-prometheus-stack` in monitoring. Pin the chart version to the latest release at authoring time; record it. Values:
- Disabled: `alertmanager.enabled`, `defaultRules.create`, `grafana.defaultDashboardsEnabled`. Also `kubeControllerManager`, `kubeScheduler`, `kubeEtcd` and `kubeProxy` `.enabled`, which never work on k3s (Pitfall 5).
- `fullnameOverride: kps`, so Service names are deterministic (`kps-prometheus`, …). Record the resulting Service names in a header comment.
- Prometheus:
  - `retention: 30d` (Phase 12 D-07);
  - `storageSpec`: PVC local-path, 10Gi;
  - `serviceMonitorSelectorNilUsesHelmValues: false` and `podMonitorSelectorNilUsesHelmValues: false`.
- node-exporter subchart: `hostNetwork: false`, `hostPID: false`, `hostRootFsMount.enabled: true`. Same trade-off as Phase 12: host filesystems visible, network metrics from the pod namespace. Add a ServiceMonitor relabeling that sets `instance` from `__meta_kubernetes_pod_node_name`, so hard-coded dashboard values survive pod restarts.
- Grafana:
  - `image.tag: 13.2.1`;
  - `admin.existingSecret: grafana-admin` (keys `admin-user` / `admin-password`, created on the VM in 13-07 — D-10);
  - `grafana.ini`: `server.root_url: https://kanban-board-rud-vlad-473-monitoring.duckdns.org`, `users.allow_sign_up: false`, `auth.anonymous.enabled: false`, `security.cookie_secure: true`;
  - persistence enabled, local-path, 1Gi — it holds the recreated public-share tokens (D-18);
  - dashboards sidecar on label `grafana_dashboard: "1"`;
  - datasources sidecar on label `grafana_datasource: "1"`, with `sidecar.datasources.defaultDatasourceEnabled: false`, so the only Prometheus datasource is ours with its literal UID.
- Every component's `resources` is labelled `PROVISIONAL (restart-ladder in 13-09)`, starting from the Phase 12 measured caps where a same-binary predecessor exists (Prometheus 256Mi, Grafana 384Mi, node-exporter 32Mi), otherwise from research's ranges.
- `install.crds: CreateReplace`, `upgrade.crds: CreateReplace`.

**datasources.yaml:** ConfigMap `grafana-datasources` in monitoring, label `grafana_datasource: "1"`. It carries the two datasources from the Compose file with identical names and UIDs. URLs: `http://kps-prometheus.monitoring.svc:9090` and `http://loki.monitoring.svc:3100` — use the exact Service names the fullnameOverrides produce.

**Dashboard (tracer: node-exporter-full only):**
- Copy it to `k8s/monitoring/configs/dashboards/`.
- Rewrite the Compose-era hard-coded `job`/`instance` matchers (the Compose scrape job name and the exporter's container address) to the chart's node-exporter job name and the node-name `instance` from the relabeling.
- Keep uid `rYdddlPWk`, literal datasource UIDs and no template variables.
- `configs/kustomization.yaml`: `namespace: monitoring`, a configMapGenerator entry per dashboard with label `grafana_dashboard: "1"`, and `disableNameSuffixHash: true`. One ConfigMap per dashboard: node-exporter-full alone is ~470 KB. These must only ever be server-side applied (Flux); client-side `kubectl apply` would exceed the 256 KiB last-applied annotation.

**verify-public-dashboards.py:** turn the single `(JSON_DIR, DATASOURCES, COMPOSE)` scope into a `SCOPES` list of `(json_dir, uid_source, grafana_version_source)`:
- Compose scope: unchanged; 13-10 deletes it.
- k8s scope: UIDs read from the datasources ConfigMap's embedded YAML; the Grafana version read from the HelmRelease `values.grafana.image.tag`.

I1-I5 and the uid mapping apply per scope. A scope whose json_dir does not exist fails loudly rather than skipping. Add selftest cases: a k8s-scope dashboard with a `${DS_*}` datasource fails; a HelmRelease Grafana tag drift fails. Tasks 2-3 add the other two dashboards; until then, list them in the k8s scope's expected set as pending, so the gate says exactly which files are missing.
  </action>
  <verify>
    <automated>
set -e
cd "$(git rev-parse --show-toplevel)"
bash scripts/verify-k8s-manifests.sh && python3 scripts/verify-k8s-invariants.py
python3 scripts/verify-public-dashboards-selftest.py && python3 scripts/verify-public-dashboards.py
HELM=$(bash scripts/verify-k8s-manifests.sh --tool helm)
python3 - "$HELM" <<'PY'
import subprocess, sys, tempfile, yaml
helm = sys.argv[1]
hr = yaml.safe_load(open('k8s/monitoring/controllers/kube-prometheus-stack.yaml'))
repo = {d['metadata']['name']: d for d in yaml.safe_load_all(open('k8s/monitoring/controllers/repositories.yaml')) if d}
chart = hr['spec']['chart']['spec']
url = repo[chart['sourceRef']['name']]['spec']['url']
with tempfile.NamedTemporaryFile('w', suffix='.yaml', delete=False) as f:
    yaml.safe_dump(hr['spec']['values'], f)
out = subprocess.check_output([helm, 'template', 'kps', chart['chart'], '--repo', url, '--version', chart['version'],
                               '-n', 'monitoring', '-f', f.name, '--include-crds=false'], text=True)
docs = [d for d in yaml.safe_load_all(out) if d]
kinds = {(d['kind'], d['metadata']['name']) for d in docs}
fails = []
if any(k[0] == 'Alertmanager' for k in kinds): fails.append("Alertmanager rendered")
for sm in ('kube-controller-manager', 'kube-scheduler', 'kube-etcd', 'kube-proxy'):
    if any(k[0] == 'ServiceMonitor' and sm in k[1] for k in kinds): fails.append(f"{sm} ServiceMonitor rendered")
for d in docs:
    if d['kind'] == 'DaemonSet' and 'node-exporter' in d['metadata']['name']:
        ps = d['spec']['template']['spec']
        if ps.get('hostNetwork') or ps.get('hostPID'): fails.append("node-exporter uses host network/pid")
    if d['kind'] == 'Deployment' and d['metadata']['name'].endswith('grafana'):
        imgs = [c['image'] for c in d['spec']['template']['spec']['containers']]
        if not any(i.endswith(':13.2.1') for i in imgs): fails.append(f"grafana image not 13.2.1: {imgs}")
if any('docker.sock' in yaml.safe_dump(d) for d in docs): fails.append("docker.sock mounted")
ds = yaml.safe_load(yaml.safe_load(open('k8s/monitoring/configs/datasources.yaml'))['data'][next(iter(yaml.safe_load(open('k8s/monitoring/configs/datasources.yaml'))['data']))])
uids = {x['uid'] for x in ds['datasources']}
if uids != {'PBFA97CFB590B2093', 'P8E80F9AEF21F6940'}: fails.append(f"datasource uids {uids}")
if fails:
    print("\n".join("FAIL: " + x for x in fails)); sys.exit(1)
print("TASK 1 PASS")
PY
    </automated>
    <fails_when>Non-zero exit or any `FAIL:` line. Specifically:
- the manifest, invariant or public-dashboard gates (or their selftests) fail;
- `helm template` of the pinned chart with the committed values renders an Alertmanager, a k3s-incompatible control-plane ServiceMonitor, a node-exporter with hostNetwork/hostPID, a Grafana image other than 13.2.1, or any docker.sock mount;
- the datasource ConfigMap's UIDs differ from the Compose ones.</fails_when>
  </verify>
  <acceptance_criteria>
    - `verify-public-dashboards.py` reports both scopes, passes the Compose scope unchanged, and passes node-exporter-full in the k8s scope.
    - The k8s node-exporter-full.json contains no `node-exporter:9100` and no `job="node"` matcher, and still has uid `rYdddlPWk`.
    - Every `resources` block in the HelmRelease carries a PROVISIONAL label naming 13-09.
    - `git diff --stat docker/` is empty: the live Compose dashboards are untouched.
  </acceptance_criteria>
  <done>The Prometheus/Grafana path is declared, statically proven against the real chart, and gated for public-dashboard safety.</done>
</task>

<task type="auto">
  <name>Task 2: Loki, Alloy, postgres-exporter, Redpanda PodMonitor, Grafana route with its login rate limit</name>
  <files>k8s/monitoring/controllers/loki.yaml, k8s/monitoring/controllers/alloy.yaml, k8s/monitoring/controllers/postgres-exporter.yaml, k8s/monitoring/controllers/kustomization.yaml, k8s/monitoring/configs/podmonitors.yaml, k8s/monitoring/configs/ingressroute.yaml, k8s/monitoring/configs/kustomization.yaml</files>
  <read_first>
    - docker/loki/loki-config.yaml — the retention, compactor and delete_request_store settings to carry.
    - docker-compose.prod.yml `postgres-exporter:` (flags, the monitoring user, `DATA_SOURCE_URI`) and `promtail:` (label set).
    - Caddyfile `{$APP_DOMAIN_MONITORING}` block — the `grafana_login` zone.
    - 13-01-PLAN.md `<phase_contracts>` (exporter label, TLS Secret, hostnames).
  </read_first>
  <action>
**loki.yaml:** HelmRelease `loki` from `grafana/loki`, chart version pinned. Values:
- Deployment mode single-binary. The key is `SingleBinary` or `Monolithic` depending on chart version — check the pinned chart's values.yaml, because the wrong key silently no-ops.
- `loki.auth_enabled: false`, `commonConfig.replication_factor: 1`, filesystem storage.
- tsdb v13 schema, `from:` dated before first ingest.
- `limits_config.retention_period: 720h`; compactor with `retention_enabled: true` and `delete_request_store: filesystem`.
- singleBinary: replicas 1, persistence local-path 10Gi.
- read/write/backend replicas 0.
- Disabled: gateway, chunksCache, resultsCache, lokiCanary, test, self-monitoring.
- Resources PROVISIONAL (13-09).

**alloy.yaml:** HelmRelease `alloy` from `grafana/alloy`, pinned. Values:
- Controller DaemonSet; no hostPath mounts (`alloy.mounts.varlog: false`, `dockercontainers: false`).
- Config: `discovery.kubernetes` (role pod) → `discovery.relabel`, keeping labels namespace, pod, container and app from `app.kubernetes.io/name` → `loki.source.kubernetes`, which tails via the API with no hostPath → `loki.write` to `http://loki.monitoring.svc:3100/loki/api/v1/push`.
- Resources PROVISIONAL (13-09).

**postgres-exporter.yaml:** HelmRelease `postgres-exporter` from `prometheus-community/prometheus-postgres-exporter`, pinned. Values:
- Image tag `v0.20.1`, as in Compose.
- Datasource: host `postgres.kanban-data.svc.cluster.local`, port 5432, database `kanban_prod`, user `monitoring`, sslmode disable. Password from Secret `postgres-exporter` key `password` (created in 13-07; D-10).
- extraArgs `--auto-discover-databases --collector.postmaster --collector.stat_statements`.
- `podLabels: {kanban-board/postgres-client: "true"}` — the NetworkPolicy contract.
- `serviceMonitor.enabled: true`, with a relabeling that keeps a stable `job`/`instance` for the dashboard.
- Resources PROVISIONAL.

Add loki, alloy and postgres-exporter to the controllers kustomization.

**podmonitors.yaml:** PodMonitor `redpanda` in monitoring. `namespaceSelector.matchNames` [kanban-prod, kanban-nonprod], selector `app.kubernetes.io/name: redpanda`, podMetricsEndpoints port `admin`, path `/public_metrics`. A relabeling derives `env` (prod/nonprod) from `__meta_kubernetes_namespace`, preserving the Compose-era `env` label semantics.

**ingressroute.yaml** (namespace monitoring):
- Middleware `grafana-login-rate-limit`: `rateLimit` average 20, period 5m, burst 20, `sourceCriterion.ipStrategy` with `ipv6Subnet: 56`. Comment: `PROVISIONAL translation of Caddy zone grafana_login; re-derived from token-bucket semantics in 13-08`.
- Middleware `redirect-https`: redirectScheme https, permanent.
- IngressRoute `grafana`:
  - websecure: ``Host(`kanban-board-rud-vlad-473-monitoring.duckdns.org`) && PathPrefix(`/login`)`` → rate-limit middleware → the Grafana Service; ``Host(…)`` → the Grafana Service;
  - `tls.secretName: grafana-tls`;
  - web: ``Host(…)`` → redirect-https (the default web route stays clear of cert-manager's solver ingress, whose longer rule wins).
  - Grafana Service name: from the kube-prometheus-stack fullnameOverride; verify it with `helm template`.

Add podmonitors.yaml and ingressroute.yaml to the configs kustomization.
  </action>
  <verify>
    <automated>
set -e
cd "$(git rev-parse --show-toplevel)"
bash scripts/verify-k8s-manifests.sh && python3 scripts/verify-k8s-invariants.py
HELM=$(bash scripts/verify-k8s-manifests.sh --tool helm)
python3 - "$HELM" <<'PY'
import subprocess, sys, tempfile, yaml
helm = sys.argv[1]
repos = {d['metadata']['name']: d['spec']['url'] for d in yaml.safe_load_all(open('k8s/monitoring/controllers/repositories.yaml')) if d}
fails = []
def render(path):
    hr = yaml.safe_load(open(path)); c = hr['spec']['chart']['spec']
    with tempfile.NamedTemporaryFile('w', suffix='.yaml', delete=False) as f: yaml.safe_dump(hr['spec'].get('values', {}), f)
    out = subprocess.check_output([helm, 'template', hr['metadata']['name'], c['chart'], '--repo', repos[c['sourceRef']['name']],
                                   '--version', c['version'], '-n', 'monitoring', '-f', f.name], text=True)
    return hr, [d for d in yaml.safe_load_all(out) if d]
hr, loki = render('k8s/monitoring/controllers/loki.yaml')
kinds = [(d['kind'], d['metadata']['name']) for d in loki]
if any('canary' in n or 'gateway' in n or 'memcached' in n or 'chunks-cache' in n or 'results-cache' in n for _, n in kinds):
    fails.append(f"loki renders extra components: {kinds}")
cfg = yaml.safe_dump(loki)
if '720h' not in cfg or 'retention_enabled: true' not in cfg: fails.append("loki retention not 720h/enabled")
_, alloy = render('k8s/monitoring/controllers/alloy.yaml')
for d in alloy:
    if d['kind'] == 'DaemonSet' and any('hostPath' in v for v in d['spec']['template']['spec'].get('volumes', [])):
        fails.append("alloy mounts a hostPath")
_, pe = render('k8s/monitoring/controllers/postgres-exporter.yaml')
dep = next((d for d in pe if d['kind'] == 'Deployment'), None)
if not dep or dep['spec']['template']['metadata']['labels'].get('kanban-board/postgres-client') != 'true':
    fails.append("postgres-exporter pod lacks kanban-board/postgres-client=true")
ir = [d for d in yaml.safe_load_all(open('k8s/monitoring/configs/ingressroute.yaml')) if d]
grafana = next((d for d in ir if d['kind'] == 'IngressRoute' and d['metadata']['name'] == 'grafana' and 'websecure' in d['spec']['entryPoints']), None)
if not grafana or grafana['spec'].get('tls', {}).get('secretName') != 'grafana-tls': fails.append("grafana websecure route/tls wrong")
elif not any('/login' in r['match'] and any(m['name'] == 'grafana-login-rate-limit' for m in r.get('middlewares', [])) for r in grafana['spec']['routes']):
    fails.append("login route lacks rate-limit middleware")
if fails:
    print("\n".join("FAIL: " + x for x in fails)); sys.exit(1)
print("TASK 2 PASS")
PY
    </automated>
    <fails_when>Non-zero exit or any `FAIL:` line. Specifically:
- the manifest or invariant gates fail;
- the pinned Loki chart renders a canary, gateway or cache component, or lacks 720h retention with retention enabled;
- the Alloy DaemonSet mounts a hostPath;
- the postgres-exporter pod lacks the `kanban-board/postgres-client: "true"` label;
- the Grafana IngressRoute lacks the websecure route with `grafana-tls`, or its `/login` route lacks the rate-limit middleware.</fails_when>
  </verify>
  <acceptance_criteria>
    - Each of the four HelmReleases pins an exact chart version; none says `*` or `latest`.
    - The PodMonitor selects both app namespaces, and its relabeling yields `env` values `prod` and `nonprod`.
    - No manifest under k8s/monitoring references `docker.sock`, `hostNetwork: true` or `hostPID: true`.
  </acceptance_criteria>
  <done>Logs, exporter and Redpanda scraping, and the Grafana edge route are declared and statically proven.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite cadvisor and postgres-exporter dashboards to Kubernetes labels; complete the dual-scope gate</name>
  <files>k8s/monitoring/configs/dashboards/cadvisor.json, k8s/monitoring/configs/dashboards/postgres-exporter.json, k8s/monitoring/configs/kustomization.yaml, scripts/verify-public-dashboards.py, scripts/verify-public-dashboards-selftest.py</files>
  <read_first>
    - docker/grafana/provisioning/dashboards/json/cadvisor.json and postgres-exporter.json — every hard-coded matcher (`instance="cadvisor:8080"`, `name=~".+"`, `instance="postgres-exporter:9187"`, `job="postgres-exporter"`).
    - docs/INFRA_RUNBOOK.md "Public Grafana dashboards rendered no data — debug session (2026-09-12)".
  </read_first>
  <action>
Copy both dashboards to `k8s/monitoring/configs/dashboards/`.

**cadvisor.json (Pitfall 7):** rewrite every query from Docker-cAdvisor labels to kubelet-cAdvisor labels:
- `job="kubelet"`, `metrics_path="/metrics/cadvisor"`;
- `container!=""` and `container!="POD"` in place of the Docker `name` regex matcher;
- legend and grouping by `namespace`/`pod`/`container` in place of `name`;
- no Compose-era cAdvisor `instance` literal.

Keep the uid, the literal datasource UID and zero template variables.

**postgres-exporter.json:** replace `instance`/`job` literals with the stable values Task 2's ServiceMonitor relabeling produces.

Add both to the configMapGenerator and to the k8s scope's expected set in verify-public-dashboards.py; drop the pending markers.

Add a k8s-scope invariant — in the gate if it fits the existing numbering, otherwise as a documented extra check. A public k8s dashboard must contain none of the Docker-era literals: `cadvisor:8080`, `node-exporter:9100`, `postgres-exporter:9187`, `container_label_com_docker`, and `name=~` on `container_*` metrics. Add a selftest case for it.

Record in the script docstring's KNOWN HOLES that "renders data" is proven only live, in 13-07.
  </action>
  <verify>
    <automated>
set -e
cd "$(git rev-parse --show-toplevel)"
python3 scripts/verify-public-dashboards-selftest.py && python3 scripts/verify-public-dashboards.py
bash scripts/verify-k8s-manifests.sh && python3 scripts/verify-k8s-invariants.py
python3 - <<'PY'
import json, re, sys
fails = []
expect = {'node-exporter-full.json': 'rYdddlPWk', 'cadvisor.json': 'pMEd7m0Mz', 'postgres-exporter.json': 'v5ciIbUZz'}
banned = ['cadvisor:8080', 'node-exporter:9100', 'postgres-exporter:9187', 'container_label_com_docker']
for fn, uid in expect.items():
    raw = open(f'k8s/monitoring/configs/dashboards/{fn}').read(); d = json.loads(raw)
    if d.get('uid') != uid: fails.append(f"{fn} uid {d.get('uid')}")
    for b in banned:
        if b in raw: fails.append(f"{fn} still contains {b}")
    if (d.get('templating') or {}).get('list'): fails.append(f"{fn} has template variables")
    if re.search(r'\$\{?DS_', raw): fails.append(f"{fn} has a DS_ placeholder")
if 'namespace' not in open('k8s/monitoring/configs/dashboards/cadvisor.json').read(): fails.append("cadvisor has no namespace label usage")
if fails:
    print("\n".join("FAIL: " + x for x in fails)); sys.exit(1)
print("TASK 3 PASS")
PY
    </automated>
    <fails_when>Non-zero exit or any `FAIL:` line. Specifically:
- the dual-scope gate or its selftest fails;
- a k8s dashboard's uid differs from its public-share uid;
- a k8s dashboard still contains a Docker-era literal, a template variable or a `DS_` placeholder;
- the cadvisor dashboard never uses the `namespace` label.</fails_when>
  </verify>
  <acceptance_criteria>
    - All three k8s dashboards pass every public-dashboard invariant in the k8s scope, and the Compose scope still passes unchanged.
    - The configMapGenerator produces exactly three dashboard ConfigMaps, each labelled `grafana_dashboard: "1"`.
    - `git diff --stat docker/grafana` is empty.
  </acceptance_criteria>
  <done>All three public dashboards have Kubernetes-label versions ready to activate in 13-07, gated pre-merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Helm chart repos → Flux | Third-party chart content rendered in-cluster |
| Internet → Grafana (from 13-07) | Public login page and public dashboards |
| Monitoring namespace → Postgres | Exporter holds a pg_monitor credential |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-12 | Tampering | Helm charts pulled by Flux | medium | mitigate | Exact chart versions pinned; charts from the upstream prometheus-community/grafana repos (Research legitimacy audit: Approved); rendering statically inspected with `helm template` before activation. |
| T-13-13 | Information Disclosure | Public dashboards leaking via template variables / wrong datasource | medium | mitigate | Dual-scope verify-public-dashboards.py (literal UIDs, no variables, no Docker-era literals) gates the k8s copies pre-merge. |
| T-13-14 | Spoofing | Grafana login brute force | medium | mitigate | `grafana-login-rate-limit` Middleware on `/login` (re-derived in 13-08); sign-up and anonymous access disabled; admin password from Secret. |
| T-13-15 | Elevation of Privilege | Log collector host access | low | mitigate | Alloy uses `loki.source.kubernetes` (API) with no hostPath; node-exporter has no hostNetwork/hostPID; no docker.sock anywhere. |
| T-13-16 | Information Disclosure | Exporter DB credential | low | mitigate | `monitoring` role holds only pg_monitor + CONNECT; password via Secret (D-10); NetworkPolicy admits only the labelled exporter pod (13-04). |
| T-13-SC | Tampering | Chart images | medium | mitigate | Upstream first-party charts; Grafana image pinned to 13.2.1; exporter image pinned to v0.20.1. |
</threat_model>

<verification>
1. All four HelmReleases render with pinned charts, with no Alertmanager, no control-plane monitors, no host mounts.
2. The dual-scope public-dashboard gate passes; Compose dashboards are untouched.
3. Manifest and invariant gates pass; the monitoring Kustomizations remain suspended (13-02).
</verification>

<success_criteria>
- 13-07 can activate observability by unsuspending two Kustomizations and creating two Secrets, without authoring anything.
</success_criteria>

<output>
Create `.planning/phases/13-introduce-kubernetes/13-03-SUMMARY.md` when done.
</output>
