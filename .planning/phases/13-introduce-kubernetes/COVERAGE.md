No external API integration: Phase 13 migrates the runtime and deploy infrastructure (Compose → k3s, Flux, Traefik, cert-manager, kube-prometheus-stack). The detector matched "Kubernetes API" in a threat-model row. The external services touched — the Let's Encrypt ACME endpoint via cert-manager, Docker Hub tag listing via Flux's image-reflector, GitHub over an SSH deploy key, and Grafana's own public-dashboard endpoint — are consumed through standard off-the-shelf controllers or one-off operator calls. No product capability wraps an external API surface.
