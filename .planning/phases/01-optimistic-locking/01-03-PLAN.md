---
phase: 01-optimistic-locking
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - docs/plans/backend-modernization/02-optimistic-locking-ddl.sql
  - docs/plans/backend-modernization/STATUS.md
autonomous: false
requirements: [LOCK-01]
user_setup:
  - service: postgres
    why: "The real Postgres schema has ddl-auto unset, so Hibernate will NOT create the new version column automatically — a human must run the delivered ALTER TABLE script manually against the live DB right before merge/deploy (D-06/D-07)."
    dashboard_config:
      - task: "Run the delivered ALTER TABLE script against the real Postgres instance immediately before merging/deploying this phase's PR (auto-deploy on push to master means skipping it breaks production)."
        location: "psql against the production/real Postgres database, using docs/plans/backend-modernization/02-optimistic-locking-ddl.sql"
estimate:
  tokens: 30000
  raw_tokens: 15000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A ready-to-run SQL script exists containing ALTER TABLE tasks ADD COLUMN version bigint NOT NULL DEFAULT 0; and the equivalent for the columns table (LOCK-01, D-06, D-07)."
    - "The DDL is delivered as an actual runnable file (not prose), placed under docs/plans/backend-modernization/ following that directory's naming (D-07, Claude's discretion on exact location)."
    - "STATUS.md records that this manual DDL is a one-off bridge step for this phase, to be run manually right before merge/deploy, and is NOT a replacement for Epic 3's Flyway work."
    - "A blocking decision checkpoint precedes the DDL deliverable because D-06 is rated one-way (skipping the manual run breaks production on the next auto-deploy)."
  prohibitions:
    - statement: "The DDL step must NOT be silently deferred to Epic 3 (Flyway) — that would leave production broken between merge and Epic 3 landing (auto-deploy on push to master)."
      verification: "STATUS.md explicitly states the DDL is run manually this phase, before merge; the flag-raised-and-resolved note from CONTEXT.md D-07 is reflected."
    - statement: "The column table's default value must NOT be omitted — existing rows need a concrete version so @Version(nullable=false) does not fail on pre-existing data."
      verification: "Both ALTER TABLE statements include NOT NULL DEFAULT 0."
  artifacts:
    - docs/plans/backend-modernization/02-optimistic-locking-ddl.sql
    - docs/plans/backend-modernization/STATUS.md
  key_links:
    - "The version column added by this DDL is the persistent backing for the @Version fields added in Plan 01; without it, any Task/Column request against the real DB hits a missing-column SQL error (D-06 one-way)."
    - "H2 test profile is unaffected (tests create schema from entities), so this DDL is only for the real Postgres instance — the test suite proves the code, this script provisions production."
---

<objective>
Deliver the exact, ready-to-run manual DDL that adds the `version` column to the real Postgres `tasks` and `columns` tables, and record in STATUS.md that it is a one-off bridge step run manually right before merge/deploy (D-06/D-07). Because D-06 is rated one-way (auto-deploy on push to master means a missed manual run breaks production immediately), this plan leads with a blocking decision checkpoint confirming the DDL script is correct and the operator understands the manual-run obligation.

Purpose: ddl-auto is unset in the real Postgres profile, so Hibernate will not create the version column. The @Version fields from Plan 01 are inert (and actively break production) against the real DB until this column exists. This provides the concrete, runnable artifact and the operational note so the step is not lost.
Output: docs/plans/backend-modernization/02-optimistic-locking-ddl.sql; a STATUS.md note capturing the one-off-bridge decision and the flag-raised-and-resolved rationale.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-optimistic-locking/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/SUMMARY.md
@docs/plans/backend-modernization/STATUS.md
@docs/plans/backend-modernization/README.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Confirm the manual DDL script is correct and the operator accepts the one-way obligation to run it manually against the real Postgres schema immediately before merge/deploy.</decision>
  <context>
    D-06 is rated one-way in CONTEXT.md: the real Postgres profile has ddl-auto unset, so the version column is NOT created automatically. master auto-deploys to EC2 on every push (.github/workflows/deploy.yml). If this ALTER TABLE is not run before the PR merges, the next auto-deploy leaves production broken — any request touching Task or Column hits a missing-column SQL error. This is why the DDL is delivered as a runnable file this phase (not deferred to Epic 3's Flyway work, which would leave a broken window between merge and Epic 3). Confirm before the script is committed and the manual-run step is baked into the merge checklist.
  </context>
  <options>
    <option id="deliver-sql-file">
      <name>Deliver as a standalone .sql file under docs/plans/backend-modernization/ (recommended, D-07)</name>
      <pros>Directly runnable via psql; lives beside the epic docs; unambiguous artifact; matches D-07's "ready-to-run script" intent.</pros>
      <cons>Operator must remember to run it manually at merge time (mitigated by the STATUS.md note + PR checklist).</cons>
    </option>
    <option id="documented-command">
      <name>Deliver as a documented exact command inside STATUS.md only</name>
      <pros>One fewer file.</pros>
      <cons>Weaker than D-07's "script or exact documented command, not just prose" bar; easier to overlook; not directly executable as a file.</cons>
    </option>
  </options>
  <resume-signal>Select: deliver-sql-file (recommended) or documented-command, and confirm you accept running the DDL manually before merge/deploy.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Create the ready-to-run ALTER TABLE DDL script and record the one-off-bridge decision in STATUS.md</name>
  <files>
    docs/plans/backend-modernization/02-optimistic-locking-ddl.sql,
    docs/plans/backend-modernization/STATUS.md
  </files>
  <read_first>
    - docs/plans/backend-modernization/STATUS.md (existing "Notes / decisions log" format to append to; Epic 2 entries)
    - docs/plans/backend-modernization/README.md (one-epic-per-PR discipline; where the DDL fits)
    - .planning/research/ARCHITECTURE.md (confirms ddl-auto unset in the real application.properties, not just test)
    - CONTEXT.md D-06 (one-way — manual ALTER TABLE required on tasks and columns), D-07 (deliverable script, run before merge, NOT deferred to Epic 3), and the "Flag raised and resolved during discussion" note
    - CONTEXT.md Claude's Discretion (exact DDL script format/location — pick what fits docs/plans/backend-modernization/)
  </read_first>
  <action>
    Create docs/plans/backend-modernization/02-optimistic-locking-ddl.sql containing exactly the two statements required (idempotency-friendly form preferred): `ALTER TABLE tasks ADD COLUMN version bigint NOT NULL DEFAULT 0;` and `ALTER TABLE columns ADD COLUMN version bigint NOT NULL DEFAULT 0;` (use `ADD COLUMN IF NOT EXISTS` so a second accidental run is safe — this addresses the LOCK-01 idempotency backstop). Include a short header comment block stating: this is a one-off manual bridge step for Epic 2 optimistic locking; ddl-auto is unset in the real Postgres profile so Hibernate will not create these columns; run manually via psql against the real DB right before merging/deploying this phase's PR; NOT a replacement for Epic 3's Flyway migrations. Append a dated note to STATUS.md's "Notes / decisions log" recording: the manual DDL deliverable and its path, that it must be run before merge (one-way per D-06 — auto-deploy on push to master), the flag-raised-and-resolved rationale from D-07 (initial instinct to defer to Epic 3 was rejected because it leaves production broken between merge and Epic 3), and that Epic 3 must not silently re-apply or lose this manual step in migration history. Do not run any SQL — this is a deliverable + documentation task only.
  </action>
  <precondition>The version column does not yet exist in the real Postgres schema; the operator will run this script manually before merge (asserted operationally, not by this task).</precondition>
  <verify>
    <automated>cd C:/Dev/Repos/kanban-board-backend && grep -Eiq "alter table +tasks +add column( if not exists)? +version +bigint +not null +default +0" docs/plans/backend-modernization/02-optimistic-locking-ddl.sql && grep -Eiq "alter table +columns +add column( if not exists)? +version +bigint +not null +default +0" docs/plans/backend-modernization/02-optimistic-locking-ddl.sql && grep -iq "flyway\|epic 3\|before merge\|one-off" docs/plans/backend-modernization/STATUS.md</automated>
  </verify>
  <reversibility rating="one-way">D-06: skipping the manual run before deploy breaks production immediately (missing-column SQL error on any Task/Column request), because master auto-deploys to EC2 on push. The blocking decision checkpoint above guards this.</reversibility>
  <acceptance_criteria>
    - docs/plans/backend-modernization/02-optimistic-locking-ddl.sql contains both `ALTER TABLE ... ADD COLUMN version bigint NOT NULL DEFAULT 0` statements (tasks and columns), preferably with `IF NOT EXISTS`.
    - The script header states it is a one-off manual bridge, run before merge, not an Epic 3 Flyway replacement.
    - STATUS.md's decisions log has a dated entry recording the manual DDL deliverable, its one-way merge-time obligation, and the defer-to-Epic-3-rejected rationale.
  </acceptance_criteria>
  <done>A ready-to-run DDL script exists for both tables and STATUS.md records the one-off-bridge decision and its rationale; no SQL executed by the agent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → real Postgres schema | Manual DDL execution crosses here; the only human-privileged mutation in this phase. |

## STRIDE Threat Register (ASVS Level 1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Denial of Service | missing version column on auto-deploy | high | mitigate | Blocking decision checkpoint + STATUS.md merge-checklist note ensure the DDL is run manually before the PR merges, preventing the missing-column production outage (D-06 one-way). |
| T-01-10 | Tampering | accidental double-run of the DDL | low | mitigate | Script uses `ADD COLUMN IF NOT EXISTS ... DEFAULT 0`, so a second run is a no-op and pre-existing rows get a concrete non-null version (idempotency backstop). |
</threat_model>

<verification>
- The .sql file contains both ALTER TABLE statements with `version bigint NOT NULL DEFAULT 0`.
- STATUS.md records the one-off manual DDL, its before-merge obligation, and the Epic-3-defer-rejected rationale.
- No SQL was executed by the agent (deliverable-only).
</verification>

<success_criteria>
- Ready-to-run DDL script delivered for both `tasks` and `columns` (LOCK-01, D-06, D-07).
- Operational note in STATUS.md so the one-way manual step is not lost.
- Blocking decision checkpoint confirms operator accepts the manual-run obligation before the script is committed.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)
- New file `docs/plans/backend-modernization/02-optimistic-locking-ddl.sql` (two `ALTER TABLE ... ADD COLUMN version bigint NOT NULL DEFAULT 0` statements).
- Appended decisions-log entry in `docs/plans/backend-modernization/STATUS.md` (one-off manual DDL bridge; run before merge; not an Epic 3 Flyway replacement).
</artifacts_produced>

<output>
Create `.planning/phases/01-optimistic-locking/01-03-SUMMARY.md` when done
</output>
