---
phase: 01-optimistic-locking
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/main/java/com/vrudenko/kanban_board/dto/column_dto/UpdateColumnRequestDTO.java
  - src/main/java/com/vrudenko/kanban_board/dto/column_dto/ColumnResponseDTO.java
  - src/main/java/com/vrudenko/kanban_board/service/ColumnService.java
  - src/main/java/com/vrudenko/kanban_board/controller/ColumnController.java
  - src/test/java/com/vrudenko/kanban_board/controller/ColumnControllerTest.java
  - src/test/java/com/vrudenko/kanban_board/e2e/column/ColumnLockingE2ETest.java
autonomous: true
requirements: [LOCK-01, LOCK-02, LOCK-03]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "ColumnController exposes PUT /boards/{boardId}/columns/{columnId} mapped to a new updateById method (D-04)."
    - "ColumnService.updateById loads the ownership-verified column via findById, compares dto.getVersion() against column.getVersion() BEFORE mutating, throws OptimisticLockingFailureException on mismatch, then sets name and saves (D-02, D-04)."
    - "Of two concurrent conflicting updates to the same column, exactly one succeeds and the other receives HTTP 409 (LOCK-02, LOCK-03 concurrency guarantee for Column)."
    - "UpdateColumnRequestDTO is a new class with a required (@NotBlank/@Size) name and a required (@NotNull) Long version (D-02, D-04)."
    - "ColumnResponseDTO exposes a Long version field (D-01)."
    - "The bulk-delete / @Version-bypass tradeoff and the ColumnRepository.deleteAllByBoardId derived-vs-bulk asymmetry are explicitly documented (Claude's discretion on form: Javadoc on TaskService.deleteAllByColumn and/or a STATUS.md note)."
    - "An automated test drives a stale-version column update and asserts HTTP 409 at the status-code level (LOCK-03)."
    - "A stale-version column update retried after a 409 without refetching returns 409 again, not a silent success (LOCK-02 idempotency backstop)."
  prohibitions:
    - statement: "The version field on UpdateColumnRequestDTO must NOT become effectively optional/ignorable server-side."
      verification: "UpdateColumnRequestDTO.version carries @NotNull; a test submitting a column update with no version returns HTTP 400 (flagged-unverified until asserted)."
    - statement: "The new ColumnController.updateById must NOT allow a user to update another user's column."
      verification: "ColumnService.updateById calls findById → OwnershipVerifierService.verifyOwnershipOfColumn before any mutation; flagged-unverified — a not-owner test should return 401/404, mirroring existing controller-test conventions."
    - statement: "The existing bulk-delete paths (TaskService.deleteAllByColumn, SubtaskRepository.deleteAllByTaskIdIn) must NOT start silently catching/swallowing OptimisticLockingFailureException — the accepted tradeoff is that they bypass version checks entirely (bulk JPQL never loads entities), documented, not defensively caught."
      verification: "No new try/catch around OptimisticLockingFailureException is introduced in TaskService/SubtaskService bulk-delete methods; the bypass is documented as an accepted delete-wins tradeoff (flagged-unverified — reviewer confirms)."
  artifacts:
    - src/main/java/com/vrudenko/kanban_board/dto/column_dto/UpdateColumnRequestDTO.java
    - src/main/java/com/vrudenko/kanban_board/dto/column_dto/ColumnResponseDTO.java
    - src/main/java/com/vrudenko/kanban_board/service/ColumnService.java
    - src/main/java/com/vrudenko/kanban_board/controller/ColumnController.java
    - src/test/java/com/vrudenko/kanban_board/e2e/column/ColumnLockingE2ETest.java
  key_links:
    - "ColumnService.updateById throws OptimisticLockingFailureException → GlobalExceptionHandler (fixed in Plan 01) → 409. This plan reuses the handler fixed in 01-01; do not re-fix it."
    - "@Version on ColumnEntity (added in 01-01) is what makes the Column update path's version check meaningful and Hibernate-enforced at flush; this plan depends on that field existing."
    - "ColumnService.updateById → findById → OwnershipVerifierService.verifyOwnershipOfColumn: ownership gate before version comparison (security key link)."
---

<objective>
Expand the proven Task locking tracer to the Column entity: add the missing Column update endpoint (D-04 — ColumnController currently has NO update endpoint) so ColumnEntity's @Version (added in Plan 01) is actually reachable and testable through the API, apply the same explicit version-check pattern in a new ColumnService.updateById, expose version on ColumnResponseDTO, and explicitly document the bulk-delete/@Version-bypass tradeoff carried from research.

Purpose: Without a Column update endpoint, ColumnEntity's @Version would only be defensively present, never exercised. This makes column concurrency conflicts real, testable, and 409-mapped, and closes the documented-tradeoff obligation.
Output: UpdateColumnRequestDTO; ColumnController.updateById (PUT /boards/{boardId}/columns/{columnId}); ColumnService.updateById with explicit version check; version on ColumnResponseDTO; a Column 409 conflict test; documented bulk-delete tradeoff.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-optimistic-locking/01-CONTEXT.md
@.planning/phases/01-optimistic-locking/01-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/phases/01-optimistic-locking/01-01-SUMMARY.md

# Ground-truth source files
@src/main/java/com/vrudenko/kanban_board/controller/ColumnController.java
@src/main/java/com/vrudenko/kanban_board/service/ColumnService.java
@src/main/java/com/vrudenko/kanban_board/dto/column_dto/ColumnResponseDTO.java
@src/main/java/com/vrudenko/kanban_board/controller/TaskController.java
@src/main/java/com/vrudenko/kanban_board/service/TaskService.java
@src/main/java/com/vrudenko/kanban_board/constant/ApiPaths.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add UpdateColumnRequestDTO, version on ColumnResponseDTO, and ColumnService.updateById with explicit version check</name>
  <files>
    src/main/java/com/vrudenko/kanban_board/dto/column_dto/UpdateColumnRequestDTO.java,
    src/main/java/com/vrudenko/kanban_board/dto/column_dto/ColumnResponseDTO.java,
    src/main/java/com/vrudenko/kanban_board/service/ColumnService.java
  </files>
  <read_first>
    - PATTERNS.md "dto/column_dto/UpdateColumnRequestDTO.java (new file)" section (lines 138-175) — annotation stack + validation constants to reuse from SaveColumnRequestDTO
    - PATTERNS.md "service/ColumnService.java — new updateById" section (lines 236-288) — the explicit version-check excerpt (lines 268-288) is the exact pattern
    - PATTERNS.md "dto/task_dto/TaskResponseDTO.java / ColumnResponseDTO.java" section (D-01: add version to both)
    - src/main/java/com/vrudenko/kanban_board/service/ColumnService.java (findById at lines 56-61 — ownership-verified, reuse as-is; @Autowired field injection convention)
    - src/main/java/com/vrudenko/kanban_board/dto/column_dto/ColumnResponseDTO.java (current: id, name)
    - CONTEXT.md D-02 (client-supplied required version, server-compared), D-04 (Column only mutable field is name), D-05 (plain-string message, no id)
    - 01-01-SUMMARY.md (confirm @Version already on ColumnEntity + handler already returns 409; do NOT re-fix the handler)
  </read_first>
  <behavior>
    - ColumnService.updateById with a matching version updates name, saves, and returns a ColumnResponseDTO whose version is incremented.
    - ColumnService.updateById with a non-matching version throws OptimisticLockingFailureException before mutating name.
    - A client cannot reach updateById for a column they do not own: findById → verifyOwnershipOfColumn rejects first.
    - UpdateColumnRequestDTO with a blank name fails validation; with a null version fails validation.
  </behavior>
  <action>
    Create UpdateColumnRequestDTO in package com.vrudenko.kanban_board.dto.column_dto implementing BaseColumn, following the annotation stack from PATTERNS.md (@Getter @Setter @Builder @EqualsAndHashCode @JsonInclude(NON_NULL)): a required name field reusing the same @NotBlank + @Size(min=ValidationConstants.MIN_COLUMN_NAME_LENGTH, max=ValidationConstants.MAX_COLUMN_NAME_LENGTH, message=ValidationConstants.NAME_LENGTH_VALIDATION_MESSAGE) constraints as SaveColumnRequestDTO, plus a @NotNull Long version. Add a Long version field to ColumnResponseDTO (D-01; MapStruct auto-maps by name). Add a new ColumnService.updateById(String userId, String columnId, UpdateColumnRequestDTO dto) method mirroring TaskService.updateById's pattern (from Plan 01): @Transactional, load the managed column via the existing findById (ownership-verified — do NOT bypass), then BEFORE mutating, compare dto.getVersion() against column.getVersion() and throw org.springframework.dao.OptimisticLockingFailureException with a plain message ("Column was modified by another request, please refetch." — D-05, no id) on mismatch; then setName, columnRepository.save(column), return columnMapper.toColumnResponseDTO(column). Keep @Autowired field injection. Run ./gradlew spotlessApply before verifying.
  </action>
  <verify>
    <automated>cd C:/Dev/Repos/kanban-board-backend && ./gradlew compileJava && grep -q "OptimisticLockingFailureException" src/main/java/com/vrudenko/kanban_board/service/ColumnService.java && grep -q "private Long version" src/main/java/com/vrudenko/kanban_board/dto/column_dto/ColumnResponseDTO.java</automated>
  </verify>
  <reversibility rating="costly">D-02 applied to Column: the explicit version check is the actual stale-read-conflict mechanism; removing it silently disables column optimistic-locking protection.</reversibility>
  <acceptance_criteria>
    - UpdateColumnRequestDTO exists with a validated name and a `@NotNull Long version`.
    - ColumnResponseDTO has a `Long version`.
    - ColumnService.updateById throws OptimisticLockingFailureException when versions differ, before setName runs, after ownership-verified findById.
    - Project compiles.
  </acceptance_criteria>
  <done>Column update service layer exists with the explicit version-check pattern and version exposed on the response DTO; compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ColumnController.updateById endpoint + column locking tests (controller 409 + E2E 409)</name>
  <files>
    src/main/java/com/vrudenko/kanban_board/controller/ColumnController.java,
    src/test/java/com/vrudenko/kanban_board/controller/ColumnControllerTest.java,
    src/test/java/com/vrudenko/kanban_board/e2e/column/ColumnLockingE2ETest.java
  </files>
  <read_first>
    - PATTERNS.md "controller/ColumnController.java — new updateById" section (lines 208-232) — exact @PutMapping shape + required imports
    - src/main/java/com/vrudenko/kanban_board/controller/ColumnController.java (current: @RequestMapping BOARDS+BOARD_ID+COLUMNS; GetMapping + PostMapping(COLUMN_ID) only)
    - src/main/java/com/vrudenko/kanban_board/controller/TaskController.java (updateById at lines 45-51 — the exact analog shape)
    - src/test/java/com/vrudenko/kanban_board/controller/ColumnControllerTest.java (existing test conventions for this controller)
    - PATTERNS.md "Test harness / mock fixtures" (lines 337-369) and "Route path constants" (reuse ApiPaths.COLUMN_ID)
    - src/test/java/com/vrudenko/kanban_board/AbstractAppE2ETest.java (signin() + RestAssured RANDOM_PORT harness)
    - CONTEXT.md D-04 (route PUT /boards/{boardId}/columns/{columnId})
  </read_first>
  <behavior>
    - PUT /boards/{boardId}/columns/{columnId} with a valid body and matching version → 200 with updated ColumnResponseDTO.
    - Two concurrent PUTs to the same column with the same starting version → first 200, second 409.
    - Re-submitting a stale-version column PUT after a 409 → 409 again (idempotency backstop).
    - A column update with no version → 400 (version required).
  </behavior>
  <action>
    Add a @PutMapping(ApiPaths.COLUMN_ID) updateById method to ColumnController mirroring TaskController.updateById (lines 45-51): parameters (@CurrentUserId String userId, @PathVariable @NotBlank String columnId, @Valid @RequestBody UpdateColumnRequestDTO dto), returning ResponseEntity.ok(columnService.updateById(userId, columnId, dto)). Add the needed imports (PutMapping, UpdateColumnRequestDTO). The resulting route is PUT /boards/{boardId}/columns/{columnId} (D-04). Add controller-level tests in ColumnControllerTest: a success (200) test and a shouldReturnConflict_whenVersionIsStale (409) test, following the MockMvc perform(put(url)...) style used elsewhere. Create src/test/java/com/vrudenko/kanban_board/e2e/column/ColumnLockingE2ETest.java as @SpringBootTest(webEnvironment = RANDOM_PORT) extending AbstractAppE2ETest, driving two sequential real HTTP PUTs to the same column with the same starting version via signin() + RestAssured, asserting first 200 / second 409, plus a re-submitted-stale → 409 assertion (LOCK-03 status-level + idempotency backstop). Run ./gradlew spotlessApply before verifying.
  </action>
  <verify>
    <automated>cd C:/Dev/Repos/kanban-board-backend && ./gradlew test --tests "com.vrudenko.kanban_board.e2e.column.ColumnLockingE2ETest" --tests "com.vrudenko.kanban_board.controller.ColumnControllerTest"</automated>
  </verify>
  <acceptance_criteria>
    - ColumnController has a @PutMapping(ApiPaths.COLUMN_ID) updateById resolving to PUT /boards/{boardId}/columns/{columnId}.
    - ColumnControllerTest asserts status().isConflict() on a stale-version column update.
    - ColumnLockingE2ETest passes: concurrent stale column update → 409; re-submitted stale → 409.
  </acceptance_criteria>
  <done>Column update endpoint is reachable via HTTP and a concurrent column conflict returns 409 end-to-end, proven by passing controller and E2E tests.</done>
</task>

<task type="auto">
  <name>Task 3: Document the bulk-delete / @Version-bypass tradeoff and the deleteAllByBoardId derived-vs-bulk asymmetry</name>
  <files>
    src/main/java/com/vrudenko/kanban_board/service/TaskService.java,
    src/main/java/com/vrudenko/kanban_board/service/ColumnService.java
  </files>
  <read_first>
    - src/main/java/com/vrudenko/kanban_board/service/TaskService.java (existing Javadoc on deleteAllByColumn at lines 96-106 — extend it, do NOT rewrite the bulk logic)
    - src/main/java/com/vrudenko/kanban_board/service/ColumnService.java (deleteAllByBoardId at lines 28-37 — derived deleteAllByBoardId is fetch-then-remove-per-entity, so it WILL honor @Version, unlike the bulk task path)
    - .planning/research/PITFALLS.md Pitfall 5 (bulk delete bypasses @Version — accepted delete-wins tradeoff), Pitfall 6 (ColumnRepository.deleteAllByBoardId derived-vs-bulk asymmetry), Pitfall 10 (bulk-update boundary)
    - CONTEXT.md Claude's Discretion (documentation form is your choice: code comment vs STATUS.md; must be explicit, not silently implicit)
    - .planning/STATE.md Blockers/Concerns (both carried from research — must be documented this phase, not deferred)
  </read_first>
  <action>
    Extend the existing Javadoc on TaskService.deleteAllByColumn to explicitly state that the bulk JPQL delete (taskRepository.deleteAllByIdInBatch + SubtaskRepository.deleteAllByTaskIdIn) bypasses @Version checking entirely by design — bulk statements never load entities, so there is no version to compare — and that this is an ACCEPTED delete-wins tradeoff (a delete racing a version-mismatched update discards the update's effect on a row that is being deleted anyway, which is correct behavior for a delete). Do NOT add per-row version clauses and do NOT wrap the bulk deletes in a try/catch for OptimisticLockingFailureException (per the prohibition — the bypass is documented, not defensively caught). Add a short note (Javadoc on ColumnService.deleteAllByBoardId, or a comment) recording the asymmetry: ColumnRepository.deleteAllByBoardId is a DERIVED (fetch-then-remove) delete, so once ColumnEntity has @Version it WILL honor version checks per-entity — unlike the sibling bulk task-delete path which does not. Keep it to documentation only (no behavior change). Run ./gradlew spotlessApply before verifying.
  </action>
  <verify>
    <automated>cd C:/Dev/Repos/kanban-board-backend && grep -iq "version" src/main/java/com/vrudenko/kanban_board/service/TaskService.java && ./gradlew spotlessCheck && ./gradlew test</automated>
  </verify>
  <acceptance_criteria>
    - TaskService.deleteAllByColumn Javadoc explicitly states the bulk-delete @Version bypass is an accepted delete-wins tradeoff.
    - The deleteAllByBoardId derived-vs-bulk asymmetry is documented (comment or Javadoc).
    - No try/catch swallowing OptimisticLockingFailureException was added to any bulk-delete path.
    - `./gradlew spotlessCheck` and `./gradlew test` pass.
  </acceptance_criteria>
  <done>Both research-carried tradeoffs are explicitly documented in code, behavior unchanged, full suite and format check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → ColumnController.updateById | NEW endpoint — untrusted JSON body (name + client-supplied version) crosses here for the first time. |
| ColumnService.updateById → OwnershipVerifierService | Ownership gate; must run before any mutation or version comparison. |

## STRIDE Threat Register (ASVS Level 1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Elevation of Privilege | new ColumnController.updateById | high | mitigate | ColumnService.updateById calls findById → OwnershipVerifierService.verifyOwnershipOfColumn before any mutation, reusing the exact ownership pattern of TaskService — a user cannot update another user's column. Verified by a not-owner test returning 401/404. |
| T-01-06 | Tampering | client-supplied `version` on UpdateColumnRequestDTO | medium | mitigate | Version compared only against the ownership-verified managed column; cannot bypass ownership. |
| T-01-07 | Information Disclosure | Column 409 body | low | mitigate | Plain-string message, no column id/type embedded (D-05). |
| T-01-08 | Tampering | bulk-delete paths silently swallowing conflicts | medium | mitigate | No try/catch around OptimisticLockingFailureException added to bulk-delete methods; bypass is documented, not hidden (prohibition enforced). |
</threat_model>

<verification>
- `./gradlew test --tests "com.vrudenko.kanban_board.e2e.column.ColumnLockingE2ETest"` passes (409 on concurrent stale column update).
- `./gradlew test --tests "com.vrudenko.kanban_board.controller.ColumnControllerTest"` passes.
- `./gradlew spotlessCheck` and `./gradlew test` both pass.
- grep confirms ColumnService.updateById throws OptimisticLockingFailureException and ColumnResponseDTO carries version.
- Bulk-delete tradeoff and deleteAllByBoardId asymmetry documented in service Javadoc/comments.
</verification>

<success_criteria>
- New PUT /boards/{boardId}/columns/{columnId} endpoint exists and is ownership-verified (D-04).
- Concurrent conflicting column update returns HTTP 409 (LOCK-02, LOCK-03).
- version exposed on ColumnResponseDTO (D-01); UpdateColumnRequestDTO required version (D-02).
- Bulk-delete/@Version-bypass tradeoff and deleteAllByBoardId asymmetry explicitly documented.
- Full suite and format check green.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)
- New class `UpdateColumnRequestDTO` (validated `name` + `@NotNull Long version`).
- New `version` field on `ColumnResponseDTO`.
- New `ColumnController.updateById` method (`PUT /boards/{boardId}/columns/{columnId}`).
- New `ColumnService.updateById` method (explicit version-check-before-save, ownership-verified).
- New test class `ColumnLockingE2ETest` (concurrent column update → 409).
- New/updated tests in `ColumnControllerTest` (success + stale-version 409).
- Extended Javadoc on `TaskService.deleteAllByColumn` (bulk-delete @Version-bypass tradeoff) and documentation of the `ColumnService.deleteAllByBoardId` derived-vs-bulk asymmetry.
</artifacts_produced>

<output>
Create `.planning/phases/01-optimistic-locking/01-02-SUMMARY.md` when done
</output>
