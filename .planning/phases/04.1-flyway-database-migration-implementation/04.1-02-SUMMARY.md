---
phase: 04.1-flyway-database-migration-implementation
plan: 02
subsystem: database
tags: [flyway, postgresql, migrations, optimistic-locking, activity-log, schema]

requires:
  - phase: 04.1-flyway-database-migration-implementation (plan 01)
    provides: Flyway wired end-to-end, V1__init.sql applied and proven against real docker-compose Postgres, spring.flyway.enabled=false in test profile
provides:
  - V2__add_optimistic_locking_version_columns.sql (tasks.version, columns.version)
  - V3__add_activity_log.sql (activity_log table + composite index)
  - V4__add_password_hash_not_null.sql (guarded users.password_hash NOT NULL)
  - Proven ordered migration history (V1-V4) applied against a genuinely empty Postgres volume via docker compose
affects: [04.1-03 (ddl-auto=validate cutover), phase-5-infra-migration]

actuals:
  tokens: 12000
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Flyway migrations ported from manual DDL bridge scripts with idempotency guards (IF NOT EXISTS) stripped, since flyway_schema_history already guarantees exactly-once application"
    - "Data-state guards (V4's NULL-count RAISE EXCEPTION) are kept even when schema-state guards are stripped, since Flyway's history table cannot protect against bad existing data"

key-files:
  created:
    - src/main/resources/db/migration/V2__add_optimistic_locking_version_columns.sql
    - src/main/resources/db/migration/V3__add_activity_log.sql
    - src/main/resources/db/migration/V4__add_password_hash_not_null.sql
  modified: []

key-decisions:
  - "Ported all three manual DDL scripts verbatim in content but stripped their IF NOT EXISTS/idempotency framing (Approach A from the plan's trade-off matrix) — Flyway's checksummed history table replaces that hand-rolled safety net"
  - "Kept V4's pre-flight NULL-count guard and RAISE EXCEPTION — it protects against data state, which Flyway's schema-history mechanism has no visibility into"
  - "Did not touch docker-compose.yml's SPRING_JPA_HIBERNATE_DDL_AUTO: update override — that's explicitly Plan 03's job (ddl-auto=validate cutover), not this plan's"

patterns-established:
  - "Middle-migration porting pattern: read the manual DDL script → strip IF NOT EXISTS / re-run framing → add a one-line header comment naming the historical origin → verify column set against the entity class"

requirements-completed: []

coverage:
  - id: D1
    description: "V2 and V3 migrations exist, ported from the manual DDL scripts, with no idempotency guards and no destructive statements"
    verification:
      - kind: other
        ref: "grep -civ 'if not exists'/'drop '/'truncate'/'delete from' over V2/V3 non-comment body, all return 0; grep for uk_activity_log_event_id and idx_activity_log_board_created_id in V3, both found"
        status: pass
    human_judgment: false
  - id: D2
    description: "V4 migration exists with a PL/pgSQL DO block, a RAISE EXCEPTION NULL-count guard, and exactly one ALTER TABLE users ALTER COLUMN password_hash SET NOT NULL"
    verification:
      - kind: other
        ref: "grep -qi 'RAISE EXCEPTION' V4__add_password_hash_not_null.sql; grep -civ 'if not exists' returns 0"
        status: pass
    human_judgment: false
  - id: D3
    description: "All four migrations (V1-V4) apply cleanly and in order against a genuinely empty Postgres volume via docker compose down -v && up --build, producing the exact schema the current entities expect"
    verification:
      - kind: manual_procedural
        ref: "docker compose down -v && docker compose up -d --build; docker compose logs app contains 'Started KanbanBoardApplication'; SELECT version, success FROM flyway_schema_history ORDER BY installed_rank returned 1|t, 2|t, 3|t, 4|t; SELECT is_nullable, data_type FROM information_schema.columns confirmed tasks.version/columns.version are bigint NOT NULL and users.password_hash is NOT NULL; SELECT indexname FROM pg_indexes WHERE tablename='activity_log' returned activity_log_pkey, uk_activity_log_event_id, idx_activity_log_board_created_id; SELECT count(*) FROM information_schema.tables WHERE table_name='activity_log' returned 1"
        status: pass
    human_judgment: false

duration: 45min
completed: 2026-08-05
status: complete
---

# Phase 04.1 Plan 02: V2-V4 Flyway migrations Summary

**Ported the three manual DDL bridge scripts (optimistic-locking version columns, activity_log, password_hash NOT NULL) into checksummed Flyway migrations V2-V4, proven end-to-end against a genuinely empty docker-compose Postgres volume alongside Plan 01's V1 baseline.**

## Performance

- **Duration:** ~45 min (including two ~3.5min pre-commit hook test runs and a ~4.5min docker compose build)
- **Started:** 2026-08-05T13:45:00+02:00 (approx)
- **Completed:** 2026-08-05T14:01:07+02:00
- **Tasks:** 2
- **Files modified:** 3 (all new)

## Accomplishments
- `V2__add_optimistic_locking_version_columns.sql` adds `tasks.version` and `columns.version` (`bigint NOT NULL DEFAULT 0`), ported from `docs/plans/backend-modernization/02-optimistic-locking-ddl.sql` with the `IF NOT EXISTS` guards stripped.
- `V3__add_activity_log.sql` creates the `activity_log` table plus `uk_activity_log_event_id` and `idx_activity_log_board_created_id`, ported from `03-activity-log-ddl.sql`.
- `V4__add_password_hash_not_null.sql` adds the guarded `users.password_hash SET NOT NULL` constraint via a PL/pgSQL `DO` block that keeps its pre-flight `NULL`-count `RAISE EXCEPTION` guard, ported from `04-password-hash-not-null-ddl.sql`.
- Full migration history (V1-V4) proven by `docker compose down -v && docker compose up -d --build`: `flyway_schema_history` shows four rows (versions 1-4, all `success = t`), `tasks.version`/`columns.version` are `bigint NOT NULL`, `users.password_hash` is `NOT NULL`, and `activity_log` plus its composite index exist.

## Task Commits

Each task was committed atomically:

1. **Task 1: V2 and V3 — optimistic-locking version columns and the activity_log table** - `18242cf` (feat)
2. **Task 2: V4 password_hash NOT NULL guard, then apply all four migrations against a wiped volume** - `34e94ce` (feat)

_Note: Task 2's commit message also documents the docker compose verification results, since that verification is unrepeatable state (a wiped-then-rebuilt volume) rather than a separate committable artifact._

## Files Created/Modified
- `src/main/resources/db/migration/V2__add_optimistic_locking_version_columns.sql` - Adds `version bigint NOT NULL DEFAULT 0` to `tasks` and `columns`
- `src/main/resources/db/migration/V3__add_activity_log.sql` - Creates `activity_log` table and its composite descending index
- `src/main/resources/db/migration/V4__add_password_hash_not_null.sql` - Guarded `users.password_hash SET NOT NULL` via a PL/pgSQL `DO` block

## Decisions Made
- Followed the plan's Approach A exactly: byte-faithful content port from each manual DDL script, idempotency/existence guards stripped (Flyway's `flyway_schema_history` replaces that safety net), V4's data-state `RAISE EXCEPTION` guard kept (Flyway has no visibility into existing row data).
- Left `docker-compose.yml`'s `SPRING_JPA_HIBERNATE_DDL_AUTO: update` override untouched — the plan explicitly scopes that change to Plan 03's `ddl-auto=validate` cutover, not this plan.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- The pre-commit hook (`spotlessApply` + `fastTest`) takes ~3.5 minutes per commit due to the Testcontainers-backed Kafka test classes spinning up real Redpanda containers; this is expected suite behavior (matches STATE.md's documented ~200-237s full-suite baseline), not a hang, though it looked stalled when tailing a backgrounded process's output file mid-run. First commit attempt was retried after a stray, still-warm Gradle daemon held a file lock from an earlier interrupted attempt (`Unable to delete directory '...\build\test-results\fastTest\binary'`) — resolved with `./gradlew --stop` before retrying, no code change involved.
- `docker compose build` for the `app` image took ~4.5 minutes (full Gradle build inside the multi-stage Dockerfile, no layer cache hit for source changes) — expected for a `--build` invocation, not investigated further.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
Plan 03 (the `ddl-auto=validate` cutover, D-03) can proceed: the full V1-V4 migration history is proven to produce exactly the schema shape the current entities expect on a genuinely empty database, which is the precondition Plan 03's Hibernate-validate step depends on. `docker-compose.yml`'s `SPRING_JPA_HIBERNATE_DDL_AUTO: update` override remains untouched and is explicitly Plan 03's concern per the phase's Open Question 1.

---
*Phase: 04.1-flyway-database-migration-implementation*
*Completed: 2026-08-05*
