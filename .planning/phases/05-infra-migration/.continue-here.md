---
context: phase
phase: 05-infra-migration
task: 0
total_tasks: 3
status: not_started
last_updated: 2026-08-16T20:11:30.455Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

None identified for the work ahead (Plan 05-06). Everything encountered this session that
required care was specific to already-completed Plans 05-04/05-05 and is fully resolved and
documented in `docs/INFRA_RUNBOOK.md`. See Advisory notes below for context worth knowing,
none of it gates resumption.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Docker Compose project name is directory-derived unless pinned | Moving `docker-compose.prod.yml` between directories (as Plan 05-05 Task 1 did, `/root` → `/opt/deploy/kanban-board-backend`) silently changes Compose's default project name and every named volume's actual name, orphaning already-running containers' data. Already fixed — `docker-compose.prod.yml` now carries a top-level `name: kanban-board-backend` pin (commit `5eea749`). | advisory | Do not remove or rely around this pin. If Plan 05-06's audit work ever needs to move or copy the compose file again, re-verify the pin travels with it. |
| `checkout@v4` still counted as "fixed" by a naive scope read | Three separate GitHub-Actions-deprecation quick tasks landed today (`260816-sv1`, `260816-uc8`); the fix (bump to `@v5`) is now applied uniformly across all 4 `checkout` references in `deploy.yml`. Nothing left open here, but if any NEW job is added to `deploy.yml` during 05-06, match `@v5`, not an older pinned example elsewhere in the file's history. | advisory | Grep `deploy.yml` for `checkout@` before assuming a version to copy. |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Phase 05 (infra-migration) is at **38/39 plans complete (97%)**. Plans 05-01 through 05-05 are
fully done, verified, and pushed. **Plan 05-06 — the phase's last plan — has not been started.**
Its `PLAN.md` was written back on 2026-08-04 (pre-dating the Oracle→Netcup pivot and everything
built since), so read it critically rather than assuming every line is still accurate — this
project has repeatedly found stale plan text this milestone (stale subject counts, stale
migration counts, stale Oracle-Cloud framing) and corrected it against live evidence rather than
trusting the document.

Today's session (2026-08-16) closed out Plan 05-05 (CI/CD cutover to Netcup) and did three
follow-on quick tasks: fixed GitHub Actions deprecation warnings across all of `deploy.yml`,
rewrote the CI/CD delivery-path sequence diagram to match current reality, and filed a real
(reproduced-twice) tooling bug in `gsd-tools`' STATE.md progress-percent writer.
</current_state>

<completed_work>

Completed Tasks (this session):
- Plan 05-05 Task 1 (secrets checkpoint) — verified complete from a prior session
- Plan 05-05 Tasks 2-3 (Flyway CI job + deploy-to-netcup rewrite) — verified against live evidence, one real latent bug fixed (`cleanup-old-images`' DELETE URL), a second deeper bug diagnosed and filed as a todo rather than guess-fixed
- Quick task 260816-sv1 — fixed `checkout@v3→v5`/`setup-java@v4→v5`/`adopt→temurin` in 3 of 4 jobs
- Quick task 260816-tqc — rewrote `docs/diagrams/infra-delivery-scenario.mmd`/`.png` as accurate current-state (this directly satisfies part of Plan 05-06's own stated artifact: "delivery diagram promoted from target state to current state" — **check this before redoing it**)
- Quick task 260816-uc8 — closed the last `checkout@v4→v5` gap in `deploy-to-netcup`
</completed_work>

<remaining_work>

**Plan 05-06 — Prove the migrated stack is closed/bounded, then decommission the AWS-era leftovers.** Three tasks per its `must_haves`:
1. External network audit — confirm only 22/80/443 are reachable from off-VM (not just reading local `iptables`/Netcup-panel rule listings — the plan is explicit that only an actual off-VM scan counts as evidence for INFRA-08)
2. Observed (not asserted) Docker log rotation — generate enough log volume to cross the configured `max-size`/`max-file` cap and confirm old files actually get deleted
3. Decommission: revoke the AWS-era GitHub secrets (`EC2_SSH_KEY`, `EC2_HOST`, `EC2_USER`, old `DB_*` — now safe, since the new pipeline has proven multiple green end-to-end deploys), prune accumulated Docker Hub tags from the migration window, correct any remaining doc that still describes the deleted AWS host as the deploy target

**Known complication for the Docker Hub tag-pruning sub-item:** `cleanup-old-images` (the job meant to prune tags automatically) has an open, diagnosed-not-fixed bug — its `DELETE` calls are rejected `unauthorized` (Docker Hub Hub API v2 needs a JWT token exchange, not Basic auth). See `.planning/todos/pending/2026-08-16-cleanup-old-images-delete-calls-rejected-unauthorized.md`. Plan 05-06's "tags pruned" acceptance criterion may need either fixing that bug first, or a manual one-time prune with the automation gap explicitly recorded rather than silently worked around.
</remaining_work>

<decisions_made>

- Today's CI-deprecation and diagram work was treated as separate quick tasks, not folded into Plan 05-06, since 05-06 hadn't started yet and the CI fixes were unrelated to its actual scope (network audit / log rotation / secret revocation).
</decisions_made>

<blockers>
None currently.
</blockers>

## Required Reading (in order)
1. `.planning/phases/05-infra-migration/05-06-PLAN.md` — full task detail, threat model, acceptance criteria (read critically — written pre-Netcup-pivot)
2. `docs/INFRA_RUNBOOK.md` — current, accurate VM/deploy-user/secret state; the "Repository secret inventory" section near the end lists exactly which secrets exist today and under what names (note: `DB_HOST`/`DB_NAME`/`DB_USER`/`DB_PASS` deliberately reuse AWS-era secret *names* with new Neon values — don't confuse these with the actual dead AWS secrets, which are `EC2_SSH_KEY`/`EC2_HOST`/`EC2_USER` and haven't been touched)
3. `docs/diagrams/infra-delivery-scenario.mmd` / `docs/INFRA_ARCHITECTURE.md` — already current-state as of today; check before assuming 05-06's diagram artifact still needs work

## Infrastructure State
- **Netcup VM** (`netcup-prod` SSH alias, root; also `deploy` user, docker-group, no sudo, key at `~/.ssh/netcup_deploy_key`): `caddy`/`app`/`redpanda` all running healthy under Compose project `kanban-board-backend` (pinned). Deploy directory `/opt/deploy/kanban-board-backend`.
- **GitHub Actions**: `deploy.yml` fully green pipeline (`run-tests` → `build-and-push-docker-image` + `flyway-verify` in parallel → `deploy-to-netcup` → `cleanup-old-images`/`cleanup-unused-image`). All `checkout`/`setup-java` references on current, non-deprecated versions.
- **AWS-era secrets** (`EC2_SSH_KEY`, `EC2_HOST`, `EC2_USER`) are still present and unrevoked in GitHub repo settings — this is exactly what Plan 05-06 exists to close out.

<context>
This was a long session covering Plan 05-05's full close-out plus three follow-on quick tasks.
Everything is committed and pushed (`origin/master` at `071267f` as of this handoff). The user
is deliberately pausing here to start a fresh session — nothing is mid-task, this is a clean
boundary, not a recovery scenario.
</context>

<next_action>
Start with `/gsd-execute-phase 05` (or `/gsd-plan-phase 05` first if 05-06's existing PLAN.md
needs revision given how much has changed since it was written) to begin Plan 05-06 — the
phase's last remaining plan.
</next_action>
