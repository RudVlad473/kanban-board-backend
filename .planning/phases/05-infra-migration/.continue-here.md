---
context: phase
phase: 05-infra-migration
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-08-14T11:48:49.824Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: If the Netcup VM ever goes unexpectedly unreachable (SSH and/or ping) right
  after touching the Netcup Cloud Firewall in the SCP, do NOT assume the ruleset is misconfigured
  and start re-editing rules. Try toggling "Firewall active" off then back on first — this exact
  session hit a 7+ minute total-unreachability incident that a toggle cycle fixed, with the SCP
  displaying a correct ruleset the entire time (a stuck sync/propagation bug on Netcup's side).
- [ ] CONSTRAINT: Do not assume Netcup's deploy target is ARM64. It is x86_64 (confirmed via
  `uname -a` on the provisioned VM). `.github/workflows/deploy.yml` was fixed this session
  (linux/arm64 → linux/amd64) but `docs/INFRA_ARCHITECTURE.md` still describes the OLD
  Oracle/ARM64 topology throughout its Mermaid diagrams — do not trust that document's
  architecture claims until it's updated.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Netcup Cloud Firewall can silently desync from its displayed ruleset | Assigning a firewall policy to the VPS for the first time made the server totally unreachable (SSH and ICMP both) for 7+ minutes, despite the SCP showing a correct rule set the whole time (SSH/80/443 ACCEPT ahead of the implicit drop-all). Confirmed via the VNC/serial console that the VM itself was fine — not wedged, not crashed. Toggling "Firewall active" off then on again fixed it immediately and it has stayed stable since. | blocking | Before assuming a rule is wrong, toggle the policy off, retest, toggle back on, retest again. Only start editing rule content if the problem survives a clean toggle cycle. |
| Manual `ssh root@<ip>` fails even though key-based access works | The user hit `Permission denied (publickey)` running a plain `ssh root@159.195.114.230` — their SSH client was offering their personal default key (`~/.ssh/id_ed25519`), which was never installed on this server. Only the dedicated `kanban-backend-prod-netcup` key (`~/.ssh/id_ed25519_netcup_prod`) is in `authorized_keys`. | advisory | Use `-i ~/.ssh/id_ed25519_netcup_prod` explicitly, or the `ssh netcup-prod` alias now configured in the user's local `~/.ssh/config` (`IdentitiesOnly yes` so it never falls back to a different key). |
| Oracle-era assumptions hide in files nobody thought to re-check | `.github/workflows/deploy.yml`'s Docker build step was hardcoded to `linux/arm64` with an explicit Oracle-A1-Flex-citing comment — found only because this session did a genuine re-validation pass on `docker-compose.prod.yml`/Caddyfile/`.env.prod.example` per the prior handoff's own `05-02-revalidate-for-netcup` task, and happened to check the CI workflow too. It was NOT on the prior handoff's explicit remaining-work list. | blocking | Before treating the Oracle→Netcup re-validation as complete, explicitly grep the whole repo for `arm64`/`ARM64`/`Ampere`/`A1.Flex`/`Oracle` — don't assume the prior handoff's list of "files to check" was exhaustive. `docs/INFRA_ARCHITECTURE.md` is the next known offender, not yet fixed. |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Phase 05 (infra-migration), plan 05-03, Task 2 of 3 is now fully DONE (provision + harden the
production VM). Task 3 (Neon + DuckDNS) has not started. The user explicitly chose to stop the
session at this exact boundary rather than continue into Task 3.
</current_state>

<completed_work>

**This session (2026-08-14, resumed from an accidental `/clear` mid-SSH-setup):**
- Confirmed SSH access to the Netcup VM (`159.195.114.230`, Vienna datacenter) via the
  pre-generated `~/.ssh/id_ed25519_netcup_prod` key — the user reported this was already working
  before the session reset, verified directly rather than re-discovered.
- Docker was already installed correctly (official `download.docker.com` apt repo, `docker-ce`
  29.7.2, `docker-compose-plugin` v5.4.0) — this must have happened before the reset too, no
  install work needed this session.
- Installed `iptables-persistent`, applied the OS-level firewall (default-drop INPUT, allow
  established/related + loopback + TCP 22/80/443), saved persistently, verified survival across a
  full reboot.
- Hardened SSH: `PasswordAuthentication no`, `PermitRootLogin prohibit-password` in
  `/etc/ssh/sshd_config`. Verified both that key-auth still works and that a forced
  password-auth attempt is correctly rejected.
- External port probe (off-VM): 22 open, 80/443/8080/8081/9092 all closed (80/443 closed is
  expected — nothing is deployed to listen on them yet).
- Configured the Netcup Cloud Firewall (SCP policy "Default"/"Basic firewall policy", same
  22/80/443 ruleset, assigned to this VPS) — hit and resolved the toggle-desync incident
  documented in the anti-patterns table above.
- Set up a local `~/.ssh/config` `Host netcup-prod` entry so `ssh netcup-prod` works without `-i`.
- Wrote `docs/INFRA_RUNBOOK.md` — provider, datacenter (Vienna), spec, OS, Docker versions, both
  firewall layers' rule sets and the known Netcup gotcha, verified state as of 2026-08-14.
- Found and fixed a real bug while re-validating `docker-compose.prod.yml`/Caddyfile/
  `.env.prod.example` against Netcup's shape (the prior handoff's `05-02-revalidate-for-netcup`
  task): `.github/workflows/deploy.yml`'s Docker build step was hardcoded to `linux/arm64`
  (Oracle-era leftover) — fixed to `linux/amd64`, dropped the now-unneeded QEMU step. User
  confirmed "fix now" when asked.
- Fixed two stale Oracle-referencing comments in `docker-compose.prod.yml` (header, Redpanda
  resource-cap comment) to reflect Netcup/measured reality instead.
- Verified Caddyfile has no provider-specific assumptions — untouched, already correct.

**Carried over from before this session (Oracle→Netcup pivot, prior session — see prior
HANDOFF/git history for full detail, not re-litigated here):** hosting target decision, SSH
keypair generation, VM ordering.

</completed_work>

<remaining_work>

**Immediate next step:** Task 3 — Neon + DuckDNS:
- Create a Neon project, region closest to Vienna, Austria (the actual server location — NOT
  Zurich, which was the old Oracle-plan assumption). Leave the DB empty.
- Locate (do not share/record in any file) both the pooled and direct connection strings.
- Register a DuckDNS (or equivalent free dynamic-DNS) subdomain, point its A record at
  `159.195.114.230`, confirm DNS propagation from off-VM. Do NOT attempt certificate issuance yet
  — that's plan 05-04's job once Caddy is actually deployed.
- The agent has no Neon or DuckDNS credentials — the account-side clicking is the user's; the
  agent can verify DNS resolution/connectivity once the user reports the subdomain chosen.
- Append provider/location, Neon project name/region, pooled-vs-direct distinction, subdomain,
  and hostname to `docs/INFRA_RUNBOOK.md`.

**Then:**
- Write `.planning/phases/05-infra-migration/05-03-SUMMARY.md` — must explicitly document the
  Oracle→Netcup provider switch AND this session's ARM64→AMD64 CI fix, not just describe the
  final Netcup state as if it were the original plan. Do not write this before Task 3 is done —
  a SUMMARY for an incomplete plan misrepresents it as finished.
- Proceed to wave 2 (plan 05-04, the tracer deploy) once 05-03 is merged and summarized — but
  first re-check `05-04-PLAN.md` and `05-05-PLAN.md` for other Oracle-specific assumptions beyond
  the already-corrected DDL-script premise (prior session) — this has NOT been done yet, still
  open from the prior handoff too.

**Separately, unrelated to Phase 5, not gating it, carried forward across many sessions now:**
- **User action needed:** strip the leading `?` from `DB_JDBC_PARAMS` in `.env.prod.example`
  (target: `DB_JDBC_PARAMS=sslmode=require&channel_binding=require&prepareThreshold=0`) — the
  agent cannot edit this file (permission deny rule on env files). The variable *name* was
  already fixed between sessions (`DB_URL_PARAMS` → `DB_JDBC_PARAMS`), only the leading `?`
  remains.
- Decide on the `openapi.json` anomaly (restore the committed file, or confirm the deletion was
  intentional and commit it) — flagged multiple times, still unresolved.
- **New this session:** `docs/INFRA_ARCHITECTURE.md`'s Mermaid diagrams still describe the
  Oracle/ARM64 topology throughout — deliberately not fixed this session (real diagram-redraw
  work per `docs/DIAGRAM_CONVENTIONS.md`, out of scope for VM provisioning). Needs its own pass
  before or during 05-04/05-05.

</remaining_work>

<decisions_made>

- Fixed `deploy.yml`'s Docker build platform (`linux/arm64` → `linux/amd64`), dropped QEMU —
  confirmed via `uname -a` that Netcup is x86_64; the old build step was a genuine, previously
  undiscovered latent bug that would have broken 05-04/05-05's deploy silently. User chose to fix
  immediately rather than defer.
- Hardened SSH beyond the plan's literal branch coverage (disabled password auth even though the
  plan only explicitly called for that "if password-issued", and this VM's access was
  key-issued) — leaving password auth on with port 22 as the only open SSH-adjacent port is a
  real exposure the plan's spirit clearly intended to close.
- Set up the Netcup Cloud Firewall as a genuine second layer (user confirmed) rather than relying
  on OS-level iptables alone, matching the original OCI dual-layer design intent.
- Deliberately did NOT fix `docs/INFRA_ARCHITECTURE.md`'s stale Oracle/ARM64 diagrams this
  session — filed as explicit remaining work instead of silently leaving it undiscovered.
- Deliberately stopped before Task 3 at the user's explicit choice, rather than continuing
  through Neon/DuckDNS in the same session.

See `.planning/HANDOFF.json` for the structured version of all of the above plus the fuller
decision/rationale log.

</decisions_made>

<blockers>
**Non-blocking, carried forward across many sessions:** `.env.prod.example`'s leading-`?` strip
(permission-blocked for the agent); the `openapi.json` deletion anomaly.

**Nothing is currently blocking.** Task 3 requires the user's own Neon/DuckDNS account actions,
but that's normal task shape, not a blocker in the sense of something stuck.
</blockers>

## Required Reading (in order)
1. This file's `<decisions_made>` and the anti-patterns table above — the Netcup-firewall-toggle
   gotcha and the ARM64/AMD64 correction are the two things most likely to trip up a fresh resume.
2. `.planning/HANDOFF.json` — full structured decision log.
3. `docs/INFRA_RUNBOOK.md` — the actual current state of the provisioned VM (provider, spec,
   firewall, access) — treat this as more current than any Oracle-era plan text.
4. `.planning/phases/05-infra-migration/05-03-PLAN.md` — Task 3's original steps (Neon + DuckDNS
   portion is provider-agnostic, unaffected by the Oracle→Netcup pivot except the target IP/region).

## Infrastructure State
- **Netcup VM:** `159.195.114.230`, Vienna datacenter, Debian 13, 4 vCPU/7.8GiB/251GB, Docker
  29.7.2 active, both firewall layers configured and verified (22/80/443 only). SSH:
  `ssh netcup-prod` (local alias) or `ssh -i ~/.ssh/id_ed25519_netcup_prod root@159.195.114.230`.
- **Neon:** no project created yet.
- **DuckDNS:** account registered (per user, prior session) but no subdomain claimed or A record
  set yet.
- **Oracle Cloud:** unchanged from prior sessions — Reserved IP, VCN/subnet still exist unused,
  zero cost to leave alone. Poller process was NOT found running when checked this session
  (`ps aux` showed nothing matching) — likely stopped at some point; not investigated further
  since it's a non-blocking background bet, not the active path.
- **Git:** nothing from this session committed yet — this pause/handoff is being committed now.
  `openapi.json`'s deletion and `.planning/ROADMAP.md`/`WINDOWS.md` modifications are pre-existing
  uncommitted state from before this session started, not touched this session.

<context>
This session resumed from an accidentally-`/clear`'d session that was mid-SSH-setup on a Netcup
VPS. Rather than re-discovering state from scratch, the user directly confirmed SSH was already
working (key-based) and provided the server IP, letting the session skip straight to verifying
and continuing Task 2's remaining steps. Docker turned out to already be installed. The core
firewall/hardening work went smoothly except for one real incident: assigning the Netcup Cloud
Firewall policy made the server totally unreachable for 7+ minutes despite a correct-looking
ruleset, diagnosed via the VNC console (VM itself fine) and fixed by a toggle-off/on cycle — a
genuine provider-side bug now documented as a known gotcha. A second, unrelated real bug was
found during the re-validation pass the prior handoff had already scheduled: the CI/CD pipeline's
Docker build was still targeting ARM64 from the Oracle days, which would have broken the eventual
deploy to this x86_64 box — fixed with the user's explicit go-ahead. The user also hit a
`Permission denied (publickey)` trying to SSH in manually, traced to their client offering the
wrong (personal, not server-specific) key by default, fixed with an SSH config alias. The session
ends cleanly at the user's own choice to stop before Task 3, not because of any blocker.
</context>

<next_action>
Resume at 05-03 Task 3: create a Neon project (region closest to Vienna, Austria), leave it
empty, locate but do not record connection strings. Register a DuckDNS subdomain, point its A
record at 159.195.114.230, confirm propagation. The user needs to do the account-side steps
(no credentials available to the agent for either service) — ask them to start, then verify DNS/
connectivity once they report back. Append the results to docs/INFRA_RUNBOOK.md. Only after Task
3 is fully done, write 05-03-SUMMARY.md covering the whole plan including the provider pivot and
this session's ARM64->AMD64 fix — then commit, push, and proceed toward re-checking 05-04/05-05
for other Oracle-specific assumptions before executing them.
</next_action>
