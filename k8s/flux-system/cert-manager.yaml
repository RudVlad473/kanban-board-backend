# Suspended until 13-06's maintenance window (D-02, D-17). Plan 13-02.
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cert-manager
  namespace: flux-system
spec:
  interval: 10m
  path: ./k8s/platform/cert-manager
  prune: true
  suspend: true
  dependsOn:
    - name: platform
  sourceRef:
    kind: GitRepository
    name: flux-system
