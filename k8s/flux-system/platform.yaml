# Flux Kustomization for k8s/platform: namespaces + interim Traefik HelmChartConfig. Plan 13-02.
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: platform
  namespace: flux-system
spec:
  interval: 10m
  path: ./k8s/platform
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: flux-system
